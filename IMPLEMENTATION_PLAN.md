# IMPLEMENTATION_PLAN.md — The Kicker

## Current status

- **Branch:** `v3.0-TheKicker` (will rename to `kicker-mvp` at K0a per master plan; for now the bootstrap and R0 slices live on this branch).
- **Source spec:** `/Users/tombutler/.claude/plans/sleepy-moseying-ripple.md` — user-approved Kicker pivot plan; read in full before any K-slice.
- **Handoff folder (gitignored, read-only reference):** `the_kicker_handoff/` — README, CLAUDE.md, JSX prototypes, HTML preview snapshots, screenshots. Do NOT commit; do NOT delete.
- **Live deployment:** still serving v3 from `v3.0-redesign` branch / `main`. No urgency, no forced cutover. Cut over at K0h (`k1.0`) sign-off.
- **v3 closed-out state:** `v3.1`–`v3.15` tags applied (verified `git tag --sort=v:refname | grep "^v3\."`). v3 plan archived at `docs/archive/v3-broadcast/IMPLEMENTATION_PLAN.md`. Phase 3 sub-plans archived at `docs/archive/v3-broadcast/2026-04-26-phase-3-per-hub/`. Phase 0/1/2 sub-plans remain in `docs/superpowers/plans/` as signed-off historical references.
- **Stack target (K0a onwards):** SvelteKit + TypeScript + Vitest + Playwright + Tailwind + `@anthropic-ai/sdk`. Replaces v3's Svelte 4 + Vite + svelte-routing setup in-place.
- **Predictions moat:** salvaged engines (`optimizedPredictions`, `advancedPredictions`, `predictionTracker`, `kelly`, `value`, `calibrationIndex`, `gameweek`, `dataService`, `footballData`, `adapters/v3`, `types/`) carry over read-only into `frontend/src/lib/preserved/` at K0a. UI is wholly replaced; data layer is preserved.
- **Tag track:** R0 is **untagged** (research-only, plan-file edit, no git commit). `k0.1`–`k0.7` cover the seven pre-MVP code slices K0a–K0g. `k1.0` lands at MVP cutover (K0h sign-off). `k1.x` for polish (K1a–K1d → `k1.1`–`k1.4`). `k2.x` for Phase 2 (K2a–K2d → `k2.1`–`k2.4`). Convention: every commit-bearing slice line uses the arrow `→ k0.N`; R0 uses `→ no tag` to make its untagged status explicit (avoids "missing arrow = forgot to fill in" misreads on future loop scans).
- **Validation gates:**
  - **Auto-gated** slice = `npm run check` 0/0 + `npm run test -- --run` green → ralph commits and flips `[x]` autonomously.
  - **Manual-gated** slice = ralph commits when validation passes, then stops with a one-paragraph summary. Human flips `[x]` after sweep.
- **Mid-loop feedback channel:** `## Human notes for next iteration` (below). Ralph consumes notes in the next iteration as part of the slice contract; addressed notes move to `## Notes / discoveries` prefixed `(addressed)`.

## Active phase

**🔀 Pivot to The Kicker** — newsprint tabloid UX with 10 AI columnist personas. Same backend, same prediction engine, same Football-Data feed; completely different presentation metaphor. The first code-bearing slice is K0a (scaffold). Before that, **R0 (deep research pass)** must enrich this plan with verbatim persona prompts, component contracts, screen layout grammars, and broadsheet JSON shapes pulled directly from the handoff.

**Pre-flight gate before R0 starts:** the user must confirm "all reference assets are in place — start R0." This guards against re-doing R0 if more screenshots / preview snapshots arrive after the read pass. Do not run R0 until that confirmation lands in the loop input.

**Loop slice order:**
1. **R0** (next active slice) — research-only, plan-file edit only, no code, no commits in repo
2. **K0a–K0h** — MVP build (handoff Steps 1–6) ending at `k1.0` cutover
3. **K1a–K1d** — polish (handoff Steps 7–9 + Settings)
4. **K2a–K2d** — Phase 2 (mobile + audio + paywall)

## Ordered checklist

### Phase R — Research

- [ ] **R0 — Deep research pass** *(Manual-gated, plan-file edit only — produces no git commit; deliverable is the enriched master plan at `/Users/tombutler/.claude/plans/sleepy-moseying-ripple.md`)* → no tag (research-only) — Read all 15 handoff files in full (README, CLAUDE.md, 11 JSX files, The Kicker.html index, sample 2–3 preview snapshots, all 8 screenshots). Append a `## Design reference (research output)` section to the master plan covering: (1) verbatim 13-token system with consumer mapping; (2) verbatim `systemPrompt` strings for all 10 personas with id/name/region/accent/tic/voice/length-bound/sample-one-liner; (3) component contracts for all 12 primitives (`GeoffTicker`, `GeoffMasthead`, `GeoffMessage`, `UserMessage`, `GeoffComposer`, `MatchSheetCard`, `CheersGeoffCallout`, `HotTake`, `SectionRule`, `CornerOrnament`, `PunditCard`, `VoiceColumn`) with props/slots/states/data-attrs/tokens/persona-accent/screenshot-ref; (4) screen layout grammars for Home, Onboarding, Roster, Voice Range, Mickey broadsheet, Gaffer broadsheet, Landing, mobile variants; (5) token-parser canonical examples from `KICKER_VOICE_TAKES`; (6) Mickey + Gaffer broadsheet JSON shape examples; (7) pricing tier entitlement matrix; (8) design ideas surfaced (with file+line citations); (9) open questions for user; (10) actionable plan deltas naming which slice to amend and how. **Loop posts a one-paragraph summary** ("R0 complete. Plan grew by N lines. Top 3 design ideas: …. Top 3 open questions: …. Top 3 proposed deltas: ….") and stops. User reviews + approves before any K-slice begins.

### Phase K0 — Kicker MVP (handoff Steps 1–6)

- [ ] **K0a — Scaffold + tokens** *(Auto-gated)* → `k0.1` — Branch `kicker-mvp` off current branch. Archive existing `frontend/` to `archive/v3-frontend` side-branch. Wipe `frontend/` on `kicker-mvp`. Scaffold fresh SvelteKit (`npm create svelte@latest .`): TypeScript + Vitest + Playwright + ESLint + Prettier. Install Tailwind, `@anthropic-ai/sdk`, `lucide-svelte`. Create `frontend/src/lib/tokens.css` (13 newsprint custom properties verbatim from handoff CLAUDE.md). Wire Google Fonts in `app.html` (Instrument Serif regular+italic, Inter 400–800, JetBrains Mono 400–700). Extend `tailwind.config.ts` with token aliases (`paper`/`ink`/`red`/`amber`/`green`/`rule`). Salvage v3 prediction/data layer into `frontend/src/lib/preserved/` (read-only): `services/api/footballData.ts`, `services/dataService.ts`, `lib/optimizedPredictions.ts`, `lib/advancedPredictions.ts`, `services/predictionTracker.ts`, `services/betting/kelly.ts`, `services/betting/value.ts`, `lib/calibrationIndex.ts`, `lib/gameweek.ts`, `lib/adapters/v3.ts`, `types/`, all co-located `.test.ts`. Create `docs/the-kicker-spec/` with `tokens.md`, `personas.md`, `build-order.md`, `screens-inventory.md` (committed mirror of handoff load-bearing content; insurance against gitignored handoff being moved/deleted). **Validation:** `npm run dev` boots; `/` renders blank SvelteKit page on cream `--paper`; svelte-check 0/0; vitest 0/0.

- [ ] **K0b — Persona system** *(Auto-gated)* → `k0.2` — Create `frontend/src/lib/personas/` with 10 `.ts` files (one per persona) + `index.ts` aggregating into `PERSONAS` map. Each exports a `PersonaConfig {id, name, short, region, accent, tic, voice, systemPrompt}`. Source content from `the_kicker_handoff/design_reference/kicker-personas.jsx` + README persona table; verbatim `systemPrompt` per R0 output. Create `frontend/src/lib/stores/persona.ts` — Svelte writable, localStorage-backed (`kicker:personaId`), default `'voice'` (legal-safe-default; flag in commit message for user override). **Tests:** `personas.test.ts` (every persona has all required fields; all 10 IDs present; no duplicate accents; no real-pundit-name strings via deny-list regex); `personaStore.test.ts` (set/read/reset, localStorage round-trip, SSR-safe `typeof localStorage === 'undefined'` guard). **Validation:** targeted vitest passes; svelte-check 0/0.

- [ ] **K0c — Shared primitives part A: structural** *(Auto-gated)* → `k0.3` — 5 components: `lib/components/broadsheet/GeoffTicker.svelte` (running black tape), `GeoffMasthead.svelte` (compact + full variants), `SectionRule.svelte` (kicker + title + optional byline), `CornerOrnament.svelte` (decorative ink corners), `MatchSheetCard.svelte` (uses preserved `matchToFixture` adapter). Match pixel-for-pixel against handoff screenshots + `geoff-shared.jsx`. Use only `tokens.css` custom properties; no inline hex (per handoff CLAUDE.md rule 2). One `.test.ts` per component asserting marker presence (`[data-component="geoff-ticker"]` etc.), token-class application, prop-driven variants. ~20 tests across 5 files.

- [ ] **K0d — Shared primitives part B: chat + callouts** *(Auto-gated)* → `k0.4` — 5 components: `lib/components/chat/GeoffMessage.svelte` (3-column grid: avatar · body · marginalia; `cheers` prop for amber stamp), `UserMessage.svelte` (terminal-styled mono, right-aligned), `GeoffComposer.svelte` (composer + prompt-chip strip + active-persona indicator), `lib/components/broadsheet/CheersGeoffCallout.svelte` (amber hatched box), `HotTake.svelte` (large left-border pull quote). Tests ~20 across 5 files. `GeoffMessage` parses inline tokens via mocked parser output (real parser arrives K0e).

- [ ] **K0e — Token parser + headless data context** *(Auto-gated)* → `k0.5` — `lib/utils/parseGeoffResponse.ts` verbatim from handoff README; splits Claude response into `{type:'text'|'fixture'|'cheers', ...}` parts. 8+ tests: pure text, single FIXTURE token, single CHEERS token, both interleaved, malformed token (graceful degradation), empty input, escaped brackets, multiline. `lib/context/buildKickerContext.ts` — **the predictions moat** — aggregates preserved engines into structured JSON for system-prompt injection (`fixtures` with `ourProb`/`marketImplied`/`valueEdge`, `standings`, `accuracyStats {brier, calibration, sampleSize}`). Wires `dataService.getMatches({upcoming:true, days:7})` → `optimizedPredictions` → `predictionTracker.getAccuracyStats()` → `betting/value.identifyValueBets()`. ~6 tests covering happy path, empty fixtures, missing odds, no settled history.

- [ ] **K0f — API layer** *(Auto-gated)* → `k0.6` — `frontend/src/routes/api/chat/+server.ts` streaming Haiku endpoint: POST `{personaId, messages, threadId?}`, server calls `buildKickerContext()` and injects into system prompt, streams `claude-haiku-4-5` (max_tokens 256) as `text/event-stream` ReadableStream, rate-limit 10 req/min/IP via in-memory map keyed on `x-forwarded-for`. `frontend/src/routes/api/broadsheet/+server.ts` one-shot Sonnet endpoint: POST `{personaId, gameweek}`, server calls `buildKickerContext({daysAhead:14, includeStandings:true})`, calls `claude-sonnet-4-5` with BROADSHEET_PROMPT, parses to JSON, returns `{broadsheet: BroadsheetJson, generatedAt: ISO}`. Anthropic key from `process.env.ANTHROPIC_API_KEY` (never client-exposed). ~6 integration tests via mocked Anthropic SDK: chat happy path, persona ID validation (404), rate-limit boundary (429 on req 11), broadsheet JSON-parse failure (500), missing API key (503), context injection asserts `ourProb` reaches the system prompt. **Cutover decision point**: live deployment still on v3; K0f does not flip DNS.

- [ ] **K0g — Onboarding** *(Auto-gated)* → `k0.7` — `frontend/src/routes/onboarding/+page.svelte` 3-screen first-run flow per `kicker-onboarding.jsx`: (1) Welcome "Pick your pundit"; (2) Roster picker (5×2 grid of `PunditCard`); (3) "Open my paper" CTA → writes `personaStore` + navigates `/`. `frontend/src/routes/+layout.svelte` root layout: mounts `<GeoffTicker />`, sets `data-persona` on `<html>` for accent scoping, onboarding gate (`localStorage.getItem('kicker:personaId') === null` → `goto('/onboarding')`). `lib/components/roster/PunditCard.svelte` primitive (parallelogram monogram + fingerprint stats + tic + CTA; reused by `/roster` in K1b). ~5 tests: route renders, gate fires on missing persona, "Open my paper" persists + navigates, `prefers-reduced-motion` removes ticker marquee.

- [ ] **K0h — Home chat (MVP closes here)** *(Manual-gated, signed off via spec sweep at `docs/the-kicker-spec/sweep-k0h.md`)* → `k1.0` — **First user-shippable slice; live deployment cuts over from v3 to The Kicker after sweep passes.** `frontend/src/routes/+page.svelte` three-column home from `geoff-home.jsx`: left rail (saved threads + watchlist via `lib/stores/threads.ts` adapted from preserved v3 `oracle/threads.ts`, localStorage key `kicker:threads`); centre (streaming chat — `GeoffComposer` at bottom, `GeoffMessage`/`UserMessage` history, parsed tokens swap in `MatchSheetCard`/`CheersGeoffCallout` inline); right rail (today's fixture stack from `dataService.getMatches({days:1})` → 3 `MatchSheetCard`s with `data-rail-card`). Streaming: client `fetch('/api/chat',{body,signal})` consumes ReadableStream via `getReader()`, accumulates partial text, re-parses tokens per chunk, debounced re-render at 60fps. Persona accent applied via `--persona-accent` on `<html data-persona="...">`. ~8 tests: route renders, three-column layout (1024+), empty chat history, send-streams-response (mocked endpoint), token swap (FIXTURE → MatchSheetCard not raw text), thread persist+retrieve, persona switch updates `data-persona`, mobile collapses to single column below `lg`. **Manual sweep (15+ items in `docs/the-kicker-spec/sweep-k0h.md`):** persona switching mid-conversation, marginalia rendering on long messages, thread sidebar interaction, fixture rail accuracy vs football-data, axe-core a11y scan, theme parity (no dark-mode escape paths). **Cutover playbook after sign-off:** rebase `kicker-mvp` onto `main` → fast-forward → push `main` → Vercel auto-deploys → smoke-test prod URL on 3 personas + 3 routes (`/`, `/onboarding`, `/api/chat`) → tag `k1.0` at deployment commit.

### Phase K1 — Post-MVP polish (handoff Steps 7–9 + Settings)

- [ ] **K1a — Broadsheet** *(Manual-gated)* → `k1.1` — `routes/broadsheet/+page.svelte` triggers `/api/broadsheet`, renders JSON into broadsheet template per `kicker-broadsheets.jsx`. Cache result in localStorage keyed `kicker:broadsheet:gw{N}:{personaId}`.
- [ ] **K1b — Roster** *(Manual-gated)* → `k1.2` — `routes/roster/+page.svelte` (5×2 PunditCard grid + voice-range demo).
- [ ] **K1c — Landing** *(Manual-gated)* → `k1.3` — `routes/landing/+page.svelte` per `kicker-landing.jsx` — pricing tiers (Touchline / Press Box / Print Run); **no auth wiring yet** (paywall is K2c).
- [ ] **K1d — Settings** *(Auto-gated)* → `k1.4` — `routes/settings/+page.svelte` — persona switcher, API key config (Football-Data + bring-your-own-Anthropic option), notifications, data export.

### Phase K2 — Audio + mobile + monetisation (handoff Step 10 + new)

- [ ] **K2a — Mobile responsive pass** *(Manual-gated)* → `k2.1` — Below-`lg` everywhere; bottom-sheet persona switcher; single-column home; mobile broadsheet tweaks per `geoff-mobile.jsx`.
- [ ] **K2b — ElevenLabs audio per persona** *(Manual-gated)* → `k2.2` — 10 voice IDs (one per persona); audio column on broadsheet; persistent audio player.
- [ ] **K2c — Auth + paywall** *(Manual-gated)* → `k2.3` — Stripe-backed Press Box (£4/mo) and Print Run (£20/mo); Touchline gates persona picker to `voice` only. **Legal blocker:** "Cheers, Geoff is cultural parody — clear with a lawyer before monetising" (handoff README). Park until lawyer review lands.
- [ ] **K2d — Print Run logistics** *(Deferred indefinitely)* → `k2.4` — Physical broadsheet generation + post-on-demand integration (`lob.com` or similar). Activate only if Print Run tier proves viable.

## Notes / discoveries

*(Filled by ralph as it discovers blockers. The bootstrap slice produced no discoveries beyond what the master plan already names.)*

- **(K-bootstrap, no blocker, 2026-05-03)** v3 tags `v3.1`–`v3.15` already applied (verified via `git tag --sort=v:refname | grep "^v3\."`). No tag operations required during bootstrap. Plan archive moved cleanly via `git mv`. `docs/superpowers/plans/2026-04-26-phase-{0,1,2}` retained in place per master plan (signed-off historical references, not active).
- **(R0 → K0a handoff, advisory)** R0 is plan-file-only; its deliverable lives at `/Users/tombutler/.claude/plans/sleepy-moseying-ripple.md` (outside the repo, in the user's `.claude/plans/` dir). R0 produces **no git commit in this repo**. After R0 lands and the user approves, the next loop input should explicitly say "K0a — start" to begin code-bearing work.

## Human notes for next iteration

*(Empty.)*

## Open follow-ups (not blocking MVP)

- **Persona default:** handoff CLAUDE.md says `'scouser'`; pricing tiers say Touchline tier is `'voice'`-only. K0b will go `'voice'` for legal-safe default; K2c paywall enforces tier-driven access. Surface in K0b commit message; user can override.
- **Anthropic API key:** server-held by default (`process.env.ANTHROPIC_API_KEY`). K1d may also offer bring-your-own-key for Touchline tier — TBD.
- **Football-Data caching:** preserved 3-tier IndexedDB cache (`dataService.ts`) survives. Verify SvelteKit SSR doesn't break the IndexedDB-only path; may need server-side memory fallback. Address in K0a or K0e.
- **Legal:** "Cheers, Geoff is cultural parody — clear with a lawyer before monetising" — K2c blocker, not K1.0 blocker.
- **SEO / OG / structured data:** out of scope for MVP; landing (K1c) gets basic meta tags; deeper SEO is Phase 3.
- **Cutover communication:** at K1.0 land, decide whether to redirect old v3 routes (`/today`, `/fixtures/*`, `/predictions/*`) → 404 or 301 to `/`. Recommend 301 to `/` for first 90 days, then drop redirects.
