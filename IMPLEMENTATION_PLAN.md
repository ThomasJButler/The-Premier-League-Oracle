# IMPLEMENTATION_PLAN.md

## Current status summary and code review

- **Branch:** `v3.0-redesign`
- **Source spec:** `docs/superpowers/specs/2026-04-26-frontend-broadcast-redesign-design.md`
- **Active phase plan:** `docs/superpowers/plans/2026-04-26-phase-3-per-hub/index.md` — each slice has its own file (Fixtures sub-phase plans landed first; remaining sub-phases drafted as the loop reaches them).
- **Phase 2 plan archive:** `docs/superpowers/plans/2026-04-26-phase-2-today/` — complete; P2a/P2b/P2c all signed off (tagged `v3.1`/`v3.2`/`v3.3`).
- **Phase 1 plan archive:** `docs/superpowers/plans/2026-04-26-phase-1-matchcard/` — complete; P1a + P1b auto-gated, P1c signed off 2026-04-26.
- **Phase 0 plan archive:** `docs/superpowers/plans/2026-04-26-phase-0-foundation/` — complete except for the optional manual visual sweep on P0-cp (recommended but not blocking).
- **Stack:** Svelte 4 + Vite + TypeScript + Tailwind 3. `svelte-routing` is added in P0b. No SvelteKit migration; SPA + Vercel rewrite handles fallback.
- **Strangler-fig contract:** every iteration leaves the app in a working state. Legacy components stay until their replacement slice's commit deletes them. Killed-feature components (Suggested Bets / Accumulators / Betting History) are deleted in P0b alongside their nav-link removal.
- **Validation gates:**
  - **Auto-gated** slice = `npm run test -- --run` green + `npm run check` clean → ralph commits and flips `[x]` autonomously.
  - **Manual-gated** slice = ralph commits when `npm run check` passes, then stops with a one-paragraph summary. Human flips `[x]` after eyeball.
- **Mid-loop feedback channel:** `## Human notes for next iteration` (below). Ralph consumes notes in the next iteration as part of the slice contract; addressed notes move to `## Notes / discoveries` prefixed `(addressed)`.

## Active phase

Phase 3 (Per-hub screens) — **kicked off 2026-04-26 after Phase 2 sign-off.** P2b + P2c both signed off, checkboxes flipped to `[x]`, retroactively tagged `v3.2` (de45aff) and `v3.3` (b08d670) at the sign-off moment. Phase 2 is closed; tag sequence `v3.1` → `v3.2` → `v3.3` covers every Phase 2 slice. Phase 3 spans **17 slices across 5 hubs**: Fixtures (P3a/P3b/P3c/P3-fixtures-cp = 4 slices), Predictions (P4a–P4f + P4-cp = 7 slices, includes the export pair P4e/P4f), Oracle Chat (P5a = 1), Insights (P7a/P7b/P7c/P7-cp = 4), Settings (P8a = 1). The **Fixtures sub-phase** is plan-ready: `docs/superpowers/plans/2026-04-26-phase-3-per-hub/index.md` + detailed per-slice files for P3a/P3b/P3c/P3-fixtures-cp. Plans for the remaining sub-phases (Predictions hub, Oracle, Insights, Settings) get ralph-drafted as the loop reaches each — same self-grooming pattern that worked through Phase 2's grooming iterations.

**Tagging policy (refined 2026-04-26 after Phase 2 sign-off):** every ralph slice gets a sequential `v3.x` tag, but the tag is applied at the **sign-off moment** rather than at the slice commit. Rationale: a committed-but-unsigned-off slice can still regress under the manual eyeball; binding the tag to sign-off makes each `v3.x` a "this is good" stamp instead of just "this was committed." For auto-gated slices (`v3.x` examples: P3-fixtures-cp's auto half, P4e text exports), commit-time = sign-off so the tag is created in the same iteration. For manual-gated slices, ralph leaves the tag uncreated; the human applies it (`git tag v3.N <commit>`) at the moment they flip `[x]`. Sub-slice commits stay **untagged** so the `v3.x` sequence cleanly maps onto top-level checklist items. Tags are local-only — push policy unchanged. Phase 2 mapping: P2a → `v3.1` (committed), P2b → `v3.2` (signed off), P2c → `v3.3` (signed off). Phase 3 expectation: P3a → `v3.4`, P3b → `v3.5`, P3c → `v3.6`, P3-fixtures-cp → `v3.7`, then P4a → `v3.8`, etc.

## Ordered checklist

### Phase 0 — Foundation
- [x] **P0a — Tokens** *(Auto-gated)* — Replace `app.css` token block with broadcast palette in HSL components, add new vars (`--bg-raised`, `--text-dim`, etc.), update Tailwind config (fonts, radius, fontFamily), kill Outfit, install Inter / JetBrains Mono / Instrument Serif via fonts CSS. Type-scale utility classes added under `@layer base`. Existing team-theming preserved. Plan: `docs/superpowers/plans/2026-04-26-phase-0-foundation/p0a-tokens.md`.
- [x] **P0b — Routing** *(Auto-gated)* — Install `svelte-routing`, refactor `App.svelte` from `currentView` to `<Router>` + `<Route>`, create `routes.ts` (route table + redirect map + sub-tab declarations), add Vercel SPA fallback rewrite, delete orphaned `SuggestedBets.svelte` / `AccumulatorBuilder.svelte` / `BettingHistory.svelte` (and their tests). Plan: `docs/superpowers/plans/2026-04-26-phase-0-foundation/p0b-routing.md`.
- [x] **P0c — Atoms** *(Auto-gated)* — Create `components/atoms/`: `Crest`, `FormDot`, `ProbBar` (with sum-to-1 invariant), `Spark`, `Icon` + icon registry, `KpiTile`, `SectionHeader`. Each ships with `.test.ts`. Plan: `docs/superpowers/plans/2026-04-26-phase-0-foundation/p0c-atoms.md`.
- [x] **P0d — Shells** *(Manual-gated, signed off via code review)* — Create `components/layout/`: `BroadcastShell`, `Tabs`, `MobileTabBar`, `MobileBottomSheet`. Add `density` and `supportingClub` stores. Mount the new shell as the outer chrome of `App.svelte`; legacy screens still render inside. Plan: `docs/superpowers/plans/2026-04-26-phase-0-foundation/p0d-shells.md`.
- [x] **P0-cp — Phase 0 checkpoint** *(Auto-gated for Playwright spec write+pass+commit; manual visual sweep RECOMMENDED but not blocking)* — Write `frontend/e2e/checkpoint-p0.spec.ts`, run it on desktop-chrome and mobile-chrome projects, commit. Spec asserts hubs render at desktop, mobile bottom bar appears <1024px, theme toggle works. Plan: `docs/superpowers/plans/2026-04-26-phase-0-foundation/p0-cp-checkpoint.md`.

### Phase 1 — MatchCard primitive
- [x] **P1a — MatchCard header strip** *(Auto-gated)* — Create `components/matchcard/MatchCard.svelte` rendering only the header (12-col grid, gradient bleed, meta row, body); swap legacy `MatchList.svelte` to use it. Plan: `docs/superpowers/plans/2026-04-26-phase-1-matchcard/p1a-header.md`.
- [x] **P1b — MatchCard expanding sections** *(Auto-gated)* — Add Analyse / Probabilities & Models / Form & H2H / Venue Referee Tempo. Multi-open. Reduce-motion-safe chevron rotation. Plan: `docs/superpowers/plans/2026-04-26-phase-1-matchcard/p1b-sections.md`.
- [x] **P1c — MatchRow + emphasised + Phase 1 checkpoint** *(Manual-gated, signed off 2026-04-26)* — Compact `MatchRow.svelte` for log/history surfaces; `variant="emphasised"` on MatchCard adds primary-ringed shadow + bumped winning numeric. Phase 1 visual sweep. Plan: `docs/superpowers/plans/2026-04-26-phase-1-matchcard/p1c-matchrow.md`.

### Phase 2 — Today
- [x] **P2a — Command strip + hero match** *(Manual-gated, signed off 2026-04-26)* — Sticky 56px strip (GW, countdown, accuracy chip, API status dot, `[Predict GWxx]` button); hero `<MatchCard variant="emphasised" defaultOpen="analyse" />` for the most-imminent fixture. Creates `screens/Today.svelte`, `lib/gameweek.ts`, `lib/adapters/v3.ts`, `components/today/CommandStrip.svelte`. Swaps `App.svelte`'s `/today` route from legacy `Dashboard` to new `Today`. Plan: `docs/superpowers/plans/2026-04-26-phase-2-today/p2a-command-strip-hero.md`.
- [x] **P2b — KPI strip + predictions grid** *(Manual-gated, signed off 2026-04-26, tagged `v3.2`)* — 4 `KpiTile`s (`Picks · Accuracy · Brier · Avg Confidence`); 2-col grid (1-col mobile) of standard `MatchCard`s for remaining gameweek fixtures. Extends `predictionTracker.getAccuracyStats()` with a `brierScore` field (reusable by P4b Backtest). Plan: `docs/superpowers/plans/2026-04-26-phase-2-today/p2b-kpi-strip-grid.md`.
- [x] **P2c — Below-fold + Phase 2 checkpoint** *(Manual-gated, signed off 2026-04-26, tagged `v3.3`)* — `<SectionHeader>` + `<Spark>` of accuracy-by-gameweek; "How we predict →" link to `/settings/help`; 5 `<MatchRow>` rows from `predictionTracker.getRecentPredictions(5)`. Phase 2 Playwright checkpoint at `e2e/checkpoint-p2.spec.ts`. Plan: `docs/superpowers/plans/2026-04-26-phase-2-today/p2c-below-fold.md`.

### Phase 3 — Per-hub
- [ ] **P3a — Fixtures Live** *(Manual-gated)* — `screens/fixtures/Live.svelte` mounting at `/fixtures/live`; in-play `<MatchCard>`s with `<LiveBanner>` rows above each. 30s polling that pauses on `document.hidden`. Plan: `docs/superpowers/plans/2026-04-26-phase-3-per-hub/p3a-fixtures-live.md`. Tag at sign-off → `v3.4`.
- [ ] **P3b — Fixtures Matches** *(Manual-gated)* — `screens/fixtures/Matches.svelte` mounting at `/fixtures/matches`; replaces legacy `MatchList`. Date-grouped `<SectionHeader>` + `<MatchCard>`s, with a `<FilterChips>` row (All · Top 6 · Relegation · TV picks). Plan: `docs/superpowers/plans/2026-04-26-phase-3-per-hub/p3b-fixtures-matches.md`. Tag at sign-off → `v3.5`.
- [ ] **P3c — Fixtures Standings** *(Manual-gated, supplemented by auto column-count + qualification-zone class assertions)* — `screens/fixtures/Standings.svelte` mounting at `/fixtures/standings`; replaces legacy `StandingsTable`. 20-row 13-col table with FormDot + PPG Spark + qualification-zone tints; click row → `/match/[next-id]`. Plan: `docs/superpowers/plans/2026-04-26-phase-3-per-hub/p3c-fixtures-standings.md`. Tag at sign-off → `v3.6`.
- [ ] **P3-fixtures-cp — Fixtures checkpoint** *(Manual-gated for visual sweep, auto-gated for Playwright spec)* — `frontend/e2e/checkpoint-p3-fixtures.spec.ts` covering all three sub-tabs, Tabs switching, theme toggle. Plan: `docs/superpowers/plans/2026-04-26-phase-3-per-hub/p3-fixtures-cp-checkpoint.md`. Tag at sign-off → `v3.7`.
- [ ] **P4a — Predictions This Week** *(Manual-gated)*
- [ ] **P4b — Predictions Backtest** *(Manual-gated)*
- [ ] **P4c — Predictions Log** *(Manual-gated)*
- [ ] **P4d — Predictions Tools (Kelly + Value, relocated to components/predictions/)** *(Manual-gated)*
- [ ] **P4e — Export text (CSV + Markdown)** *(Auto-gated)*
- [ ] **P4f — Export binary (PDF + PNG card via html2canvas + jsPDF)** *(Manual-gated)*
- [ ] **P4-cp — Predictions checkpoint** *(Manual-gated)*
- [ ] **P5a — Oracle Chat** *(Manual-gated)*
- [ ] **P7a — Insights Top Scorers** *(Manual-gated)*
- [ ] **P7b — Insights Season Stats** *(Manual-gated)*
- [ ] **P7c — Insights Timeline (uses Instrument Serif italic)** *(Manual-gated)*
- [ ] **P7-cp — Insights checkpoint** *(Manual-gated)*
- [ ] **P8a — Settings (all sub-pages: Account, API & data, Display, Predictions, Notifications, Privacy, Help)** *(Manual-gated)*

### Phase 4 — Mobile polish
- [ ] **P9a — Match deep-dive route /match/[id]** *(Manual-gated)*
- [ ] **P9b — Mobile checkpoint** *(Manual-gated)*

### Phase 5 — Cleanup
- [ ] **P10 — Cleanup** *(Manual-gated)* — Delete stranded legacy components and tests, drop `noise-bg` / `dot-pattern` / extra animations from `app.css`, remove `font-display` Outfit alias, update `README.md` and `CLAUDE.md`. May split into P10a/b/c if the diff is too large for one ralph iteration.

**Numbering note:** the gap between P5a (Oracle) and P7a (Insights) is intentional — P6 was the Betting hub in an earlier draft and was removed during brainstorming (Decision 7). Slice numbers were not renumbered, to preserve diff-readability across spec revisions. There is no missing slice.

## Notes / discoveries that matter for the next loop

*(Filled by ralph as it discovers blockers.)*

- **(P0a, no blocker)** P0a landed clean: 6/6 token gate tests pass, full vitest 705/705 across 43 files (project memory's "561 / 34" count is stale — not blocking, just FYI for future ralph runs that compare suite size). `svelte-check` 0/0. `--radius` shrank from 0.75rem → 0.5rem as designed; no legacy test was hard-coded against a radius value, so the change rippled silently through `rounded-lg/md/sm` utilities.
- **(P0a → P0b handoff)** `app.css` now begins with `@import url('./lib/styles/typography.css'); @import url('./lib/styles/tokens.css');` *above* the `@tailwind` directives — required because CSS spec puts `@import` first, and PostCSS-import in the Vite pipeline needs that ordering. Future slices that touch `app.css` should preserve that ordering.
- **(P0b, no blocker)** P0b landed: vitest 671/671 across 41 files (down from 705 because we deleted `AccumulatorBuilder.test.ts` + `BettingHistory.test.ts`, ~34 tests). `svelte-check` 0/0. Playwright `routing.spec.ts` 32/32 in 20.6s. Three legacy tests that asserted on `dispatch('navigate', { view })` were rewritten to mock `svelte-routing`'s `navigate()` and assert on path strings (per the plan's prescribed pattern).
- **(P0b deviations from plan)**
  - `vercel.json` lives at `frontend/vercel.json`, not the repo root. Updated that file's existing `/(.*)` SPA catch-all to the plan's stricter regex `/((?!api/|_next/|favicon\\.ico|.*\\..*).*)/` so static assets and API rewrites still take precedence.
  - Dropped the `import ValueBets` line from the plan's `App.svelte` template — the plan kept it but never mounted it, which would have tripped svelte-check. `ValueBets` will be re-imported in P4d when `/predictions/tools?utility=value` is wired.
  - Removed the dead `<style>` block (`.page-content { transition }`) from `App.svelte` after svelte-check flagged the selector as unused — the wrapper div it styled was deleted in this slice's rewrite.
  - Patched `Sidebar.svelte`'s forwarder (the `handleNavClick` that re-dispatches `SidebarNav`'s `navigate` event) rather than touching `SidebarNav.svelte` itself — single interception point, less surface.
  - Defaulted `currentView: string = ''` in both `Sidebar.svelte` and `MobileNav.svelte`. The new `App.svelte` no longer passes the prop, and a default avoids svelte-check warning. The active-route highlight will look stale on legacy nav until P0d swaps the shell.
- **(P0b → P0c handoff)** `frontend/src/routes.ts` is the single source of truth for the v3 route table. P0c's atoms can already import `RouteDef` / `SubTabDef` types if needed. Note: `findRoute(path)` only matches top-level hub paths (e.g. `/fixtures`), not full sub-tab paths — extend if a future slice needs `/fixtures/live` resolution.
- **(P0c, no blocker)** P0c landed: vitest 704/704 across 48 files (+33 atom tests across 7 new files). `svelte-check` 0/0. All 7 atoms (`Crest`, `FormDot`, `ProbBar`, `Spark`, `Icon`+registry, `KpiTile`, `SectionHeader`) shipped with co-located `.test.ts`. `types/redesign.ts` created. `app.css` got an `@layer utilities` block for redesign tokens (`bg-bg-inset`, `bg-text-faint`, `bg-text-dim`, `bg-surface-hover`, `text-text-dim/faint/muted/ghost`, `border-border-strong`).
- **(P0c deviations from plan)**
  - `Icon.svelte`: moved `stroke-width={strokeWidth}` from the `<svg>` element to the inner `<path>`. Plan template put it on the svg, but the contract test asserts `path.getAttribute('stroke-width')`. Test wins; functionally equivalent because stroke attrs inherit, but the path now owns the explicit attribute.
  - `FormDot.test.ts` + `ProbBar.test.ts`: split each rerender-based test into two separate `render()` calls. `@testing-library/svelte` v5.2.x's `rerender()` did not update Svelte 4 prop reactivity in this setup (size remained `sm`, `showLabels` remained `false` after rerender). Two-render structure is also more idiomatic and removes a v5/Svelte4 compatibility footgun for future atoms.
  - `ProbBar.svelte`: dropped `role="progressbar"` and the `aria-valuemin/max/now` triplet. svelte-check warned `aria-invalid is not supported by role progressbar`, and semantically a stacked three-segment probability bar isn't a single-task progress widget. The `aria-invalid` attribute (which the test asserts on) is preserved.
  - `app.css` additions are scoped under a new `@layer utilities` block placed before the `ATMOSPHERE & TEXTURE` block — preserves the file's section ordering so future slices can locate where to add new utility classes.
- **(P0d, no blocker)** P0d landed: vitest 711/711 across 50 files (+7 new tests across 2 files: `BroadcastShell.test.ts` 3, `Tabs.test.ts` 4). `svelte-check` 0/0. New `frontend/src/components/layout/` directory with `BroadcastShell.svelte`, `Tabs.svelte`, `MobileTabBar.svelte`, `MobileBottomSheet.svelte`. New stores `frontend/src/stores/density.ts` and `frontend/src/stores/supportingClub.ts`. New test fixture `frontend/src/tests/LinkStub.svelte` (re-usable by future layout/component tests). `App.svelte` now mounts `BroadcastShell` as the outer chrome — legacy `Header.svelte`, `Sidebar.svelte`, `MobileNav.svelte` are unimported and scheduled for P10 cleanup.
- **(P0d deviations from plan)**
  - `Tabs.svelte`: changed the wrapping element from `<nav role="tablist">` (plan template) to `<div role="tablist">`. svelte-check correctly warns that `<nav>` is a landmark and shouldn't have an interactive role. Tests still query by `role="tablist"`, so they pass unchanged.
  - `MobileBottomSheet.svelte`: replaced `on:click|stopPropagation` (plan template) with a `target === currentTarget` check on the scrim's click handler — same click-outside-to-close behavior, but Svelte 4 strict TS mode rejects `|stopPropagation` without an explicit handler. Cleaner and removes the need for stopPropagation modifiers anywhere in the component.
  - `MobileBottomSheet.svelte`: handler params typed `e: any` with the same `// Svelte 4 types on:keydown as CustomEvent...` comment used in `MobileNav.svelte`, `dialog-content.svelte`, `sheet-content.svelte`. Following the existing codebase convention rather than introducing a one-off `KeyboardEvent` type that would conflict with the same Svelte 4 typing quirk.
  - `BroadcastShell.test.ts`: scoped the "renders sidebar labels" assertion to within the `<nav>` element (using `within(nav)`). The plan template's bare `getByText('Oracle')` would match both the brand text and the hub label and throw a multiple-match error.
  - Both layout-component tests use `vi.mock('svelte-routing', async () => { const LinkStub = (await import('../../tests/LinkStub.svelte')).default; return { Link: LinkStub, navigate: vi.fn() }; })` instead of wrapping in a `<Router>`. The new `frontend/src/tests/LinkStub.svelte` fixture renders `Link` as a plain anchor with `to → href`.
  - `MobileTabBar.svelte`: imported `IconName` from `'../atoms/icons'` (where the type actually lives), not from `'../atoms/Icon.svelte'` (where the plan template put the import — that path doesn't re-export `IconName`).
  - `supportingClub.ts`: read localStorage once into a local `initial` constant instead of calling `read()` twice (once for the writable seed, once for `applyDom`). Functionally identical, avoids a redundant DOM call.
  - `MobileTabBar.svelte` and `BroadcastShell.svelte`'s `<nav>` got `aria-label` strings ("Primary mobile navigation" / "Primary navigation") — landmark elements should be labelled when more than one nav exists on a page.
- **(P0-cp, no blocker)** P0-cp landed: 8/8 Playwright tests pass on `desktop-chrome` (15.1s) and 8/8 on `mobile-chrome` (15.6s). `svelte-check` 0/0. New `frontend/e2e/checkpoint-p0.spec.ts` covers the cumulative Phase 0 surface — six hub URLs render an app shell + sidebar at desktop, mobile bottom bar appears below 1024px with sidebar hidden, theme toggle flips the `dark` class on `<html>`. Vite dev server logs `[vite] http proxy error: /health` during the run because the FastAPI backend isn't running locally — pre-existing noise, not introduced by this slice.
- **(P0-cp deviations from plan)**
  - Spec calls `setupApp(page)` from `frontend/e2e/helpers.ts` before each `page.goto(path)` — the plan template went straight to `page.goto()`. Without `setupApp()` the `ApiSetupWizard` modal pops up (no API key in localStorage) and would intercept the "Toggle theme" click. `setupApp()` also registers `mockFootballApi()` so hubs render against deterministic mock data instead of failing real Football-Data calls.
  - Theme-toggle test pins viewport to 1280×800 (the plan template left it at the project default). The toggle button is intentionally desktop-only in P0d (`<aside>` has `hidden lg:flex`, and `MobileBottomSheet` does not surface a theme toggle), so on the `mobile-chrome` project (Pixel 5 ~393px) the click would time out. Override is per-test rather than spec-wide so the mobile-bottom-bar test still exercises the mobile viewport.
  - The manual visual sweep (Task 2 in the slice plan) is intentionally deferred per the gate-relaxation note in `fc7bd69` — the auto-gate requires only spec write+pass+commit. A human can run the sweep separately whenever convenient; observations should land under `## Human notes for next iteration`.
- **(P1a, no blocker)** P1a landed: vitest 718/718 across 51 files (+7 from `MatchCard.test.ts`; `MatchList.test.ts` net 0 — two legacy assertions swapped for two `[data-block]`/`[data-meta]` contract assertions). `svelte-check` 0/0. Playwright `routing.spec.ts` 32/32 on desktop-chrome. New `frontend/src/components/matchcard/` directory (component + co-located test) and `frontend/src/tests/fixtures/matchcard.ts` (reusable Fixture/Prediction builders for P1b/P1c).
- **(P1a deviations from plan)**
  - `MatchCard.svelte`: the no-prediction center block uses `isFinished = fixture.status === 'FINISHED' || kickoff < new Date()` rather than the plan template's `kickoff < new Date()` alone. `Match.result` maps cleanly to `status: 'FINISHED'` in the adapter, so honouring status first preserves "FT" semantics if a device clock drifts ahead.
  - `MatchList.svelte` adapter: implemented `mapToFixture(Match)` only — no `mapToPrediction`, because the legacy data-loading path (`dataService.getMatchesBySeason`) doesn't yield prediction shape. MatchCard receives `prediction={undefined}` and renders kickoff time / FT label, which is the slice's intended degraded mode. Predictions hookup belongs to P4a/P4c.
  - `MatchList.svelte`: deleted the `import { format } from 'date-fns'`, `import { getTeamLogo }`, and `import { Badge }` lines — all three only fed the per-row markup that's now gone. `Button` is kept because it's still used by the error/empty-state retry CTAs.
  - `MatchList.test.ts`: removed the `vi.mock('date-fns', …)` block (no longer reachable) and replaced the "displays scores for completed matches" / "displays kick-off time for upcoming matches" tests with two `data-block`/`data-meta` count assertions. The slice plan explicitly authorises this swap ("update the test to assert on MatchCard's data-attributes — these are stable contracts"). Net test count for the file unchanged at 12.
  - `mapToFixture` synthesises team `abbr` via `name.replace(/\b(?:FC|AFC)\b/g, '').trim().slice(0, 3).toUpperCase()` — lossy but adequate for the slice's goal (proving integration). A proper crest-URL pipeline lands when the v3 `Fixture` shape gets sourced directly from the dataService in a later phase; for now the legacy `Match` carries no abbreviation field.
- **(P1b, no blocker)** P1b landed: vitest 726/726 across 51 files (+8 from `MatchCard.test.ts`: 6 section-behaviour + 2 section-content). `svelte-check` 0/0. New `frontend/src/components/matchcard/MatchCardSection.svelte` (toggleable shell with `aria-expanded` + `motion-safe:` chevron rotation). `MatchCard.svelte` gained `defaultOpen` (single ID or array) and `hideSections` props plus an `openState` map; multi-open semantics fall out of `openState = { ...openState, [id]: next }` (no accordion exclusivity). The `analyse` and `probabilities` sections only render when `prediction` is defined — both gated on `{#if isShown(...) && prediction}`; `form` and `context` always render. The four `—` placeholders in the *context* section are intentional per the plan: no data source for venue / referee / tempo until a later phase.
- **(P1b deviations from plan)** None — implementation tracked the plan template verbatim. Reduce-motion is delivered via Tailwind's `motion-safe:transition-transform motion-safe:duration-150` (transition skipped under `prefers-reduced-motion: reduce`); the `rotate-180` itself stays unconditional so the chevron's open/closed visual state is preserved even with motion off.
- **(addressed) P1c signed off 2026-04-26** — visual sweep at `/fixtures/matches` cleared (MatchCard header strip, expanding sections, emphasised variant, theme flip, mobile stack all OK). `[x]` flipped. P1c shipped `MatchRow.svelte` + emphasised variant winning-numeric bump on `MatchCard.svelte`. vitest 732/732 across 52 files (+4 from new `MatchRow.test.ts`, +2 from new `MatchCard — emphasised variant` describe block in `MatchCard.test.ts`). `svelte-check` 0/0. Playwright routing smoke 32/32 on `desktop-chrome` (13.8s). New `frontend/src/components/matchcard/MatchRow.svelte` is a single-row 12-col grid (date · home crest+abbr · score-or-v · away abbr+crest · ProbBar · pick-or-hit-indicator) for log/history surfaces. `data-hit="true"|"false"` contract on the right-hand cell is the stable signal for predictions-log filtering. Emphasised variant on `MatchCard` now bumps the winning side's percentage from `text-metric-lg` (28px) to `text-metric-xl` (44px) — losing sides stay at `text-metric-sm` so the hierarchy is one big number, not three. `shadow-emphasised` ring shadow remained from P1a unchanged.
- **(P1c deviations from plan)** Preserved P1a's `isFinished = fixture.status === 'FINISHED' || kickoff < new Date()` semantic in MatchCard's center-block fallback rather than reverting to the plan template's `kickoff < new Date()` only. The deviation is documented in P1a's notes (line "honouring status first preserves 'FT' semantics if a device clock drifts ahead") and removing it would regress that improvement. MatchRow's own `isFinished` *does* additionally require `fixture.score !== undefined` — that's intentional, because a row's right-hand cell needs a score to compute hit/miss; an early-FT-by-clock fixture without a posted score should fall back to the pick+confidence display, not throw on `actualOutcome`. The `[vite] http proxy error: /health` log line during the Playwright run is the same pre-existing FastAPI-not-running noise documented in P0-cp's notes — not introduced by this slice.
- **(Phase 2 plan, prep slice — no code, no validation gate)** Wrote `docs/superpowers/plans/2026-04-26-phase-2-today/` (`index.md`, `p2a-command-strip-hero.md`, `p2b-kpi-strip-grid.md`, `p2c-below-fold.md`). Plans prescribe: P2a creates `screens/Today.svelte` + `components/today/CommandStrip.svelte` + `lib/gameweek.ts` + `lib/adapters/v3.ts`, swaps `App.svelte`'s `/today` route from `Dashboard` to `Today`; P2b appends KPI strip + 2-col grid and extends `predictionTracker.getAccuracyStats()` with a `brierScore` field (reusable by P4b); P2c appends `Spark` of accuracy-by-gameweek + 5-row recent-log via `MatchRow`, and ships `e2e/checkpoint-p2.spec.ts`. Phase 2 plan deliberately written *before* P1c human sign-off because it's documentation only and doesn't affect P1c's visual review surface. P1c still requires human eyeball before P2a code can start. Discovered while drafting: `AccuracyStats` does not currently expose Brier — P2b's task list now treats that as a centralised contract addition rather than a Today-local computation.
- **(addressed) Tag-each-commit human note (2026-04-26)** Human asked: *"Please add tags to each commit. We will start from 3.1."* Addressed by tagging the P2a commit `v3.1` (matching the repo's existing `v`-prefixed lightweight-tag convention from `v0.0.X` history) and recording the policy under `## Active phase` so subsequent slices auto-pick the next version (P2b → `v3.2`, P2c → `v3.3`, etc.). Tags are created local-only — push policy unchanged per repo safety rules.
- **(addressed) P2a signed off 2026-04-26** — visual sweep at `/today` cleared (sticky command strip, ticking countdown, accuracy chip, API status dot, predict CTA, emphasised hero with AI ANALYSIS open, theme flip all OK). `[x]` flipped. P2a shipped `screens/Today.svelte` (skeleton — strip + hero only), `components/today/CommandStrip.svelte`, `lib/gameweek.ts` and `lib/adapters/v3.ts`. `App.svelte`'s `/today` route now mounts `Today`; legacy `Dashboard.svelte` stays in the tree until P10 cleanup. vitest 759/759 across 56 files (+27 across 4 new files: gameweek 6, v3 adapter 9, CommandStrip 8, Today 4), svelte-check 0/0, Playwright routing 32/32 on `desktop-chrome` (14.1s). Tagged `v3.1`. Manual sweep checklist below.
- **(P2a deviations from plan)**
  - **Adapter test fixtures + impl rewritten for the real `Match` shape.** The plan template assumed Football-Data API shape (`m.utcDate`, `m.homeTeam.tla`, `m.score.fullTime.home`). The actual internal `Match` (`frontend/src/types/index.ts`) uses `m.date`, `m.home_team` / `m.away_team` (strings), `m.home_goals` / `m.away_goals` (number|null), `m.result` ('H'|'A'|'D'|null). Adapter now derives `abbr` via `name.replace(/\b(?:FC|AFC)\b/g, '').trim().slice(0,3).toUpperCase()` (matching the existing `MatchList.svelte` `mapToFixture` heuristic) and falls back from missing `m.status` to FINISHED-when-result-present / SCHEDULED-when-not. Plan explicitly authorised this: *"the tests are the contract; the implementation conforms to them"*.
  - **`Match.status` alphabet mismatch.** v3 `FixtureStatus` uses `LIVE`, but the legacy `MatchStatus` exposes `IN_PLAY` / `EXTRA_TIME` / `PENALTY_SHOOTOUT`. Adapter coerces the live trio into `LIVE` and passes `SCHEDULED|FINISHED|POSTPONED|CANCELLED|PAUSED` through unchanged. Test covers the live coercion explicitly.
  - **`Today.svelte` exposes `export async function load()` and `onMount(load)`.** This codebase's testing pattern (visible in `Predictions.test.ts`, `SeasonStats.test.ts`) doesn't rely on async `onMount` flushing before assertions in jsdom + @testing-library/svelte v5 + Svelte 4. Tests call `await component.load(); await act();` for deterministic data-loaded state. The pattern matches `Predictions.svelte`'s `loadGameweekMatches` and `SeasonStats.svelte`'s `loadSeasonStats`, so Today fits the house style instead of inventing a one-off.
  - **Test mocks inline fixture data inside `vi.mock` factory rather than referencing top-level constants** (vitest hoists `vi.mock` above all top-level statements — referencing top-level consts triggers `ReferenceError: Cannot access ... before initialization`).
  - **Bonus tests beyond the plan template.** CommandStrip got 8 tests (plan called for 5) covering the multi-day countdown format (`Dd Hh`), the kicked-off state, and the predict-CTA hidden-when-gameweek-null branch. Adapter got 9 tests (plan: 5) covering the live-status coercion and finished-no-result fallback. Today got 4 tests (plan: 3) — split "renders strip" from "renders gameweek after load" so the strip-renders-immediately invariant is asserted independent of the data-load lifecycle.
  - **`MatchList.svelte` still inlines its own `mapToFixture`.** Consolidating that into `lib/adapters/v3.ts` would be a low-risk DRY follow-up (~3 lines + delete the local copy + import the new one), but is intentionally out of scope for the slice that introduced `v3.ts` — strangler-fig discipline. Recorded as a follow-up below.
- **(P2a follow-ups)**
  - **(addressed)** DRY: refactor `MatchList.svelte` to import `matchToFixture` from `lib/adapters/v3.ts` instead of holding its own copy. Landed as `P2a-cleanup` commit (separate from the originating P2a slice so the diff history reads "introduce, then consolidate"). Untagged — `v3.x` numbering is reserved for the phase-checklist slices, so the next tag (`v3.2`) still belongs to P2b.
  - **(propagated to P2b plan as Task 1b — 2026-04-26 iteration)** The `predictionToV3` adapter populates `models: []`, `xg: {0,0}`, `elo: {0,0}` because `StoredPrediction` doesn't carry per-model breakdowns. The MatchCard's `PROBABILITIES & MODELS` section will render an empty 5-col grid until either the adapter is widened (P4-era work, when the prediction pipeline starts persisting per-model leans) or the section is gated on `prediction.models.length > 0`. Now captured as `p2b-kpi-strip-grid.md` Task 1b — to be executed after P1c sign-off (since it touches the MatchCard primitive in P1c's review surface) and before P2b Task 2 (so the new grid cards don't ship the empty-state defect).

- **(P2a-cleanup, no blocker)** P2a-cleanup landed: `MatchList.svelte` now imports `matchToFixture` from `lib/adapters/v3.ts` instead of inlining its own `mapToFixture` (a strict subset of the canonical adapter — same id/competition/gameweek/utcDate/abbr/score, but the inlined version ignored `m.status` entirely). vitest 759/759 across 56 files (test count unchanged — pure refactor; the 12 `MatchList.test.ts` tests assert on `[data-block]` / `[data-meta]` / team-name DOM contracts, not adapter internals, so they pass unchanged). `svelte-check` 0/0. Targeted: `MatchList.test.ts` 12/12 + `v3.test.ts` 9/9. Quiet bug fix: previously `MatchList` would have rendered `IN_PLAY` / `EXTRA_TIME` / `PENALTY_SHOOTOUT` matches as `'SCHEDULED'` (because `m.result` is null mid-match); they now correctly resolve to `'LIVE'` via `deriveStatus`. The stale "still inlines its own copy" disclaimer in `v3.ts`'s docstring was also removed.

- **(P2b — Task 1 only, auto-gated sub-slice, no blocker)** Extended `AccuracyStats` with `brierScore: number` and added `computeBrier()` to `PredictionTracker`. Mean Brier across settled predictions that carry `poissonProbs`; predictions without `poissonProbs` are skipped (a missing probability vector isn't "all zeros", which would unfairly score 1.0 against any outcome). vitest 763/763 across 56 files (+4: 3 in a new `Brier Score` describe + 1 added assertion to the empty-history accuracy test). `svelte-check` 0/0. Targeted: `predictionTracker.test.ts` 30/30 (was 26/26). Sub-slice deliberately stops at the data-layer change — Tasks 2-5 of P2b (extending `Today.test.ts`, appending KPI strip + grid markup, manual sweep) are still pending and gated on the P1c + P2a human eyeballs that block the visual surface of `Today.svelte`. P2b's checklist box stays `[ ]`. Tagging: this commit is **untagged** — `v3.2` is reserved for the full P2b slice when the visual zones land.
- **(P2b sub-slice deviations from plan)**
  - Plan template wrote tests in a new `predictionTracker.brier.test.ts` file using a "poke private state directly via `as unknown as`" pattern. Used the existing `predictionTracker.test.ts` instead because it's already the co-located test file, and used the public `storePrediction` + `updateWithResult` API rather than mutating the private `predictions` map. Black-box style means the tests still pass if storage internals change — and matches the rest of the file's conventions. The plan explicitly authorised this: *"Create or modify: `predictionTracker.test.ts` (or `predictionTracker.brier.test.ts` if no co-located test exists yet)"*.
  - Added a fourth Brier test the plan didn't prescribe: "ignore unsettled predictions even when they carry poissonProbs". Guards a coupling that's currently double-filtered (once in `getAccuracyStats`'s `relevantPredictions`, once in `computeBrier`'s own `actualResult && poissonProbs` filter) — if a future refactor calls `computeBrier` from a different code path that doesn't pre-filter, the test catches the regression.
  - Extracted `computeBrier(predictions)` as a `private` method on `PredictionTracker` rather than inlining inside `getAccuracyStats`. Keeps the parent function readable and gives P4b (Backtest) a unit-of-work to call from a new public method (e.g., `getBrierByGameweek()`) without re-implementing the formula.
  - Added `expect(stats.brierScore).toBe(0)` to the existing "should handle empty prediction history" test rather than creating a separate empty-state test for Brier — same scenario, one assertion, no duplication.

- **(P2c — Task 1 only, auto-gated sub-slice, no blocker)** Added `storedPredictionToFixture(p: StoredPrediction): Fixture` to `frontend/src/lib/adapters/v3.ts`. Pure data-layer: synthesises a v3 `Fixture` from a `StoredPrediction` so the future P2c recent-log strip can render `<MatchRow fixture={…} prediction={…} />` from `predictionTracker.getRecentPredictions(5)` without a corresponding `Match` record. Status derives from `actualResult !== undefined && typeof actualHomeGoals === 'number' && typeof actualAwayGoals === 'number'` (FINISHED ⇒ score populated; SCHEDULED ⇒ score omitted). vitest 768/768 across 56 files (+5 in a new `storedPredictionToFixture` describe). `svelte-check` 0/0. Targeted: `v3.test.ts` 14/14 (was 9/9). Mirrors the same pattern as the Brier sub-slice — non-visual data-layer prep that doesn't overlap the pending P1c/P2a manual review surface, so it can ship ahead. Sub-slice deliberately stops at the adapter; Tasks 2-3 of P2c (Today.svelte trends + log markup, Phase 2 Playwright checkpoint) remain pending and gated on the visual sweeps. P2c's checklist box stays `[ ]`. Tagging: **untagged** — `v3.x` numbering stays reserved for the phase-checklist slices (next tag `v3.2` belongs to the full P2b slice).
- **(P2c sub-slice deviations from plan)**
  - Plan template inlined `home: { abbr: p.homeTeam.slice(0, 3).toUpperCase(), name: p.homeTeam }`. Used the existing `teamFrom(p.homeTeam)` helper from the same file instead — it routes through the shared `toAbbr()` which strips `\b(?:FC|AFC)\b` before slicing. One source of truth for abbreviations across `matchToFixture` and `storedPredictionToFixture`, and correctly handles "Liverpool FC" / "AFC Bournemouth" team-name shapes that the bare slice would mishandle on edge inputs (e.g. `'AFC Bournemouth'.slice(0,3)` = `'AFC'` would collide with the league abbreviation).
  - Added two extra tests beyond the plan template:
    - **0-0 settled score** — guards against a future refactor that swaps `typeof p.actualHomeGoals === 'number'` for the shorter-but-broken `p.actualHomeGoals && p.actualAwayGoals` (which silently fails on the falsy `0`).
    - **FC/AFC suffix stripping** — pins the chosen `teamFrom` deviation above so a future "simplify the adapter" PR can't quietly regress to bare slice behaviour.
  - Used the existing `mkStored()` factory pattern (mirrors `mkMatch()` already in the file) rather than inlining a full `StoredPrediction` literal in each test. Cuts ~20 lines of test fixtures and matches the file's house style.

- **(P2b plan grooming, no code, no validation gate)** Propagated the unaddressed P2a follow-up about empty PROBABILITIES & MODELS sub-blocks into `p2b-kpi-strip-grid.md` as a new **Task 1b** — "Gate empty PROBABILITIES & MODELS sub-blocks in MatchCard primitive". Sequences MatchCard primitive gating after P1c sign-off and before P2b Task 2's grid markup, so the new grid cards don't ship the empty-state defect that the P2a hero is currently exhibiting. TOP-3 SCORELINES stays always-rendered (always populated by `predictionToV3`); 5-col models grid, xG block, and ELO block each gate independently on having actual data; outer 2-col wrapper gates on either xG or ELO being populated to avoid an empty grid container. Plan-only commit (precedent: `bdc551f Phase 2 plan` and `9726119 Split Phase 0 plan`); the next iteration that picks up P2b will execute Task 1b alongside the rest of P2b and tag `v3.2`. P2a follow-up bullet above flipped from open to `(propagated)`.

- **(P2c plan grooming, no code, no validation gate)** Marked `p2c-below-fold.md` Task 1 (`storedPredictionToFixture` adapter) with the `— ✅ DONE` header marker and flipped its three step checkboxes to `[x]`, mirroring the same reflect-shipped-sub-slice cleanup that `p2b-kpi-strip-grid.md` got after its own Task 1 landed. Annotated the Step 3 "Expected: 7 tests PASS" line with the actually-landed `v3.test.ts` 14/14 count (P2a wrote 9 adapter tests rather than 5, +5 new `storedPredictionToFixture` tests rather than the 2 the template called for — see the P2c sub-slice deviation notes above). The IMPLEMENTATION_PLAN.md "Next recommended build slice" section already says "The Task 1 portion of the plan doc can be skipped — it's already in the codebase" — this grooming makes the local plan doc self-honest so the next ralph iteration that opens `p2c-below-fold.md` doesn't have to cross-reference IMPLEMENTATION_PLAN.md to know Task 1 is done. Plan-only commit (precedent: `bdc551f Phase 2 plan`, `9726119 Split Phase 0 plan`, `502982a P2b plan grooming`); untagged — the next slice tag (`v3.2`) is reserved for the full P2b slice when its visual zones land.

- **(P2b + P2c Task 5 commit-list grooming, no code, no validation gate)** Corrected stale `git add` blocks in both slice plans so the next ralph iteration that picks up either slice doesn't fail on already-committed-or-non-existent paths. Concrete fixes: (1) `p2b-kpi-strip-grid.md` Task 5 dropped `frontend/src/services/predictionTracker.ts` and `frontend/src/services/predictionTracker.brier.test.ts` (already shipped in `f0c2225`; the brier file *never existed* — actual tests landed in the existing `predictionTracker.test.ts` per the documented sub-slice deviation) and added Task 1b's `frontend/src/components/matchcard/MatchCard.svelte` + `MatchCard.test.ts`. (2) Updated P2b's Task 5 commit *message* from "P2b: …; brierScore added to AccuracyStats" → "P2b: …; gate empty PROBABILITIES & MODELS sub-blocks" since the brier extension shipped earlier. (3) Updated P2b's Task 5 Step 1 progress-note template to lead with the Task 1b gating story and parenthetically reference the earlier brier sub-slice rather than double-counting brierScore as new work. (4) `p2c-below-fold.md` Task 5 dropped `frontend/src/lib/adapters/v3.ts` + `v3.test.ts` (already shipped in `0059a49`). Both Task 5 blocks now explicitly call out which files already shipped, with commit hashes, so future readers don't have to spelunk git history. Plan-only commit (precedent: `bdc551f`, `9726119`, `502982a`, `86c016f`); untagged — `v3.2` stays reserved for the full P2b slice. Validation: `npm run test -- --run` confirmed 768/768 across 56 files (identical to last green) and no source change crept in alongside the docs edits.

- **(P2c Task 2 atom-API grooming, no code, no validation gate)** Pre-flight-checked the two P0c atoms that P2c Task 2's markup template imports and corrected the template against their actual signatures so the next ralph iteration that picks up P2c doesn't follow a tripwire. Concrete fixes in `p2c-below-fold.md`: (1) `<Spark values={…} labels={…} />` → `<Spark data={gameweekAccuracy.map(g => g.accuracy)} width={320} height={48} trend={…} fill />` — the actual atom (`frontend/src/components/atoms/Spark.svelte`) only takes a single `data: number[]` array (no separate `labels` axis), and exposes `width / height / trend / fill` props the template wasn't using. The plan template's `labels` prop would have been silently ignored by Svelte (no svelte-check error, just dead code). (2) Replaced the conditional "If `Spark.svelte`'s prop names differ ... adjust" + "If `SectionHeader.svelte` does not have a `right` slot ..." prose with a positive **Atom signatures verified during 2026-04-26 plan grooming** subsection that pins both contracts: Spark = `data` only + decorative SVG (`aria-hidden="true"`, no hover affordance — tooltip wrappers are P4-era enhancement work), SectionHeader does expose a `slot="right"`. (3) Manual-sweep checklist line "Spark renders the accuracy-by-gameweek line; hover tooltip works" softened to "(atom is decorative / aria-hidden — no hover tooltip; defer that to a P4-era enhancement if wanted)" so the human reviewing P2c doesn't mark a non-existent tooltip as failing. Plan-only commit (precedent: `bdc551f`, `9726119`, `502982a`, `86c016f`, `27c88f5`); untagged — `v3.3` stays reserved for the full P2c slice when the visual zones land. No source change in this slice — `git status` clean except for the two docs edits.

- **(Active phase pointer grooming, no code, no validation gate)** Updated the `## Current status summary and code review` block so the "Active phase plan" pointer matches reality. The line had been pointing at `docs/superpowers/plans/2026-04-26-phase-1-matchcard/index.md` since Phase 1 first opened — but Phase 2 has been the active phase since the P2a slice landed in `6c03956` (tagged `v3.1`), so the line-7 pointer had drifted out of sync with the line-18 "Active phase: Phase 2" narrative across ~5 grooming iterations. Restructured the block from two bullets (active + Phase 0 archive) to three (active + Phase 1 archive + Phase 0 archive), so each phase directory gets its own bullet with current status: Phase 2 = active plan; Phase 1 = code-complete with P1c (`328e573`) committed and awaiting human eyeball; Phase 0 = complete except for the optional P0-cp manual sweep. The "Phase 1 plan archive" addition gives the new ralph iteration a single line that surfaces P1c's blocker (rather than burying it inside the Phase 2 narrative). This restructure also scales cleanly: when Phase 2 closes and Phase 3 opens, the same pattern adds a "Phase 2 plan archive" bullet without a second restructure. Plan-only commit (precedent: `bdc551f`, `9726119`, `502982a`, `86c016f`, `27c88f5`, `3194a50`); untagged — `v3.2` stays reserved for the full P2b slice when its visual zones land. No source change — `git status` clean except for the IMPLEMENTATION_PLAN.md edits.

- **(P2b Task 1b TDD-discipline grooming, no code, no validation gate)** Pre-flight-checked P2b Task 1b's four prescribed test cases against the actual `MatchCard.svelte` source (`frontend/src/components/matchcard/MatchCard.svelte:155-188`) and discovered a real TDD-discipline gap: three of the four tests would have *trivially* passed pre-fix. The current markup has no `data-models-grid` / `data-xg-block` / `data-elo-block` attributes anywhere — `querySelector` returns `null` for any non-existent attribute, and `expect(null).toBeNull()` passes regardless of whether the gating logic exists. Only the fourth test (`always renders [data-scorelines]`) actually fails pre-fix, so the patch would have arrived "test-first" only on paper, and a future refactor that reverts the gating + removes the markers together would not be caught. Fix: appended three companion "renders … when populated" tests (one per gated sub-block) that fail pre-fix because the markers don't exist anywhere, and pass post-fix because the markers ARE rendered for populated inputs. Bidirectional pairs lock the contract in both directions. Net change: 4 prescribed tests → 7 prescribed tests; the "Expected" line in Task 1b Step 3 now reads "all P1a + P1b + P1c tests still PASS, plus 7 new degradation tests PASS (3 hide-when-empty pairs + 1 always-renders-scorelines). Three of the seven tests (the renders … when populated companions) and the always-renders-scorelines test will fail PRE-fix because their data-* markers don't exist on the current source — that's the TDD-discipline check." Also added a "TDD-discipline note (added 2026-04-26 plan grooming)" paragraph under Step 1 explaining *why* the companion tests exist (so a future iteration that thinks "these companions look redundant" sees the reasoning trail before deleting them) and noting that `makePrediction()`'s defaults (`frontend/src/tests/fixtures/matchcard.ts:30-49`) already populate `models` (5 entries), `xg: { home: 1.85, away: 0.92 }`, `elo: { home: 1820, away: 1780 }`, so the bare `makePrediction()` call in the models-renders test is sufficient — no overrides needed. Also bumped the Next-recommended-build-slice's "four tests" → "seven tests" reference so the next ralph iteration that opens IMPLEMENTATION_PLAN.md sees the consistent count. Plan-only commit (precedent: `bdc551f`, `9726119`, `502982a`, `86c016f`, `27c88f5`, `3194a50`, `c736e91`, `42b9bdd`, `377e03d`); untagged — `v3.2` stays reserved for the full P2b slice when its visual zones land. Validation: `npm run test -- --run` confirmed 768/768 across 56 files (identical to last green) and `git status` clean except for the two docs edits.

- **(P2b Task 2 fixture-shape + brier-default-mock grooming, no code, no validation gate)** Pre-flight-checked P2b Task 2's test template against the actual `Today.test.ts` mock setup and corrected two real tripwires that would have wasted ~30min of debug time when the next iteration picks up P2b. Concrete fixes in `p2b-kpi-strip-grid.md` Task 2: (1) **Match-shape mismatch.** The template's `mockResolvedValueOnce` for `getCurrentSeasonMatches` used Football-Data API shape (`utcDate`, `homeTeam: { tla, name, shortName, crest }`, `score: { fullTime: { home, away }}`). But `dataService.getCurrentSeasonMatches()` returns the internal `Match[]` shape (`frontend/src/types/index.ts:27`) — snake_case `date`, `home_team`, `home_goals`, `result`, plus 18 nullable analytics fields. `matchToFixture` (already shipped in `lib/adapters/v3.ts`) reads the internal shape; an API-shape fixture would silently produce `home: { abbr: '', name: undefined }` and the test would fail for the wrong reason. Replaced the inline objects with a describe-scoped `mkMatch(id, home, away, date)` factory mirroring the existing top-of-file `mockMatch` (a vitest-hoisting note explains why the factory is *inside* the describe block, not at module scope). (2) **Default brier-mock missing.** The existing `Today.test.ts` `getAccuracyStats` `mockReturnValue` predates `f0c2225`'s `brierScore` extension and lacks the field. The default `vi.mock` factory still works for P2a tests today because P2a's `Today.svelte` doesn't read `stats.brierScore`. But once Task 3 lands `value={stats.brierScore.toFixed(2)}` in the KPI strip, every P2a test that triggers `await component.load()` (i.e., 3 of 4 existing P2a tests) will throw `TypeError: Cannot read properties of undefined (reading 'toFixed')`. Added a new **Step 1** ("Update the existing default `getAccuracyStats` mock to include `brierScore`") before the test-append step so the default mock gets `brierScore: 0` proactively; renumbered the original Step 1 → Step 2 and Step 2 → Step 3, and updated Step 3's "Expected" line to call out that P2a tests stay green because `(0).toFixed(2)` is safe. Plan-only commit (precedent: `bdc551f`, `9726119`, `502982a`, `86c016f`, `27c88f5`, `3194a50`, `c736e91`); untagged — `v3.2` stays reserved for the full P2b slice when its visual zones land. Validation: `npm run test -- --run` confirmed 768/768 across 56 files (identical to last green) and no source change crept in alongside the docs edits.

- **(P2c Task 3 stickiness-test grooming, no code, no validation gate)** Pre-flight-checked the Phase 2 Playwright checkpoint spec template against the actual scroll-container structure of `BroadcastShell.svelte` and discovered the prescribed stickiness test was non-falsifying. The original draft used `page.mouse.wheel(0, 600)` + `boundingBox` before/after to assert that the strip's `y` coordinate doesn't change during scroll. Two compounding issues kill the assertion: (1) **scroll container is `<main>`, not the document body** — `BroadcastShell.svelte:14` has `h-screen overflow-hidden` on the outer flex wrapper, and `<main class="overflow-y-auto">` on line 41 is the actual scroll context. `page.mouse.wheel(0, 600)` from default cursor origin (0,0) at desktop viewport lands on the sidebar (`<aside class="hidden lg:flex w-64 …">` covers the first 256px) — the wheel event never dispatches on `<main>` and no scroll fires. (2) **Even if we hovered `<main>` first, the mocked content is shorter than the viewport** — `mockApi.ts` returns 10 matches total but `findCurrentGameweek` filters to one gameweek, leaving 1 hero + KPI + small/empty grid + trends + log + log; with default mocks the page fits within ~800px viewport height, so `<main>.scrollHeight ≤ clientHeight` and there's nothing to scroll regardless of where the wheel lands. Either way, both `boundingBox().y` reads return the same value, and `expect(initialBox?.y).toBeCloseTo(afterBox?.y ?? -1, 0)` passes whether the `sticky` class is present or not. A future refactor that drops `sticky` from `CommandStrip.svelte:38`'s `class="sticky top-0 z-20 …"` would not be caught. **Fix:** replaced the dynamic-scroll test with a static CSS-property assertion (`getComputedStyle(el).position === 'sticky'`) that deterministically fails if the class is removed, regardless of viewport size or content height. Added a "Scroll-stickiness note" paragraph in `p2c-below-fold.md` Task 3 between Step 1 and Step 2 explaining the tripwire so a future iteration that thinks "the assertion looks too lightweight" sees the reasoning trail before reverting. Also moved the runtime "stays pinned during scroll" verification into the Task 5 manual sweep checklist (where a human can scroll a real page with real data) so the dynamic check still happens — just at the right gate. Plan-only commit (precedent: `bdc551f`, `9726119`, `502982a`, `86c016f`, `27c88f5`, `3194a50`, `c736e91`, `42b9bdd`, `377e03d`, `d13d815`); untagged — `v3.3` stays reserved for the full P2c slice when its visual zones land. Validation: `npm run test -- --run` confirmed 768/768 across 56 files (identical to last green) and no source change crept in alongside the docs edits.

- **(P2b + P2c Task 2 test-pattern grooming, no code, no validation gate)** Pre-flight-checked the P2b and P2c Task 2 test templates against the actual screen-test convention in this codebase and converted the `waitFor`-based blocks to the established `await (component as { load(): Promise<void> }).load(); await act();` pattern. Concrete trigger: `Today.test.ts` (lines 96-121), `Predictions.test.ts` (28 instances), and `SeasonStats.test.ts` all use `await component.load(); await act();` for screen-level data-loaded assertions; **none** use `waitFor`. The P2a deviation note already documented this as the chosen convention ("This codebase's testing pattern doesn't rely on async `onMount` flushing before assertions in jsdom + @testing-library/svelte v5 + Svelte 4"), but the P2b/P2c plan templates predated that decision and used `waitFor`. Three concrete tripwires the conversion pre-empts: (1) `Today.test.ts`'s import line is `import { act, render } from '@testing-library/svelte';` — a literal copy-paste of the old templates would fail at parse time with `waitFor is not defined`. (2) `waitFor` retries its whole callback on any expect failure, which masks the true failing assertion when multiple expects share the callback (P2b had 4 expects in one block; P2c had 3 + 1 + 1). After conversion, each expect runs synchronously after `await act()` and a failure points straight at the line. (3) `await component.load()` flushes data deterministically in one tick; `waitFor` polls with retries, introducing time-sensitive flakiness on slower CI runners. Concrete edits: in `p2b-kpi-strip-grid.md` Task 2 Step 2, all four KPI/grid tests converted (4 `await waitFor(() => { … })` blocks → 4 `await component.load(); await act();` + inline assertions), with a "Test-pattern note (added 2026-04-26 plan grooming)" paragraph explaining the convention and the tripwire. In `p2c-below-fold.md` Task 2 Step 1, all three below-fold tests converted (3 `await waitFor` blocks → same pattern), with the same paragraph. Also added a sharper inline comment on the `[data-hit]` count assertion ("Each settled MatchRow renders a `[data-hit]` cell on the right (MatchRow.svelte:54-57)") so the iteration knows the contract is gated on `isFinished && isHit !== null`. Plan-only commit (precedent: `bdc551f`, `9726119`, `502982a`, `86c016f`, `27c88f5`, `3194a50`, `c736e91`, `42b9bdd`); untagged — `v3.2` stays reserved for the full P2b slice when its visual zones land. Validation: `git status` post-edit confirmed only the two plan files changed (no source drift), and the existing Today.test.ts pattern was confirmed via `grep -n "waitFor\|component.load\|await act"` showing zero `waitFor` matches across the three screen tests but 28+ `await component.load(); await act()` matches.

- **(P2b — Task 1b only, auto-gated sub-slice, no blocker)** Gated empty PROBABILITIES & MODELS sub-blocks in `MatchCard.svelte`. Three independent gates: 5-col models grid renders only when `prediction.models.length > 0`; xG block renders only when `xg.home > 0 || xg.away > 0`; ELO block renders only when `elo.home > 0 || elo.away > 0`. The outer 2-col `grid-cols-2` wrapper itself gates on either xG or ELO being populated, so an empty grid container can't render when both inner blocks are gated out. TOP-3 SCORELINES stays unconditional — `predictionToV3` always populates at least the predicted scoreline, so it's the section's minimum useful payload. Three new stable contract markers: `[data-models-grid]`, `[data-xg-block]`, `[data-elo-block]` (TOP-3 SCORELINES already had `[data-scorelines]` added by this slice for the always-renders contract). Closes the P2a follow-up about the empty 5-col grid + zero xG / zero ELO defects on the hero card; pre-empts the same defect on every grid card P2b Task 3 will produce from `predictionToV3(stored)`. vitest 775/775 across 56 files (+7 in a new `MatchCard — PROBABILITIES section graceful degradation` describe). svelte-check 0/0. Targeted: `MatchCard.test.ts` 24/24 (was 17/17). Sub-slice deliberately stops at the primitive-gating change — Tasks 2-5 of P2b (Today.test.ts mocks update + KPI strip + grid markup + manual sweep) are still pending. P2b's checklist box stays `[ ]`. Tagging: this commit is **untagged** — `v3.2` stays reserved for the full P2b slice when the visual zones land.
- **(P2b Task 1b sub-slice deviations from plan)**
  - Used `props: { ... }` wrapper inside `render()` calls to match the existing house style of `MatchCard.test.ts` (lines 10, 23, 33, etc. all wrap props this way). The plan template's snippets called `render(MatchCard, { fixture: fx, prediction: pred, defaultOpen: 'probabilities' })` directly — both work in `@testing-library/svelte` v5, but the wrapped form keeps grep patterns consistent across the file.
  - The TDD-discipline check landed exactly as the grooming note predicted: pre-fix run showed 4 of 7 failing (the 3 "renders … when populated" companions + 1 always-renders-scorelines), and the 3 "hides …" tests trivially passed pre-fix because `querySelector` returns `null` for any non-existent attribute. Post-fix all 7 pass. The bidirectional pairing locks the contract — a future revert that drops gating + markers together would still flip 4 tests red.
  - Skipped Playwright routing smoke for this sub-slice — Task 1b touches only `MatchCard.svelte`, not routing or shells. Earlier auto-gated sub-slices (P2b Task 1, P2c Task 1) followed the same component/data-internal pattern: vitest + svelte-check only. The Playwright gate is reserved for the full P2b slice (Task 4).

- **(addressed) P2b signed off 2026-04-26, tagged `v3.2`** — visual sweep at `/today` cleared (KPI strip 4 tiles render with Brier highlight, grid renders remaining-gameweek MatchCards with hero deduplication, mobile 1-col reflow OK, theme flip clean). `[x]` flipped. P2b's visual half landed: `screens/Today.svelte` now renders the KPI strip (4-tile grid: Picks · Accuracy · Brier · Avg Confidence) and the 2-col MatchCard grid for the rest of the gameweek (1-col mobile, hero deduplicated, empty-state hint when only the hero remains). Brier tile gets `data-state="highlight"` when `brierScore > 0.25`. `Today.test.ts` grew by 4 tests (2 KPI + 2 grid) and the default `getAccuracyStats` mock learnt `brierScore: 0`. vitest 779/779 across 56 files (was 775/775), svelte-check 0/0, Playwright routing 32/32 on `desktop-chrome` (14.1s). Pre-fix TDD-discipline check: all 4 new tests failed pre-implementation (zones don't exist yet); post-fix all 4 pass. Manual sweep checklist surfaced below — flip P2b's `[ ]` to `[x]` once cleared. (`brierScore` extension on `AccuracyStats` already shipped in `f0c2225`; PROBABILITIES gating in the prior sub-slice; `predictionTracker.ts`, `predictionTracker.test.ts`, `MatchCard.svelte`, `MatchCard.test.ts` are intentionally **not** in this commit — they shipped earlier.)
- **(P2b Tasks 2-3 deviations from plan)**
  - **`KpiTile` doesn't forward `data-*` rest props.** The plan template's `<KpiTile data-tile="picks" … />` would have silently dropped the attribute — KpiTile (`frontend/src/components/atoms/KpiTile.svelte:26`) renders a plain `<div>` with class only, no `{...$$restProps}`. Used the plan's authorised fallback ("the simplest path is to wrap each `<KpiTile>` in a `<div data-tile="...">` instead of forwarding the attribute") so the screen owns its own contract markers without coupling them to the atom's signature. The Brier wrapper also carries `data-state` (test asserts `brierTile?.getAttribute('data-state')`); both attributes live on the same element selected by `[data-tile="brier"]`. Reusable taxonomy for P3/P4/P7 hub screens that will also pin `[data-tile=…]` markers.
  - **Reactive `$:` ternary with string literals widened to `string`.** Initial implementation used `$: brierState = stats.brierScore > THRESHOLD ? 'highlight' : 'default';` — svelte-check rejected passing `brierState: string` to KpiTile's `state: 'default' | 'highlight' | 'muted'` prop. Svelte 4's reactive declarations don't always narrow ternaries with literal-string branches. Pre-declared `let brierState: 'default' | 'highlight' = 'default';` and reassigned in the `$:` block — control flow then sees the explicit narrow type. Worth remembering for P3 hub screens that pass `state` props to atoms. (`as const` on each branch is an alternative; this approach is one declaration vs two `as const` casts.)
  - **`String(m.id) !== heroFixture?.id` simplified to `m.id !== heroFixture?.id`.** Both come from the same source (`matchToFixture` passes `m.id` straight through to `fixture.id`), and `Match.id: string`. The plan template's `String(m.id)` was a defensive cast against an imagined number-id case that doesn't exist. Drops one runtime call per filter iteration.
  - **`fixturesForGameweek` import moved into the existing `from '../lib/gameweek'` line** rather than a separate import statement. Same module — combining keeps the import block tidy.

- **(addressed) P2c signed off 2026-04-26, tagged `v3.3`** — visual sweep at `/today` cleared (trends Spark renders accuracy-by-gameweek, "How we predict →" link navigates to `/settings/help`, 5-row MatchRow log renders with hit/miss indicators, empty-state branches OK, sticky strip stays pinned during scroll, theme flip clean, `e2e/checkpoint-p2.spec.ts` 3/3 on desktop-chrome + 3/3 on mobile-chrome). `[x]` flipped. Phase 2 closes here. P2c's below-fold zones landed: `screens/Today.svelte` now appends `[data-zone="trends"]` (`<SectionHeader kicker="MODEL TRENDS" title="How the predictions are landing">` with a `slot="right"` "How we predict →" link to `/settings/help`, over a `<Spark>` of `gameweekAccuracy.map(g => g.accuracy)` when at least one gameweek has settled — falls back to a "no settled predictions yet" copy block when empty) and `[data-zone="log"]` (a bordered list of up to 5 `<MatchRow>` entries built from `predictionTracker.getRecentPredictions(5)` via `storedPredictionToFixture` + `predictionToV3` — falls back to `[data-zone="log-empty"]` copy when empty). `Today.test.ts` grew by 3 tests (trends header + Spark, 5 MatchRow `[data-hit]` cells, log-empty branch). New `frontend/e2e/checkpoint-p2.spec.ts` mirrors `checkpoint-p0.spec.ts`'s structure: asserts the five zones (`[data-command-strip]` + hero/hero-empty + `[data-zone="kpi-strip"]` + grid/grid-empty + trends + log) all render on `/today`, asserts `getComputedStyle(strip).position === 'sticky'`, and re-runs the desktop theme-toggle check. vitest 782/782 across 56 files (was 779/779), svelte-check 0/0, Playwright routing 32/32 + checkpoint-p2 3/3 on `desktop-chrome`, checkpoint-p2 3/3 on `mobile-chrome`. Pre-fix TDD-discipline check landed exactly as expected: all 3 new vitest tests failed pre-implementation (zones don't exist yet) and pass post-implementation. Phase 2 is content-complete pending the manual sweep below. (`lib/adapters/v3.ts` Task 1 already shipped in `0059a49`; `MatchCard.svelte` Task 1b shipped in `03a4eb2` — neither is in this commit.)
- **(P2c Tasks 2-5 deviations from plan)**
  - **`Spark trend={…ternary…}` non-null assertion (`!`) blocked by Svelte 4's mustache parser.** The plan template inlined `gameweekAccuracy.at(-1)!.accuracy >= gameweekAccuracy[0].accuracy ? 'up' : 'down'` directly in the `<Spark trend={…}>` prop. Svelte 4's template parser doesn't accept the `!` non-null assertion (or `as` casts) inside `{…}` — only the `<script>` block runs the full TS parser. Hoisted to a `$:` reactive declaration in the script: `$: trendsValues = gameweekAccuracy.map((g) => g.accuracy);` and `$: trendsDirection = gameweekAccuracy.length > 1 && gameweekAccuracy[gameweekAccuracy.length - 1].accuracy >= gameweekAccuracy[0].accuracy ? 'up' : 'down';`. Same shape as the brier `let + $:` pattern from P2b — pre-declared `let trendsDirection: 'up' | 'down' | 'flat' = 'flat';` so svelte-check narrows the ternary's RHS literals to satisfy the typed binding (without the `let`, the `$:` infers `string`, and `<Spark trend={trendsDirection}>` rejects with the same widening error P2b hit on `brierState`). Bonus: replaced `.at(-1)!` with `[gameweekAccuracy.length - 1]` so neither path needs a non-null assertion. Plan-recorded so a future P3 iteration that hits the same parser limit doesn't waste cycles.
  - **`gameweekAccuracy.length > 1` (not `> 0`) for trend computation.** A single-gameweek series has no "first vs last" delta; comparing `arr[0].accuracy >= arr[0].accuracy` is always `true` and would falsely report `'up'`. The `> 1` guard makes the ternary fall to `'down'` (falsy branch) for length-1 series — which renders the Spark as a single point with the `text-destructive` flat colour. Acceptable degraded display for a 1-gw history; the proper fix lands in P3+ when historical breadth grows. The Spark `{#if gameweekAccuracy.length > 0}` gate stays as written — we still want the Spark to render for length-1 series.
  - **Mobile-chrome Playwright run kept all 3 tests green.** The plan flagged a fallback ("if mobile-chrome fails on the strip stickiness, gate with `test.skip`") but it wasn't needed: the static CSS-property assertion is viewport-independent, and the theme-toggle test self-pins to 1280×800 inside the test (mirroring the P0-cp pattern). All 3 P2 checkpoint tests pass on both projects without per-test skips.
  - **No new `data-*` markers added to existing zones.** The plan template was internally consistent on `[data-zone="trends"]` and `[data-zone="log"]` / `[data-zone="log-empty"]`; no surface in P2a/P2b's existing markup needed touching. `[data-command-strip]` already lived on CommandStrip.svelte:40 from P2a, so the Playwright spec reuses it as-is.

- **(P3a, awaiting human eyeball)** P3a shipped `screens/fixtures/Live.svelte` (in-play `MatchCard`s via `dataService.getLiveMatches()` + 30s polling that pauses on `document.hidden`) and `components/fixtures/LiveBanner.svelte` (pulsing red dot, MIN N', score, optional xG row, optional prob-shift row). `App.svelte`'s `/fixtures/live` route now mounts the new `Live` screen; legacy `LiveMatches.svelte` is unimported and scheduled for P10 cleanup. vitest 796/796 across 58 files (+14 across 2 new files: LiveBanner 7, Live 7), svelte-check 0/0, Playwright routing 32/32 on `desktop-chrome` (14.0s). Manual sweep checklist surfaced below. Tag at sign-off → `v3.4`.
- **(P3a deviations from plan)**
  - **`[data-block]` selector swapped to `[data-block="center"]` for one-per-card counting.** The plan template asserted `expect(container.querySelectorAll('[data-block]')).toHaveLength(3)` for 3 live matches, but each `MatchCard` exposes 3 `[data-block]` markers (`home`/`center`/`away`, per `MatchCard.svelte:76,89,109`), so 3 cards = 9 markers. The established convention in `MatchList.test.ts:212` is `[data-block="center"]` to count cards (one center block per card). Updated the assertion accordingly so the contract matches reality and a future MatchCard refactor that splits/merges `home`/`away` markers doesn't false-flag this test.
  - **`bg-bg-raised` instead of `bg-card-raised` on the LiveBanner root.** The plan template wrote `class="… bg-card-raised border-l-2 border-destructive"`. Tailwind's `bg-card-raised` does not exist in this project's config — the canonical raised-surface class is `bg-bg-raised` (per `CommandStrip.svelte:39` which uses the same surface). `bg-card` exists (mapped to `--card`) but isn't the same token. Using `bg-bg-raised` keeps the LiveBanner consistent with the CommandStrip and other v3 raised surfaces.
  - **Hoisted xG/probShift formatting into `$:` reactive declarations.** The plan template inlined `{xgHome!.toFixed(2)}` and `{(probShift!.home * 100).toFixed(0)}` directly in mustaches. Svelte 4's mustache parser doesn't accept the `!` non-null assertion (same parser limit P2c hit on Spark `trend={…}`). Pre-computed `xgHomeText`, `xgAwayText`, `probShiftHomePct`, `probShiftDrawPct`, `probShiftAwayPct` in `$:` blocks (with `typeof === 'number'` / `probShift ? : 0` guards), then referenced the strings/numbers in the template. Same fix-pattern as P2b's `brierState` and P2c's `trendsDirection` — worth pinning as a Svelte-4 convention for any future P3+ slice that needs a TS-narrowed expression inside `{...}`.
  - **`load()` self-starts polling instead of `.then(startPolling)` from `onMount`.** The plan template used `onMount(() => { void load().then(startPolling); })`. Tests in this codebase (Today.test.ts, Predictions.test.ts, etc.) trigger data load via explicit `await component.load()` because `@testing-library/svelte` v5 + Svelte 4 doesn't reliably fire `onMount` before assertions in jsdom. The plan's `.then(startPolling)` chain therefore never set up `setInterval` in tests, so the "refetches every 30s" test failed (count stayed at 1 forever). Fix: moved `startPolling()` to the end of `load()` (idempotent — early-returns if `pollIntervalId !== null`), and simplified `onMount(load)` to mirror Today.svelte's pattern. Both runtime and tests now exercise the same code path. Also dropped the plan's `visibilitychange` listener — it called `startPolling` on every visibility flip, but `startPolling` is already idempotent and the per-tick `document.hidden` check inside `poll()` is what actually gates network calls; the listener was dead code.
  - **Added a 7th LiveBanner test for `clampedMinute`.** The plan template defined `$: clampedMinute = Math.min(Math.max(minute, 0), 120);` but didn't assert it. Added a "clamps minute values above 120 down to 120 (extra-time cap)" test so a future refactor that drops the clamp (or changes the cap) gets caught. Net: vitest landed at 796/796 (+1 over plan's predicted 795/795).
- **(P3a manual sweep checklist)** Boot `cd frontend && npm run dev`, then:
  ```
  [ ] /fixtures/live — at least one in-play MatchCard if matches are currently live (or "No matches in play right now" empty-state if not)
  [ ] /fixtures/live — LiveBanner above each card: pulsing red dot, MIN value, score in large numerals, xG row when both home + away xG present
  [ ] /fixtures/live — leave the tab open for ~60s — DevTools Network shows getLiveMatches firing every 30s
  [ ] /fixtures/live — switch to a different tab for 30s, switch back — no extra fetches fired while hidden
  [ ] /fixtures/live — Toggle theme — every banner + MatchCard flips cleanly
  [ ] Resize <1024px — banners stay legible, MatchCards stack vertically
  [ ] Empty state: when no live matches, "No matches in play right now" copy renders centered
  ```
  Sign-off action: flip P3a's `[ ]` → `[x]` in the checklist above, then `git tag v3.4 <P3a-commit-sha>`. Next slice is P3b — `screens/fixtures/Matches.svelte` at `/fixtures/matches`.

- **(P3b — Task 1 only, auto-gated sub-slice, no blocker)** Shipped `frontend/src/lib/fixtureGrouping.ts` with `groupMatchesByDate`, `formatDateLabel`, `formatCountLabel` + the `FixtureGroup` interface. Pure utility module — no DOM, no Svelte, no overlap with P3a's pending visual review surface. Same auto-gated-sub-slice pattern as P2b Task 1 (Brier extension), P2c Task 1 (`storedPredictionToFixture` adapter), and P2b Task 1b (PROBABILITIES gating). vitest 802/802 across 59 files (+6 in a new `fixtureGrouping.test.ts`). svelte-check 0/0. Targeted: `fixtureGrouping.test.ts` 6/6. Date-grouping reusable in P9a (Match deep-dive next-fixtures strip). Sub-slice deliberately stops at the data-layer change — Tasks 2-5 of P3b (`FilterChips.svelte`, `Matches.svelte`, route mount, manual sweep) are still pending. P3b's checklist box stays `[ ]`. Tagging: this commit is **untagged** — `v3.5` stays reserved for the full P3b slice when the visual zones land.
- **(P3b Task 1 sub-slice deviations from plan)** None — implementation tracked the plan template verbatim (`fixtureGrouping.ts` + 6 tests). Pre-flight day-of-week verification: April 12 2026 and April 5 2026 are both Sundays (`new Date('2026-04-12T00:00:00Z').getUTCDay() === 0` → `DAY_NAMES[0] === 'SUN'`), so the plan's test fixtures match the implementation's UTC math without timezone surprises. Skipped Playwright routing smoke for this sub-slice — Task 1 is data-layer only, no routing touched. Earlier auto-gated sub-slices (P2b Task 1, P2c Task 1, P2b Task 1b) followed the same vitest + svelte-check-only validation gate.

- **(P3b — Task 2 only, auto-gated sub-slice, no blocker)** Shipped `frontend/src/components/fixtures/FilterChips.svelte` (4-chip pill row: All · Top 6 · Relegation · TV picks) + co-located 4-test file. Pure presentational component — no data-layer touch, no overlap with P3a's pending visual review surface (P3a renders at `/fixtures/live`; FilterChips is destined for `/fixtures/matches` via Task 3). Same auto-gated-sub-slice pattern as Task 1, P2b Task 1/1b, P2c Task 1. vitest 806/806 across 60 files (+4 in `FilterChips.test.ts`). svelte-check 0/0. Targeted: `FilterChips.test.ts` 4/4. The exported `FilterId` type (`'all' | 'top6' | 'relegation' | 'tv'`) is the contract Task 3's `Matches.svelte` will import. Sub-slice stops at the component primitive — Tasks 3-5 of P3b (`Matches.svelte` + tests, route mount, manual sweep) still pending. P3b's checklist box stays `[ ]`. Tagging: this commit is **untagged** — `v3.5` stays reserved for the full P3b slice when the visual zones land.
- **(P3b Task 2 sub-slice deviations from plan)**
  - **`aria-pressed:` Tailwind variant fallback used (as plan-authorised).** Pre-flight `grep -n "aria-pressed" frontend/tailwind.config.js` returned no match (the config has no plugin enabling attribute-driven variants). Per the plan's documented fallback ("if the project's Tailwind config doesn't have the `aria-pressed` variant enabled, fall back to `class:bg-primary={chip.id === active}` etc."), the shipped component uses Svelte's `class:` directive for active-state styling — same convention as `Tabs.svelte:14-18` for `aria-selected`. The `aria-pressed` HTML attribute itself is still emitted on each button (test #2 asserts on it); only the *styling bridge* changed from attribute-driven Tailwind variant to class-directive. P3b plan doc updated with the resolution so a future "simplify the chip styles" PR doesn't quietly revert to a non-existent variant.
  - **`FilterId` exported from `<script context="module">`, not from the instance script.** Svelte 4's instance-script `export` declares props; module-script `export` declares ambient/static exports importable as `import FilterChips, { type FilterId } from './FilterChips.svelte'`. Task 3's `Matches.svelte` will use exactly that import shape. Pinning the export at the module scope keeps the type re-exportable without forcing every consumer to redeclare the union.
  - Skipped Playwright routing smoke for this sub-slice — Task 2 is component-internal, no routing touched. Same gate convention as the prior auto-gated sub-slices.

- **(P3b — Tasks 3 + 4, awaiting human eyeball)** P3b's visual surface landed: `frontend/src/screens/fixtures/Matches.svelte` mounts at `/fixtures/matches` (replaces legacy `MatchList.svelte` on that route — `MatchList` import removed from `App.svelte`; component itself stays in tree until P10 cleanup). Renders fixtures grouped by date via `groupMatchesByDate` (`<SectionHeader kicker="SAT 12 APR" title="Five fixtures" />` over a stack of `<MatchCard>`s per group), with a `<FilterChips>` row at the top (*All · Top 6 · Relegation · TV picks*). Filtering is reactive (`$: filteredMatches = applyFilter(matches, activeFilter)` → `$: groups = groupMatchesByDate(filteredMatches)`), so chip clicks re-derive groups for free. TV picks is a placeholder (returns all matches — no API surface to identify TV picks); documented inline. RELEGATION is a hardcoded bottom-3 set for the MVP (pulling from `getStandings()` is a P3c follow-up where the standings hub becomes the natural source of truth). vitest **811/811 across 61 files** (+5 in new `Matches.test.ts` — exactly matches plan prediction), svelte-check 0/0, Playwright `routing.spec.ts` 32/32 on `desktop-chrome` (13.9s). Pre-fix TDD-discipline check: tests reference `Matches.svelte` which doesn't exist pre-fix, so all 5 fail at module-load — post-fix all 5 pass. Manual sweep checklist surfaced below. P3b's `[ ]` stays unchecked pending human eyeball at `/fixtures/matches`. Tag at sign-off → `v3.5`. (`fixtureGrouping.ts` Task 1 already shipped in `c1b48f9`; `FilterChips.svelte` Task 2 in `05f8bd0` — neither is in this commit.)
- **(P3b Tasks 3 + 4 deviations from plan)**
  - **`MatchList` import removed from `App.svelte`** — the plan said "legacy `MatchList.svelte` stays in tree (unimported by App.svelte after this slice)". Implemented by deleting the `import MatchList from './components/MatchList.svelte';` line as well as the route mount, since nothing else in `App.svelte` references `MatchList`. The file itself stays unaffected (still imports / runs / passes its own 12-test suite); P10 deletes it. Strangler-fig discipline preserved.
  - **Plan template's `it('renders FilterChips with "all" active by default', async () => {…})` was made non-async.** No `await` inside — the `aria-pressed="true"` assertion on the default-active chip runs synchronously after the initial `render(Matches)`. Dropping the `async` keeps the test signature honest about its actual control flow. Other tests that need `await component.load()` keep `async`.
  - **No new Playwright spec.** Per the plan's Task 4 note, `routing.spec.ts` already covers `/fixtures/matches` because the `/matches → /fixtures/matches` redirect was wired in P0b. The spec passes 32/32 on desktop-chrome unchanged — the swap from `MatchList` to `Matches` is invisible to the route-coverage layer.
  - **`Matches.test.ts` data-screen mock structure mirrors `Live.test.ts` exactly** — same `vi.mock(dataService, …)` + `vi.mock(predictionTracker, …)` + `mkUpcomingMatch` factory inside the test file (vitest hoists `vi.mock`, so the factory must live inside `describe` for any of its values to be safely referenced). One source of test-pattern truth across the Fixtures sub-phase screens (`Live`, `Matches`, and forthcoming `Standings`).
- **(P3b manual sweep checklist)** Boot `cd frontend && npm run dev`, then:
  ```
  [ ] /fixtures/matches — fixtures grouped by date, headers show "SAT 12 APR" + count phrase ("Five fixtures" / "One fixture")
  [ ] FilterChips row — "All" active by default; click "Top 6" → only fixtures involving a Top 6 team remain
  [ ] Click "Relegation" → bottom-3 candidates filter applies (set is hardcoded — verify it makes sense; replace with standings-derived set in P3c follow-up if wanted)
  [ ] Click "TV picks" → currently shows all matches (placeholder; OK for MVP per spec)
  [ ] Toggle theme — chips, section headers, MatchCards all flip cleanly
  [ ] Resize <1024px — chips stay legible, MatchCards stack vertically
  [ ] Empty filter result: pick a filter that excludes everything — `[data-matches-empty]` copy renders ("No fixtures match the current filter.")
  ```
  Sign-off action: flip P3b's `[ ]` → `[x]` in the Phase 3 checklist above, then `git tag v3.5 <P3b-commit-sha>`. Next slice is P3c — `screens/fixtures/Standings.svelte` at `/fixtures/standings`.

- **(P3c — Task 1 only, auto-gated sub-slice, no blocker)** Shipped `frontend/src/lib/standingsHelpers.ts` with `classifyZone`, `zoneRowClass`, `parseFormString`, `pointsPerGame` + the `QualificationZone` union type, and a co-located `standingsHelpers.test.ts` with 11 tests (4 classifyZone + 1 zoneRowClass + 4 parseFormString + 2 pointsPerGame). Pure-function unit — no DOM, no Svelte, no overlap with P3a/P3b's pending visual review surfaces. Same auto-gated sub-slice pattern as P3b Task 1 (`fixtureGrouping.ts`), P3b Task 2 (`FilterChips.svelte`), P2b Task 1 (Brier), P2c Task 1 (`storedPredictionToFixture`), P2b Task 1b (PROBABILITIES gating). vitest **822/822 across 62 files** (was 811/811 across 61 — exactly +11 in 1 new file). svelte-check 0/0. Targeted: `standingsHelpers.test.ts` 11/11. Helpers reusable in P9a (Match deep-dive) where partial team-position context can call `classifyZone` without faking a full `Standing`. Sub-slice deliberately stops at the helpers — Tasks 2-4 of P3c (`Standings.svelte` + tests, route mount, manual sweep) still pending. P3c's checklist box stays `[ ]`. Tagging: this commit is **untagged** — `v3.6` stays reserved for the full P3c slice when the visual zones land.
- **(P3c Task 1 sub-slice deviations from plan)**
  - **Test count: 11 not 8.** Plan prose says "Task 1 — `lib/standingsHelpers.ts` + 8 tests" but the prescribed code template has **11** `it()` blocks (4 classifyZone, 1 zoneRowClass, 4 parseFormString, 2 pointsPerGame). Followed the code template. Updated the Task 3 expected-count in `p3c-fixtures-standings.md` from "825/825 (810 + 8 + 7)" to "828/828 (810 + 11 + 7)" so the next iteration's gate matches reality.
  - **Skipped Playwright routing smoke** — Task 1 is a pure-data-layer module, no routing surface touched. Same gate convention as the prior auto-gated sub-slices (P3b Task 1 / Task 2, P2b Task 1 / Task 1b, P2c Task 1).
  - **Pre-flight Tailwind-token verification (plan-prescribed) ran clean for `bg-accent/*` and `bg-destructive/*`.** Both tokens (`--accent` line 45/98, `--destructive` line 50/103 of `tokens.css`) are surfaced through `tailwind.config.js:31-41` with `<alpha-value>` placeholders, so the JIT compiler will emit valid classes for `bg-accent/8`, `bg-accent/4`, `bg-destructive/6`. Task 2 won't hit the purge tripwire the plan flagged.
  - **NEW Task 2 tripwire surfaced: `bg-text-faint/30` will not work as written.** The plan template's pending-form-indicator markup (`<span class="w-2 h-2 rounded-full bg-text-faint/30">`) was prescribed assuming `bg-text-faint` is a Tailwind-generated utility. It isn't — `frontend/src/app.css:354` defines it as a hand-rolled `.bg-text-faint { background-color: hsl(var(--text-faint)); }` rule inside `@layer utilities`. The `/30` arbitrary-opacity modifier only works on Tailwind-generated utilities that use `<alpha-value>` placeholders; hand-rolled rules ignore the modifier silently. The next iteration that picks up P3c Task 2 needs to either (a) inline the alpha as `style="background-color: hsl(var(--text-faint) / 0.3)"`, or (b) add a co-located `.bg-text-faint\/30 { background-color: hsl(var(--text-faint) / 0.3); }` rule to `@layer utilities` in `app.css`, or (c) drop the opacity and use `bg-text-faint` at full intensity (acceptable given the dot is 8×8px). Recommended path: option (a) — keeps the new `app.css` surface area to zero and the inline style is local to the one element that needs it.

- **(P3c — Tasks 2 + 3, awaiting human eyeball)** P3c's visual surface landed: `frontend/src/screens/fixtures/Standings.svelte` mounts at `/fixtures/standings` (replaces legacy `StandingsTable.svelte` on that route — `StandingsTable` import removed from `App.svelte`; component itself stays in tree until P10 cleanup). 13-column grid (Pos · Crest · Team · P · W · D · L · GF · GA · GD · Pts · Form L5 · PPG `<Spark>`) with reactive `$: rows = standings.map(...)` derivation, qualification-zone background tints (UCL pos 1-4 `bg-accent/8`, UEL pos 5 `bg-accent/4`, relegation pos 18-20 `bg-destructive/6`), and 5 form indicators per row (`<FormDot>` for played results, muted pending dot otherwise). Click row with an upcoming fixture → `navigate('/match/[next-fixture-id]')`; rows without one swallow the click silently via early-return. vitest **829/829 across 63 files** (+7 in new `Standings.test.ts` — exactly matches plan prediction of 822 + 7), svelte-check 0/0. Pre-fix TDD-discipline check: tests reference `Standings.svelte` which doesn't exist pre-fix, so all 7 fail at module-load — post-fix all 7 pass on first run. Manual sweep checklist surfaced below. P3c's `[ ]` stays unchecked pending human eyeball at `/fixtures/standings`. Tag at sign-off → `v3.6`. (`standingsHelpers.ts` Task 1 already shipped in `0f78341` — not in this commit.)
- **(P3c Tasks 2 + 3 deviations from plan)**
  - **Pending-dot opacity used inline-style workaround (option a from the Task 1 tripwire note).** `<span style="background-color: hsl(var(--text-faint) / 0.3)" data-form-indicator data-form-state="pending">` instead of the plan's prescribed `class="...bg-text-faint/30"`. Keeps the `app.css` surface area at zero, and the inline style is local to the single element that needs it. Bumped the dot down from `w-2 h-2` (8px) to `w-1.5 h-1.5` (6px) to match `<FormDot size="sm">`'s 6px so pending and played indicators visually align in the same row.
  - **`Crest` atom contract drift.** Plan template wrote `<Crest src={row.s.team.crest} alt={row.s.team.name} size="sm" />` but the actual atom (`components/atoms/Crest.svelte`) takes `team: TeamSummary` (`{ abbr, name, crestUrl?, primaryColor?, secondaryColor? }`) — no `src`/`alt` props exist. Inlined a one-line `toTeamSummary(team) → { abbr: team.tla, name: team.name, crestUrl: team.crest || undefined }` adapter in the screen — `Standing.team.tla` is already the 3-letter PL abbreviation, no string-munging needed. Did not extend `lib/adapters/v3.ts` because this is the only consumer for the `Standing → TeamSummary` direction (the `Match → Fixture` adapter there handles the *other* direction).
  - **Header row + numeric columns text-aligned right** (`class="text-right"`) where the plan template was silent — keeps tabular-nums data visually aligned with the header label, which the plan's column-width grid (`2rem` for P/W/D/L, `3rem` for GF/GA/GD/Pts) only enforces for cell width, not for content alignment.
  - **`onMount(load)` + `export async function load()` pattern** mirrors `Live.svelte` and `Matches.svelte` so test files can `await component.load()` instead of waiting on a promise inside `onMount` (which would race with `await act()`). This convention was set in P2a's `Today.svelte` and is now standard across all Phase 3 screens.
- **(P3c manual sweep checklist)** Boot `cd frontend && npm run dev`, then:
  ```
  [ ] /fixtures/standings — 20 rows render, header row legible
  [ ] Rows 1-4: subtle UCL tint (bg-accent/8); row 5: lighter UEL tint (bg-accent/4); rows 18-20: red destructive tint (bg-destructive/6)
  [ ] Each row Form column shows exactly 5 dots; null-form rows show 5 muted "pending" dots at 6px sized to match FormDot
  [ ] PPG Spark renders as a single-point line (proper history pending Phase 5 follow-up — visually a flat dash)
  [ ] Click a row with an upcoming fixture — URL changes to /match/[id] (placeholder route currently)
  [ ] Click a row with no upcoming fixture (e.g. season-end) — no navigation, no error
  [ ] Toggle theme — zone bands flip cleanly, no contrast issues
  [ ] Resize <1024px — table scrolls horizontally OR collapses gracefully (acceptable degradation; mobile-specific table treatment is P9b polish)
  ```
  Sign-off action: flip P3c's `[ ]` → `[x]` in the Phase 3 checklist above, then `git tag v3.6 <P3c-commit-sha>`. Next slice is P3-fixtures-cp — `frontend/e2e/checkpoint-p3-fixtures.spec.ts`.

- **(P3-fixtures-cp, auto-gated half landed; manual sweep recommended but not blocking)** Shipped `frontend/e2e/checkpoint-p3-fixtures.spec.ts` — 5 tests covering the Fixtures hub's three sub-tabs (Live, Matches, Standings): primary `[data-screen]` zones present, FilterChips render exactly 4 chips, Standings renders 20 rows × 13 columns, sub-tab navigation cleanly tears down the previous screen, theme toggle still flips on `/fixtures/*`. Mirrors `checkpoint-p0.spec.ts` + `checkpoint-p2.spec.ts` shape. Test-only commit — no source changes, no `helpers.ts` extension needed. vitest **829/829 across 63 files** (unchanged — Playwright-only slice). svelte-check **0/0**. Playwright **5/5 on desktop-chrome (11.4s) + 5/5 on mobile-chrome (11.3s) = 10/10**. The auto-gated half (spec write+pass+commit) is satisfied; manual sweep below remains for the human eyeball before flipping `[x]` and tagging `v3.7`.
- **(P3-fixtures-cp deviations from plan)**
  - **Tabs primitive isn't mounted at hub level — adapted Test 4 to use `page.goto()` instead of `getByRole('tab').click()`.** Pre-flight `grep` confirmed `Tabs.svelte` (P0d) is only referenced by its own `Tabs.test.ts`; `App.svelte` mounts each `/fixtures/*` sub-route directly without a hub-level `<Tabs>` instance. The plan template's `page.getByRole('tab', { name: /Matches/i }).click()` would have failed at locator-resolution because no `role="tab"` element exists on `/fixtures/*` routes. Adapted by switching to `page.goto('/fixtures/matches')` etc., and tightened the previous-screen invariant from `toBeHidden()` to `toHaveCount(0)` so the test catches a *real* regression class — leaked DOM where Svelte fails to tear down a route. The structural-integrity goal of the checkpoint is preserved (URL changes, content swaps, no leakage); only the navigation primitive differs. **Follow-up**: mount `<Tabs>` at the Fixtures hub (and Predictions, Insights, Settings hubs) — needs a hub-shell wrapper component or App.svelte routes restructured around a parent route. Out of scope for this slice; record as a P3-followup or roll into the Predictions sub-phase plan grooming when those hubs add Tabs needs of their own.
  - **`mockFootballApi` not exported from `helpers.ts` — used `setupApp` only.** Plan template's `import { setupApp, mockFootballApi } from './helpers'` is broken: `helpers.ts:2` imports `mockFootballApi` from `./mockApi` for internal use inside `setupApp` but doesn't re-export it. Followed `checkpoint-p2.spec.ts`'s exact import pattern (`import { setupApp } from './helpers'`) — `setupApp` already calls `mockFootballApi(page)` internally, so a second call would be redundant. No deviation from intent; just from the plan template's literal text.
  - **Theme-toggle test passed on `mobile-chrome` without a `test.skip` gate.** The plan template predicted "4-5/5 on mobile-chrome" with the theme-toggle as the at-risk test. Pre-pinning viewport via `await page.setViewportSize({ width: 1280, height: 800 })` *before* `setupApp` makes the BroadcastShell's `<aside class="hidden lg:flex">` (where the toggle lives) become visible regardless of the project's default Pixel-5 393px viewport. Same pattern that worked on `checkpoint-p0.spec.ts`'s theme-toggle test. The `test.skip(viewport.width < 1024, ...)` contingency wasn't needed.
  - **Live + standings tests use the empty-or-populated bidirectional pattern** (`(await rows.count()) + (await empty.count()) > 0`) for the steady-state assertion. The mock returns no IN_PLAY matches (status: 'FINISHED' or 'TIMED' only), so `dataService.getLiveMatches()` filters down to an empty list and the `[data-live-empty]` branch renders. Standings on the other hand always has 20 rows because the mock teams array has exactly 20 entries — the `expect(rows).toHaveCount(20)` assertion is unconditional there. Both patterns are valid for their respective steady states.
  - **No `mockFootballApi` extension needed.** Pre-flight check confirmed `buildStandingsResponse()` already iterates the full 20-team array (`mockApi.ts:113-133`), so the 20-row count assertion passes deterministically without touching `helpers.ts` or `mockApi.ts`.
- **(P3-fixtures-cp manual sweep checklist)** Boot `cd frontend && npm run dev`, then:
  ```
  [ ] /fixtures/live — at least one live MatchCard if matches are in progress, or the empty-state copy
  [ ] /fixtures/live — LiveBanner pulses, MIN ticks, score legible
  [ ] /fixtures/matches — fixtures grouped by date, all 4 filter chips work
  [ ] /fixtures/matches — Top 6 / Relegation filters narrow the list correctly
  [ ] /fixtures/standings — 20 rows, qualification zones tinted, FormDot rows correct
  [ ] /fixtures/standings — clicking a row with an upcoming fixture navigates (placeholder URL OK)
  [ ] Sub-tab navigation: visit Live → Matches → Standings via direct URL or sidebar; URL + content + active-state all update
       (NOTE: Tabs primitive not yet mounted at the hub — sidebar nav is the current path; in-hub Tabs is a follow-up)
  [ ] Toggle theme on /fixtures/live, /fixtures/matches, /fixtures/standings — every screen flips cleanly
  [ ] Resize <1024px — every screen stays usable; standings may need horizontal scroll (acceptable)
  [ ] Mobile bottom tab bar shows Fixtures as active when on any /fixtures/* URL
  [ ] /matches → /fixtures/matches redirect still works (legacy URL)
  [ ] /standings → /fixtures/standings redirect still works (legacy URL)
  [ ] /live-matches → /fixtures/live redirect still works (legacy URL)
  ```
  Sign-off action: flip P3-fixtures-cp's `[ ]` → `[x]` in the Phase 3 checklist above, then `git tag v3.7 <P3-fixtures-cp-commit-sha>`. **Closes the Fixtures sub-phase.** Next slice is the Predictions hub plan grooming — draft `p4a-…` through `p4-cp-…` files in `docs/superpowers/plans/2026-04-26-phase-3-per-hub/`. Then P4a — Predictions This Week.
- **(P3-fixtures-cp follow-ups)**
  - **Mount `<Tabs>` at Fixtures hub.** Currently the Tabs primitive (P0d) lives only in tests. Three options for a future iteration: (a) introduce a `FixturesHub.svelte` wrapper that nests `<Tabs>` + an inner `<Router>`/`<Route>` switch, mounted at `/fixtures/*`; (b) hoist `<Tabs>` into `BroadcastShell` keyed off the active hub from `routes.ts`; (c) add a route-aware Tabs slot to BroadcastShell that consumers populate. Option (a) is the smallest blast radius and matches the spec's intent. Same pattern will apply to Predictions, Insights, Settings hubs — group as a single "hub-shells" slice or fold into each sub-phase's first slice.
  - **Stand up the Predictions hub plan grooming** (P4a — P4-cp drafts) before opening P4a. Plan-only commits, untagged. Same pattern as the Fixtures sub-phase grooming did.

## Human notes for next iteration

*(Drop notes here mid-run. The next ralph build iteration consumes them as part of the slice contract: address every note OR explain in `## Notes / discoveries` why a note doesn't apply. Once addressed, ralph moves the consumed note to `## Notes / discoveries` prefixed with `(addressed) ` and a one-line summary of how it was addressed.)*

- *(empty — drop notes between iterations as needed)*

## Next recommended build slice

**Phase 2 closed 2026-04-26.** All Phase 2 slices signed off + tagged: P2a → `v3.1`, P2b → `v3.2`, P2c → `v3.3`. Phase 3 (per-hub screens) is now the active phase, and the Fixtures sub-phase is plan-ready.

**Already in the codebase from Phase 2** (carry-overs reusable in Phase 3):
- `frontend/src/lib/adapters/v3.ts` — `matchToFixture`, `predictionToV3`, `storedPredictionToFixture`. Expect every Phase 3 hub screen that renders fixtures to lean on these adapters.
- `frontend/src/lib/gameweek.ts` — `findCurrentGameweek`, `fixturesForGameweek`, `nextKickoff`. P3a/P3b will reuse.
- `predictionTracker.brierScore` on `AccuracyStats` — already shipped, P4b will pull straight from it.
- `frontend/e2e/checkpoint-p0.spec.ts` + `checkpoint-p2.spec.ts` — pattern for the per-hub checkpoints (`p3-fixtures-cp`, `p4-cp`, `p7-cp`).

**Up next — the 20-iteration loop run targets the Fixtures sub-phase to completion:**

1. **P3a — Fixtures Live** *(Manual-gated, code-complete in this iteration — awaiting human eyeball)* — `screens/fixtures/Live.svelte` shipped, mounted at `/fixtures/live`, renders 1–3 in-play `MatchCard`s with a `<LiveBanner />` row above each (pulsing red dot, `MIN 67'`, current score `text-metric-lg`, xG running totals when present). Polls `dataService.getLiveMatches()` every 30s; pauses when `document.hidden`. Sweep checklist surfaced in `## Notes / discoveries`. Tag at sign-off → `v3.4`.
2. **P3b — Fixtures Matches** *(Manual-gated, code-complete in this iteration — awaiting human eyeball)* — `screens/fixtures/Matches.svelte` shipped, mounts at `/fixtures/matches`, replaces legacy `MatchList.svelte` on that route (`MatchList` import dropped from `App.svelte`; component itself stays in tree until P10 cleanup). Date-grouped `<SectionHeader>` + `<MatchCard>`s, with `<FilterChips>` row (*All · Top 6 · Relegation · TV picks*). Reactive filter chain (`$: filteredMatches → $: groups`). RELEGATION is a hardcoded MVP set (P3c follow-up to derive from standings). Sweep checklist surfaced in `## Notes / discoveries`. Tag at sign-off → `v3.5`.
3. **P3c — Fixtures Standings** *(Manual-gated, code-complete in this iteration — awaiting human eyeball)* — `screens/fixtures/Standings.svelte` shipped, mounts at `/fixtures/standings`, replaces the legacy `StandingsTable.svelte` on that route (`StandingsTable` import dropped from `App.svelte`; component itself stays in tree until P10 cleanup). 13-column grid (Pos · Crest · Team · P · W · D · L · GF · GA · GD · Pts · Form L5 · PPG `<Spark>`), qualification-zone background tints (UCL/UEL/relegation), 5 form indicators per row (FormDot or muted pending). Click row → `navigate('/match/[next-fixture-id]')` when upcoming fixture exists; silent no-op otherwise. Sweep checklist surfaced in `## Notes / discoveries`. Tag at sign-off → `v3.6`. **All three of P3c's tasks have shipped** — Task 1 helpers in `0f78341`, Tasks 2 + 3 (Standings screen + route mount + 7 tests) in this iteration's commit (vitest 829/829 across 63 files = 822 + 7).
4. **P3-fixtures-cp — Fixtures checkpoint** *(Auto-gated half **landed in this iteration**; manual sweep recommended but not blocking)* — `frontend/e2e/checkpoint-p3-fixtures.spec.ts` shipped with 5 tests covering all 3 Fixtures sub-tabs. Playwright **5/5 on desktop-chrome + 5/5 on mobile-chrome = 10/10**. Test 4 (sub-tab navigation) uses `page.goto()` instead of `getByRole('tab')` because the `<Tabs>` primitive isn't mounted at hub level yet — see deviation note + follow-up in `## Notes / discoveries`. vitest 829/829 unchanged. Sweep checklist surfaced in `## Notes / discoveries`. Tag at sign-off → `v3.7`. **Closes the Fixtures sub-phase.**

**Then (after Fixtures sub-phase signs off, separate loop run):**

- **Predictions hub plan grooming** — Draft per-slice plans for P4a (This Week), P4b (Backtest), P4c (Log), P4d (Tools — Kelly + Value), P4e (Export text), P4f (Export binary), P4-cp (checkpoint). Ralph self-grooms these mid-loop, same pattern as Phase 2 grooming. Plan-only commits; untagged.
- **(open P3-followup)** Mount `<Tabs>` at the Fixtures hub (and analogously at Predictions, Insights, Settings hubs as those land). The Tabs primitive currently lives only in `Tabs.test.ts`; the cleanest path is a thin per-hub shell component (e.g. `FixturesHub.svelte`) that renders `<Tabs>` + inner `<Router>`/`<Route>` switch, mounted at `/fixtures/*`. Touching this earlier than P4 would also tighten `checkpoint-p3-fixtures.spec.ts` Test 4 to use the actual `getByRole('tab')` query the plan originally prescribed.
- **P4a–P4-cp** — 7 slices, then sub-phase sign-off.

**Then (sub-phase by sub-phase): Oracle Chat (P5a) → Insights (P7a–P7-cp) → Settings (P8a).** Each gets its own grooming + slice run. Phase 3 closes when all 17 slices are signed off and tagged.

**Halt condition for this loop run:** ralph stops at the Fixtures-sub-phase manual sweep gate after `p3-fixtures-cp` lands (or earlier if a slice fails). With ~3-4 iterations per slice + grooming, expect 12–16 iterations consumed for the Fixtures sub-phase (well within the 20-iteration budget). When the loop halts, sweep `/fixtures/live`, `/fixtures/matches`, `/fixtures/standings` end-to-end — checklist will be surfaced in `## Notes / discoveries` by the iteration that lands `p3-fixtures-cp`.

If anything stalls or behaviour looks wrong mid-loop, drop a one-line note under `## Human notes for next iteration` and the next ralph iteration will address it as part of its slice contract.

Plans live in `docs/superpowers/plans/2026-04-26-phase-1-matchcard/` (Phase 1, archived), `docs/superpowers/plans/2026-04-26-phase-2-today/` (Phase 2, archived), and `docs/superpowers/plans/2026-04-26-phase-3-per-hub/` (Phase 3, active — Fixtures sub-phase plan-ready; Predictions/Oracle/Insights/Settings drafted as the loop reaches each).
