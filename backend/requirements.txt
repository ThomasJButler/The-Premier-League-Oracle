# Python 3.11 (matches Dockerfile and CI) — updated March 2026

# Core ML
xgboost==2.1.3
scikit-learn==1.6.1
joblib==1.4.2

# Data Processing
pandas==2.2.3
numpy==2.1.3

# API & Web
fastapi==0.115.5
uvicorn==0.32.1
pydantic==2.9.2
requests==2.32.3
websockets==13.1

# Cache (optional — server starts without Redis)
redis==5.2.0

# Testing
pytest==8.3.3
pytest-cov==5.0.0
pytest-asyncio==0.24.0
httpx==0.27.2

# ---------------------------------------------------------------------------
# Pro-tier / optional — not required for free-tier runtime.
# Install separately when needed: pip install -r requirements-pro.txt
# ---------------------------------------------------------------------------
# shap==0.46.0           # SHAP explanations (XGBoost model, Pro-tier only)
# scipy==1.14.1          # Indirect dep of sklearn — installed automatically
# mlflow==2.17.2         # Experiment tracking (Pro-tier training)
# optuna==4.0.0          # Hyperparameter tuning (Pro-tier training)
# langchain==0.3.7       # AI agent (modern_oracle.py, Pro-tier)
# langchain-openai==0.2.9
# langchain-community    # LangChain tools (modern_oracle.py, Pro-tier)
# chromadb==0.5.20       # Vector store (modern_oracle.py, Pro-tier)
#
# Dead security modules (auth.py, secrets.py, validators.py) — not imported
# by main.py at runtime. Dependencies removed:
#   python-jose, passlib, cryptography, python-dotenv,
#   boto3, hvac, azure-keyvault-secrets, azure-identity, sqlalchemy, bcrypt
