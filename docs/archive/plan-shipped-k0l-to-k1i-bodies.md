- [x] **K0l-α — Settings shell + sub-tab routing + sections** *(Manual-gated, signed off 2026-05-12 → `k0.12` covers α+β)* — `frontend/src/routes/settings/+page.svelte` per `kicker-settings.html`. Wraps KickerShell (desktop, `active="settings"`) + MobileHeader/MobileNav (mobile, `active="more"`). Sub-tabs via URL hash: `pundit / api / display / account / notifications / privacy`; left rail buttons drive `activeTab` state and `history.replaceState`; `hashchange` listener syncs back/forward navigation; SSR default `pundit` (no hash visible at render). Pundit tab renders the canonical 10×`PunditPickerCard` grid wired straight to `personaStore.set` (live switch, no save button — onclick = mutation). API/Display/Account/Notifications/Privacy panels render their `Rule` headers + descriptive copy; API inputs render disabled (persistence shipped in β). **10 tests** (`src/routes/settings/page.test.ts`) covering: desktop+mobile shells, settings active in KickerShell, `more` active in MobileNav, all 6 sub-nav rows by `data-subnav-id`, default-active `pundit`, 10 picker cards rendered, default-selected card matches `voice`, no other panels render initially, no betting copy. **Manual sweep checklist:** `docs/the-kicker-spec/sweep-k0l.md`.
- [x] **K0l-β — Settings persistence** *(Manual-gated, MVP gate; signed off 2026-05-12 → `k0.12`)* — Shipped: real `<input type="password">` for both Football-Data + Anthropic keys with `bind:value`, SSR-guarded hydration in `onMount`. Explicit `Save & reload` + `Remove` buttons per key (commit `bed1d4a`) — Save trims + writes to localStorage then `window.location.reload()` so the salvaged constructor-once services pick up new key; Remove clears bound state + localStorage + reloads. Both buttons disabled when input empty. Markers `data-action="save-football-data"` / `"remove-football-data"` / `"save-anthropic"` / `"remove-anthropic"`. `PrefToggle` primitive at `lib/components/settings/PrefToggle.svelte` (role="switch", `data-pref-toggle` / `data-pref-checked` / `data-pref-switch` markers). Notifications panel renders 3 toggles (match-start / model-edge / broadsheet-ready) persisted as JSON at `kicker:notifications`. **Discovery:** `/api/chat` does NOT yet read the BYOK Anthropic key — `getAnthropic()` is env-only. Wiring this is K1d/K2c. **Validated:** svelte-check 0/0, 508 tests across 48 files.

### Phase K0-cp — K1.0 cutover sweep (manual)

- [x] **K0-cp — K1.0 cutover sweep** *(Manual-gated; signed off 2026-05-12 → `k1.0` at `b2d29b2`)* — **Cutover semantics revised:** production `main` swap deferred indefinitely per user instruction ("I won't merge into main until we are mostly done with the whole Kicker project"). Instead, `Kicker-Development` branch was created at the post-sweep HEAD and tagged `k1.0`; Vercel auto-deploys it as a preview. `main` remains on v3 Oracle. Shipped: `frontend/e2e/checkpoint-k1-0.spec.ts` (5 sub-tests × 2 projects = 10 cases, all green) + `frontend/playwright.config.ts` extended with `desktop-chrome` + `mobile-chrome` + `baseURL` + `reuseExistingServer` + `docs/the-kicker-spec/sweep-k1-0.md` (54-item checklist walked top-to-bottom by user). One follow-up surfaced: see `K1j — Predict GW button` in Open follow-ups.

### Phase K1 — Post-MVP screens

- [x] **K1a — Oracle (chat home)** *(Manual-gated, split into α/β; sweep signed off 2026-05-24 — ready for `k1.1` tag at `18c7f9d`)* → `k1.1`
  - [x] **K1a-α — Chat primitives + route shell** *(Manual-gated, ship pending sweep)* — `frontend/src/routes/oracle/+page.svelte` mounts KickerShell (desktop, `active="oracle"`) + MobileHeader/MobileNav (mobile, `active="oracle"`). Renders Rule header ("In conversation with {persona.name}") + a seeded 2-turn static thread (1 `UserMessage` + 1 `GeoffMessage` with marginalia) + pinned `GeoffComposer`. Chat primitives shipped at `lib/components/chat/{GeoffMessage,UserMessage,GeoffComposer}.svelte` — all SSR-safe, no jsdom required. `GeoffMessage` avatar+prefix driven by `var(--persona-accent)` cascading from `<html data-persona>` (no inline hex). `GeoffComposer` ships 4 default prompt chips — "Where's the value?" deliberately omitted per betting-removal rule; chips bind into a local draft state but `onsubmit`/`onchip` callbacks are exposed for β to wire. **10 tests** (`src/routes/oracle/page.test.ts`): SSR render, both shells mount, oracle active in KickerShell + MobileNav, GeoffMessage×2 + UserMessage×2 + Composer×2, all 8 prompt chips present, no betting copy, GeoffMessage style block uses CSS-variable not inline hex. **Validated:** svelte-check 0/0; 518/518 vitest green (was 508).
  - [x] **K1a-β.1 — Threads store** *(Auto-gated, ship pending sweep)* — `frontend/src/lib/stores/threads.ts` adapts v3 `oracle/threads.ts` to Kicker's Svelte-store idiom (single object exposing `subscribe` + mutation methods). localStorage key `kicker:threads`. Exports `OracleRole`, `OracleMessage` (richer than wire format — carries `timestamp` + optional `streaming` flag), `OracleThread`, `OracleThreadState`, and constants `STORAGE_KEY` / `MAX_TITLE_LENGTH` (40) / `MAX_THREADS` (20) / `MAX_MESSAGES_PER_THREAD` (50). Methods: `createThread / setActive / deleteThread / appendMessage / appendDelta / finalizeStreaming / clearAll`. Plus `deriveTitle()` (used when title is still the `'New thread'` sentinel) and `toApiMessages()` (strips timestamps + empty-content messages for the wire). Persistence via `inner.subscribe(persist)` — every mutation auto-syncs; corrupt JSON + orphan `activeThreadId` both fall back to empty state on load. **15 tests** (`src/lib/stores/threadsStore.test.ts`): SSR (no localStorage, mutate-without-throw), browser create/persist/derive-title/truncate, streaming delta concatenation, finalize removes flag, setActive ignores unknown, delete reassigns active to latest remaining, delete-last → null active, round-trip across re-imports, corrupt-JSON recovery, orphan-active drop, `toApiMessages` strips timestamps + empty messages. **Validated:** svelte-check 0/0, 533/533 vitest green (was 518).
  - [x] **K1a-β.2 — Composer → `/api/chat` SSE wiring** *(Manual-gated, ship pending sweep)* — Shipped: `frontend/src/lib/oracle/streamChat.ts` encapsulates the POST + SSE-parse + threadsStore dispatch (parameterised on `fetch` for unit-testability). `/oracle/+page.svelte` now derives `messages` from the active thread, drops the static SEED, and renders a `data-oracle-empty` italic hint when no thread exists. `GeoffComposer`'s `onsubmit` calls `handleSubmit(text)` which: creates a thread if `activeThreadId` is null, appends a user `OracleMessage`, appends an assistant placeholder with `streaming: true`, then calls `streamChat({personaId, messages: toApiMessages(...), threadId}, assistantIndex)`. Error UX is inline-in-thread per Human notes: HTTP errors (`{"error": "rate_limited"}` etc.) → `⚠ rate_limited`; non-JSON HTTP errors → `⚠ http_502`; mid-stream `{"error":…}` events → `⚠ {code}` (and stop); fetch rejection / decode failure → `⚠ {message}`. All errors land in the same placeholder so they persist with the conversation history. SSE parser handles tokens split across chunk boundaries (`\n\n` event delimiter, buffered remainder). **8 new tests** at `src/lib/oracle/streamChat.test.ts` covering: happy-path delta concatenation + finalisation, chunk-boundary splits, POST payload shape (`/api/chat`, JSON content-type, `{personaId, messages, threadId}`), HTTP error JSON → `⚠ rate_limited`, HTTP error non-JSON → `⚠ http_502`, mid-stream error event, network rejection, unparsable event ignored-but-finalised. **9 tests** (was 10) in `src/routes/oracle/page.test.ts` — SEED-based GeoffMessage/UserMessage assertions replaced with `data-oracle-empty` (2 per shell) + negative assertions for `data-geoff-message` / `data-user-message`. **Validated:** svelte-check 0/0; 540/540 vitest green (was 533).
  - [x] **K1a-β.3 — Inline `[[FIXTURE:…]]` + `[[CHEERS:…]]` token parsing** *(Manual-gated, ship pending sweep)* — Shipped: `lib/oracle/parseInlineTokens.ts` (pure + sync) emits `InlineSegment[]` of `{kind: 'text' | 'fixture' | 'cheers'}`. Single regex `/\[\[(FIXTURE|CHEERS):([^\]]+)\]\]/g` drives a `matchAll` walk; malformed tokens (FIXTURE without a dash, CHEERS with <3 pipe-parts) round-trip as verbatim text so the user always sees the source. `lib/oracle/fixtureLookup.ts` resolves `[[FIXTURE:HOME-AWAY]]` via `dataService.getMatches({upcoming: true, days: 14})` + abbr-matched lookup, with a module-level `Map<key, Promise<Fixture|null>>` cache so duplicate chips share a single roundtrip; `_resetFixtureLookupCache()` exported for tests. `lib/components/chat/FixtureChip.svelte` SSRs a static `HOME v AWAY` pair chip + `onMount`-only hydration (typeof-window guarded) that decorates with kickoff time or `· no upcoming match`. `GeoffMessage.svelte` body `<p>` swapped to `<div>` (so the inline `<aside>` CHEERS callouts are valid HTML), now iterates `parseInlineTokens(body)` and renders text/fixture/cheers segments — plain-string messages still get `whitespace-pre-line` via a `data-geoff-text` span so the visual is unchanged. **11 new tests:** 8 in `src/lib/oracle/parseInlineTokens.test.ts` (empty input, plain text, single FIXTURE, single CHEERS, ordered mixed stream, malformed FIXTURE, malformed CHEERS, extra `|` inside CHEERS body), 3 in `src/lib/components/chat/GeoffMessage.test.ts` (plain text → no callouts/chips, CHEERS render with parsed props + `data-cheers-callout`, FIXTURE render with `data-fixture-home`/`-away` attrs). **Validated:** svelte-check 0/0; 551/551 vitest green (was 540).
  - [x] **K1a-β.4 — Left rail (threads + watchlist) + right rail (today's fixtures)** *(Manual-gated, ship pending sweep)* — Shipped: `lib/components/oracle/{ThreadListRail,WatchlistRail,TodayFixtureRail}.svelte`. Desktop `/oracle` body is now a 3-column grid (`240px minmax(0,1fr) 320px`) rendered only inside the desktop KickerShell — mobile shell still mounts the chat-only body unchanged. `ThreadListRail` subscribes to `threadsStore`, sorts by `updatedAt` desc, renders title + last-message preview per thread, with `+ New` button (`threadsStore.createThread()`), per-row click → `setActive`, per-row `×` delete button (sibling of the select button to avoid nested `<button>` invalid HTML), and active-row left border driven by `var(--persona-accent)`. Empty state ships an inline hint. `WatchlistRail` is a placeholder shape ("Soon" badge + copy pointing at K1f). `TodayFixtureRail` SSRs an empty loading hint, then `onMount` calls `dataService.getMatches({upcoming: true, days: 3})` (SSR-guarded by the import contract) and stacks up to 4 `MatchSheetCard`s; falls back to "No upcoming fixtures in the next 72 hours" if the slate is empty or the API key is missing. **7 new tests:** `ThreadListRail.test.ts` (3 — header+new button, empty-state, no betting copy), `WatchlistRail.test.ts` (1 — marker + placeholder badge), `TodayFixtureRail.test.ts` (2 — SSR loading state + no betting copy), `oracle/page.test.ts` (+1 — all three rails appear exactly once, desktop-only). **Validated:** svelte-check 0/0; 558/558 vitest green (was 551).
- [ ] **K1b — Match detail + MatchHero** *(Manual-gated, split into α/β/γ)* → `k1.2` — `/fixtures/[id]/+page.svelte`. Builds `MatchHero` (`kind: 'preview' | 'live'`), section tabs (analysis / probabilities / form / h2h / venue), `EnsembleBars` (5-row grid for ELO/POISSON/FORM/H2H/XGBOOST — predictions moat per-fixture), `ScorelineBars`, `PunditQuoteBlock`. **MACCA RECOMMENDS rail dropped per R0+ delta**. Per-fixture `form`/`formA` arrays required — patch `matchToFixture` adapter. ~14 tests total across sub-slices.
  - [x] **K1b-α — Route shell + MatchHero primitive** *(Manual-gated, ship pending sweep)* — `frontend/src/routes/fixtures/[id]/+page.ts` (3-line `load` that threads `params.id` into `data.id`) + `frontend/src/routes/fixtures/[id]/+page.svelte` (mounts KickerShell `active="fixtures"` + MobileHeader/MobileNav, resolves match via `dataService.getCurrentSeasonMatches()` + `matchToFixture()` adapter in `onMount`, renders `<MatchHero>` when found, a 404 block when load completes with no match, and an italic loader during the fetch). `MatchHero.svelte` ships both `kind: 'preview'` (default) + `kind: 'live'` variants: meta strip (status pill in red / competition+GW / kickoff in `Europe/London`), 3-col team grid with 56px serif team names + abbr footers, centre block that swaps between a `v` glyph (no score), live/final score block (`H · A`), and an italic venue rule. Status label derives from fixture state: `KICK-OFF` for SCHEDULED, `LIVE · {n}'` when minute present, `FULL TIME` for FINISHED, plus POSTPONED/CANCELLED passthroughs. No inline hex — colours via existing `text-ink`/`text-red`/`text-ink-dim` tokens. **15 new tests:** 9 in `MatchHero.test.ts` (default kind=preview marker, team names+abbrs, scheduled KICK-OFF + versus glyph + no score block, venue rendered, venue omitted when absent, kickoff date+time in BST locale, LIVE+minute+score block, FINISHED → FULL TIME, no betting copy) + 6 in `routes/fixtures/[id]/page.test.ts` (renders given data prop, both shells mount, fixtures-active in both shells with `aria-current="page"` on KickerShell anchor, SSR shows two `data-match-detail-loading` markers (one per shell), no `data-match-detail-missing` during loading, no betting copy). **Validated:** svelte-check 0/0; 573/573 vitest green (was 558). **Manual sweep checklist for α:** (1) Visit `/fixtures` → click any MatchRow → confirm `/fixtures/{id}` resolves to the hero (rather than the previous 404). (2) Confirm meta strip shows `KICK-OFF · Premier League · GW {n} · {day, time}` in `Europe/London`. (3) For an in-progress fixture (or temporarily edit one in devtools to `status: 'LIVE', minute: 67`) confirm the hero flips: red `LIVE · 67'` pill + score block + `LIVE SCORE` label. (4) For a finished fixture, confirm `FULL TIME` + `FINAL SCORE` + actual score. (5) Hit `/fixtures/does-not-exist` directly → confirm the 404 block with the back-link renders after the loader. (6) Resize to mobile → confirm the hero compresses cleanly inside the mobile shell and the bottom-nav still marks `fixtures` active. **Discoveries:**
    - **No `getMatchById` on dataService.** The route fetches the full current-season slate and `.find()`s the id client-side. Acceptable because the data is already cached (3-tier IndexedDB), but if `/fixtures/[id]` ever becomes deep-linkable (shared URLs from outside the app), a dedicated `dataService.getMatchById(id)` with a targeted API call would be cheaper than a full slate load. Park until K1b-γ or K1f search.
    - **`+page.ts` `load` over `$app/stores`.** Threading params through a `load` function + `data` prop sidesteps the need to mock `$app/stores` in svelte/server tests. Codify for all future dynamic routes (`/column/[slug]`, `/insights/archive/[season]`, etc.) — keep `$app/stores` reserved for routes that genuinely need reactive store access.
    - **Hero scope deferred to β/γ.** No EnsembleBars / ScorelineBars / section tabs in α; the route ships a `data-match-detail-stub` "More to follow" rule under the hero so the page reads as intentionally incomplete rather than broken. β wires the per-fixture predictions pipeline (`optimizedPredictions` → MatchPrediction → bars); γ adds the section tabs + PunditQuoteBlock.
    - **`MatchSheetCard` ≠ `MatchHero`.** Tempting to reuse MatchSheetCard, but the hero needs 56px team names + a score-or-versus swap, while MatchSheetCard is a 28px sheet with prob-bar baked in. They share the meta-strip + 3-col-grid grammar but diverge on hierarchy — keeping them as sibling primitives matches the handoff's "card vs hero" split.
  - [x] **K1b-β — `matchToFixture` form arrays + EnsembleBars + ScorelineBars** *(Manual-gated, ship pending sweep)* — Shipped: `lib/adapters/v3.ts` adds `deriveFormLast5(matches, teamName, beforeDate)` helper (newest-first W/D/L, cutoff strict, completed-only) + `matchToFixture(m, history?)` second arg (backwards-compat — omits `formLast5` when history not passed) + `enhancedPredictionToV3(EnhancedPredictionModel)` mapper that translates the salvaged engine's raw `modelOutputs` triples into the v3 `MatchPrediction.models[]` shape (4 core rows: ELO/POISSON/FORM/H2H derived via argmax→lean+confidence; XGBOOST row appended only when `modelWeights.ml > 0`, tagged with the ensemble pick + final confidence since `modelOutputs` doesn't carry the ML triple), reconstructs the ensemble triple by re-applying `modelWeights`, and parses `topScorelines: Array<{score: "H-A", probability}>` into `{home, away, prob}`. New primitives `lib/components/match/{EnsembleBars,ScorelineBars}.svelte`: EnsembleBars is a 4-col grid (model name / lean abbr or "DRAW" / horizontal bar driven by `var(--persona-accent, var(--ink))` width=pct / right-aligned pct) per model row; ScorelineBars sorts grid cells and renders top-N (default 7) as score / bar (width *relative to top scoreline* so 7-12% spread isn't crushed) / absolute pct, with an italic empty-state when no scorelines. `routes/fixtures/[id]/+page.svelte` enriched: passes the slate as history to `matchToFixture`, calls `OptimizedPredictor.predictMatch(home, away, history, referee, date)` post-fetch, threads the result through `enhancedPredictionToV3` and renders `<EnsembleBars>` + `<ScorelineBars>` in a `lg:grid-cols-2` block below the hero. Render falls back to a "Model warming up" rule when the prediction returns undefined. **27 new tests:** 8 in `v3.test.ts` for `deriveFormLast5` (newest-first, cutoff strict, null-result skip, 5-cap, unknown-team) + `matchToFixture` history (populates formLast5, backwards-compat omit); 6 in `v3.test.ts` for `enhancedPredictionToV3` (no-modelOutputs returns undefined, 4-core models with lean+confidence, XGBOOST appended only with `modelWeights.ml`, topScorelines parsed from "H-A" strings, ensemble triple sums to 1, predictedResult→pick mapping); 7 in `EnsembleBars.test.ts` (marker, row count via lookahead, lean→abbr/DRAW mapping, percent labels, CSS-var not hex, XGBOOST row, no betting copy); 7 in `ScorelineBars.test.ts` (marker, capped row count, score data-attr, percent labels, empty-state, no betting copy, abbrs in header). **Validated:** svelte-check 0/0; 600/600 vitest green (was 573).
  - [x] **K1b-γ — Section tabs + PunditQuoteBlock wiring** *(Manual-gated, ship pending sweep)* — Shipped: `lib/components/match/SectionTabs.svelte` (Svelte 5 generic `<script lang="ts" generics="Id extends string">`, renders a horizontal tab bar driven by `data-section-tab` markers + `aria-current="page"` + an `is-active` class on the active tab, bottom border colour from `var(--persona-accent, var(--red))`); `lib/components/match/FormChips.svelte` (per-team W/D/L chip row with persona-accent W-fill, falls back to "No completed matches yet this season." when `form` is undefined or empty); `lib/match/h2h.ts` (`recentH2H(matches, teamA, teamB, beforeDate, limit=5)` — framework-free pure helper returning newest-first prior meetings between the pair with `winner` reported relative to `teamA`, strictly before cutoff, completed-only). `/fixtures/[id]/+page.svelte` now mounts `<SectionTabs>` under the hero with 5 tabs (`analysis | probabilities | form | h2h | venue`); URL-hash routing identical to `/settings` (SSR default `analysis`, `onMount` syncs from `window.location.hash`, `setTab` writes `history.replaceState`, `hashchange` listener handles back/forward). Tab bodies: **analysis** = `PunditQuoteBlock` attribution = active persona name, body = `prediction.analyseText` || `keyFactors.join(' ')` || sentinel fallback ("The model has enough to call it, but not enough to shout about it."); **probabilities** = the existing `EnsembleBars` + `ScorelineBars` 2-col grid (moved from the bare hero footer into the tab); **form** = two `<FormChips>` (home + away) in a `sm:grid-cols-2` grid + italic legend; **h2h** = `data-h2h-list` of up to 5 prior meetings rendered as `date · home v away · score`, empty-state when none; **venue** = `fixture.venue` line + italic K1d-soon note. The legacy `data-match-detail-stub` "More to follow" block is removed (superseded by tabs); the `data-match-detail-prediction` block now lives inside the probabilities panel rather than the bare hero footer. **8 new tests:** 3 in `SectionTabs.test.ts` (all tabs render with `data-section-tab=`, only active tab carries `aria-current="page"` + `is-active`, no betting copy), 2 in `FormChips.test.ts` (ordered chip render with `data-form-chip=`, empty-state fallback), 3 in `h2h.test.ts` (newest-first + limit cap, winner pivots relative to `teamA`, skips unfinished + respects strict cutoff). **Validated:** svelte-check 0/0; 608/608 vitest green (was 600). **Manual sweep checklist for γ:** (1) Visit any `/fixtures/{id}` → confirm the `<SectionTabs>` row appears under the hero with 5 tabs (Analysis · Probabilities · Form · H2H · Venue) and `Analysis` is active by default. (2) Click each tab → confirm panel swap is immediate, URL hash updates to `#analysis` / `#probabilities` / etc., and the tab's bottom border picks up the persona accent. (3) Hard-reload on e.g. `/fixtures/{id}#h2h` → confirm the H2H panel hydrates as active. (4) Hit back/forward across tabs → confirm `hashchange` syncs the active tab without a full page reload. (5) Analysis tab: confirm the `PunditQuoteBlock` shows the active persona's name as attribution + the model's key-factor line; switch persona via Settings → revisit → confirm attribution updates. (6) Probabilities tab: confirm the same two bar panels from β still render correctly. (7) Form tab: confirm both team abbrs render with their last-5 W/D/L chips (W chips fill in `var(--persona-accent)`); pick a freshly-promoted side and confirm the empty-state copy appears instead of a chip row. (8) H2H tab: pick a long-standing rivalry (e.g. Arsenal vs Liverpool) and confirm the list shows 3–5 prior meetings newest-first with the correct score; pick a freshly-promoted side's match and confirm the "No prior meetings on file in the current slate." empty-state appears. (9) Venue tab: confirm venue name appears for fixtures that ship one; otherwise the "Venue not provided by the feed" hint. (10) Resize to mobile → confirm tab bar overflow-scrolls horizontally without breaking layout, and a hash-deep-linked tab still works on mobile. (11) No betting copy anywhere on any tab. **Discoveries:**
    - **`/api/broadsheet` per-fixture cache deferred.** The plan body called for analysis = `PunditQuoteBlock` reading from `/api/broadsheet` per-fixture cache, but `/api/broadsheet` doesn't yet accept a fixture context — it generates a whole-gameweek broadsheet. Wiring per-fixture LLM analysis would need (a) extending the `/api/broadsheet` payload contract to accept `{fixtureId, prediction, fixture}` and return a single short analysis paragraph, or (b) introducing a new `/api/analysis` endpoint, plus a localStorage cache keyed `kicker:analysis:{fixtureId}:{personaId}`. Both are real K1e territory. For γ the analysis panel uses the engine's `keyFactors` joined into one line — the persona's voice still attributes the quote, but the prose is the model's, not the persona's. **Recommend `K1b-γ.1 — Live persona-voiced match analysis`** as a small follow-up slice (~50 LoC + 4 tests) once K1e ships and we have a baseline broadsheet endpoint to extend.
    - **H2H limited to the current slate.** `dataService.getCurrentSeasonMatches()` returns only the current season's fixtures, so the H2H tab can show at most one prior meeting per pair (the reverse fixture if it's already been played). To surface historical meetings (e.g. last 5 Arsenal-vs-Liverpool across all seasons), the route would need to call `dataService.getHistoricalMatches()` (or equivalent) and concatenate. Park until K1d (Archive) lands, since the cache + warm-up cost should be shared with that screen.
    - **Tab order matters for the persona signature.** `analysis` is intentionally tab 1 — the persona's voice is the front-of-paper before the model rows. Reordering to put probabilities first would invert the editorial hierarchy. Codify in the screens-inventory doc once the user signs off on γ.
    - **`SectionTabs` is reusable.** The component is generic over `Id extends string` so `/settings`, `/insights`, `/oracle` (when it grows a thread filter rail), etc. can adopt the same primitive instead of re-implementing the hash-routed tab pattern. Worth a follow-up to migrate `/settings`'s bespoke sub-nav onto `SectionTabs` once γ is signed off — same UX, fewer custom CSS classes.
- [x] **K1c — Live + scoreboard** *(Manual-gated, ship pending sweep)* → `k1.3` — Shipped: `frontend/src/routes/fixtures/[id]/live/+page.ts` (3-line `load` threading `params.id`) + `frontend/src/routes/fixtures/[id]/live/+page.svelte` mounts both shells `active="fixtures"`, resolves the match via `dataService.getCurrentSeasonMatches()` + `matchToFixture()`, forces a LIVE status (`status: 'LIVE'`, minute fallback `67`, score fallback `2-1`) so the scoreboard always has data while the real feed is mocked, and renders `<LiveScoreboard>` over a 2-col `lg:grid-cols-[1fr_minmax(0,360px)]` layout of `Match events` (left) + `On the wire` commentary (right). `lib/fixtures/liveFeed.ts` ships the mocked feed surface: `LiveEventType` union (`GOAL / OWN_GOAL / PENALTY / YELLOW / RED / SUB / VAR`), `LiveEvent` / `LiveCommentaryLine` / `LiveFeed` types, `eventGlyph(type)` map, + `getLiveFeed(fixtureId, homeAbbr?, awayAbbr?)` deterministic generator (FNV-1a-ish hash seeds minor minute offsets so the same id always renders the same stream — no fakes needed in tests). 5 events sorted ascending by minute; 3 commentary lines sorted newest-first. `lib/components/match/LiveScoreboard.svelte` is a distinct primitive from MatchHero (40px team names, 64px score, no venue/kickoff strip, pulsing red dot in the LIVE pill — `kicker-live-pulse` 1.4s ease-in-out infinite, gated by `@media (prefers-reduced-motion: reduce) { animation: none; }`). `lib/components/match/MatchEventRow.svelte` renders a 4-col grid `[44px_28px_56px_1fr]` with `data-event-row` / `data-event-type` / `data-event-side` markers + minute / glyph / team-abbr / `<strong>player</strong> — detail`. `lib/components/match/LiveCommentaryItem.svelte` renders a 44px-wide red mono minute + serif body, with a `kicker-commentary-fade` 280ms ease-out animation also gated by `prefers-reduced-motion`. **16 new tests:** 4 in `liveFeed.test.ts` (determinism via double-call equality, minutes monotonic, abbr threading, glyph coverage for every event type), 4 in `LiveScoreboard.test.ts` (marker + score + minute + abbrs, pulse dot present, reduced-motion CSS rule, no betting copy), 2 in `MatchEventRow.test.ts` (full row markers + content, omit-detail variant), 2 in `LiveCommentaryItem.test.ts` (minute + body render, reduced-motion CSS rule), 4 in `routes/fixtures/[id]/live/page.test.ts` (both shells mount, fixtures active in both with `aria-current="page"` on KickerShell anchor, SSR loader appears twice, no betting copy). **Validated:** `npm run check --prefix frontend` 0/0; `npm run test --prefix frontend -- --run` → 624/624 vitest green (was 608) across 68 files (was 63). **Discovery — route forces LIVE status:** because there's no real live feed and the demo fixture from `dataService` is almost always SCHEDULED/FINISHED, the route synthesises `{status: 'LIVE', minute: 67, score: {2,1}}` so the scoreboard always lights up. When a real feed lands (K2 or later), drop the `asLiveFixture()` synth and pull `minute` / `score` from the feed instead. **Discovery — `MatchEventRow` is sided but doesn't yet route layout off side:** the `data-event-side` marker is in place for future "home column / away column" pivots but the current single-column event list ignores it. Fine for MVP. **Manual sweep checklist for K1c:** (1) Visit any `/fixtures/{id}/live` directly → confirm the scoreboard lights up red with a pulsing dot, `LIVE · {minute}'` pill, and a 2-1 score (or whatever the synth produced). (2) Open OS-level reduced-motion (`System Settings → Accessibility → Display → Reduce motion` on macOS) → reload → confirm the dot stops pulsing AND commentary items stop fading in. (3) Confirm the match events list shows 5 rows ordered by minute ascending, with a glyph (⚽ / 🟨 / ⇄) + team abbr + player + dash + detail per row. (4) Confirm commentary list shows 3 rows ordered newest-first with red mono minute prefix. (5) Switch persona via Settings → revisit → confirm scoreboard doesn't mis-render (persona accent is irrelevant on this route — it's red-driven by design). (6) Hit `/fixtures/does-not-exist/live` → confirm the `data-live-missing` 404 block lands after the loader. (7) Resize to mobile → confirm the 2-col layout collapses to stacked sections and the bottom-nav still marks `fixtures` active. (8) No betting copy anywhere on the route. **Decision deferred:** entry point — currently `/fixtures/{id}/live` is reachable only by typing the URL. Adding a `[LIVE →]` chip to the MatchHero meta strip when a fixture status flips to LIVE (or a "View live" link from `/fixtures` row when the row's status === 'LIVE') is a tiny K1c.1 follow-up (~10 LoC + 2 tests). Park until sweep confirms the scoreboard reads correctly.
- [ ] **K1d — Insights + Archive read-only** *(Manual-gated, split into α/β)* → `k1.4` — `/insights/+page.svelte` + `/insights/archive/+page.svelte`. Builds `TopScorerRow` + `StatTile` + `SeasonAnomalyRow` (α) and `SeasonRow` + `SeasonDetail` (β). β's persona-voiced verdict reads pre-generated copy from `/api/broadsheet` cache (no live LLM per archive view).
  - [x] **K1d-α — Insights main route** *(Manual-gated, ship pending sweep)* — `frontend/src/routes/insights/+page.svelte` mounts both shells (desktop `active="insights"`, mobile `active="more"` — `insights` nests under More since MobileNav's 5-tab order is locked). Three sections under Rule headers: (1) **Top scorers** — `dataService.getTopScorers(10)` in `onMount`, SSR shows `data-scorers-loading`; client populates to a list of `<TopScorerRow>` (rank/player/team/goals/assists) or falls back to `data-scorers-empty` when the API call rejects or returns nothing (per dataService's "essential queries throw" contract). (2) **33-season league baseline** — 6-tile `<StatTile>` grid driven synchronously from the bundled `statsPack.leagueEra` + `statsPack.totalSeasons / totalMatches` (goals per match, home goals, away goals, over 2.5, BTTS, seasons-on-file). No network, no `onMount`. (3) **Season story** — `<SeasonAnomalyRow>` per `statsPack.anomalies[]` entry (3 outlier seasons in the current pack — 2018/19, 2020/21, 2023/24). Archive teaser footer `data-insights-archive-hint` points at the K1d-β follow-up. **15 new tests:** 2 in `TopScorerRow.test.ts` (markers + assists null/undefined em-dash), 2 in `StatTile.test.ts` (markers, optional sub line), 2 in `SeasonAnomalyRow.test.ts` (full row + reasons list, empty reasons), 9 in `routes/insights/page.test.ts` (SSR render, both shells, insights active in KickerShell + `more` active in MobileNav, SSR loading state appears twice, three section markers per shell, 6×2 stat tiles, 3×2 anomaly rows, no betting copy). **Validated:** svelte-check 0/0; 639/639 vitest green (was 624) across 72 files (was 68). **Manual sweep checklist for α:** (1) Visit `/insights` → confirm desktop nav highlights `Insights` and the Rule kicker reads `GOLDEN BOOT · Top scorers · LIVE FEED`. (2) With a real Football-Data API key in Settings → confirm the top-scorers list populates within a few seconds with 10 rows (rank zero-padded, player + team + goals + `+N` assists). (3) Wipe the Football-Data key in Settings → reload → confirm the empty-state hint appears instead of the loading line. (4) Stats grid: confirm `GOALS PER MATCH` reads `2.69` (or whatever `leagueEra.avgTotalGoalsOverall` rounds to), `SEASONS ON FILE` shows `33` + the locale-formatted match count, and the layout switches from 2-col (mobile) to 3-col (≥lg). (5) Season story: confirm exactly 3 anomaly rows render with red mono season labels (`2018/19`, `2020/21`, `2023/24`) and the original reason strings. (6) Resize to mobile → confirm mobile shell mounts the same three sections, MobileNav highlights `More` (not `Insights`), and the archive teaser footer still reads. (7) No betting copy anywhere. **Discoveries:**
    - **`insights` nests under MobileNav's `more` tab.** MobileNav's `NavId` is locked to 5 ids per K0d (`today/fixtures/predictions/oracle/more`). The desktop shell's nav grew an `insights` entry naturally, but mobile users will reach `/insights` via the `More` tab (currently routes to `/more` — which doesn't exist yet). When the `/more` route lands, it should expose `/insights` + `/settings` + `/roster` + `/notifications` as a list. Park as K1f-adjacent.
    - **`getTopScorers` throws on failure (per dataService contract).** The `try/catch` around the call is mandatory — without it the route would 500-out for any user without a Football-Data key. The empty-state copy points the user at Settings to add a key, which is the right call to-action.
    - **`statsPack` is bundled + synchronous.** No need for `onMount` on the stats grid — it renders fully during SSR. Codify this for any future K-slice surfacing historical aggregates: prefer `statsPack` over the live API for season-baseline numbers, since it's zero-runtime and immune to API outages.
    - **Anomaly count is currently 3.** Future stats-pack regenerations (the backend Python script) may shift this number; the test asserts the exact count to catch silent drift. If the count changes legitimately, the assertion needs a one-line update.
  - [x] **K1d-β — Archive + persona verdict** *(Manual-gated, ship pending sweep)* — Shipped: `frontend/src/routes/insights/archive/+page.svelte` mounts both shells (desktop `active="insights"`, mobile `active="more"`), renders a 33-row season rail (newest-first) of `<SeasonRow>` over a 2-col `lg:grid-cols-[minmax(260px,320px)_minmax(0,1fr)]` layout, plus a `<SeasonDetail>` panel for the active season. `$state` + `$derived` track the active season; selecting a row writes `?season=YYYY-YY` via `history.replaceState` (URL-shareable) and best-effort warms `dataService.getHistoricalMatches(seasonStartYear)` so K1b-γ's cross-season H2H tab gets cached data when the user pivots between seasons. SSR default = `2025/26` (most recent); `onMount` syncs from `?season=…` if present and valid. **Static data fixture** at `lib/fixtures/leagueHistory.ts` ships 33 typed `SeasonRecord` entries (champion team + points + GD, runner-up + points, `inProgress` flag for 2025/26). `lib/stores/verdictsStore.ts` ships SSR-safe `readVerdict / writeVerdict / clearVerdict` with key shape `kicker:verdict:{season}:{personaId}` — corrupt JSON + missing required fields both fall back to null on read; the body field round-trips alongside an ISO `generatedAt` timestamp. `SeasonDetail.svelte` renders champion + runner-up + a 4-tile `seasonStats` grid (matches / goals-per-match / home win rate / BTTS — all from `statsPack.seasonStats[season]`), surfaces the existing `isAnomalous` reasons inline when present, and the verdict block falls back to a "No verdict on file yet. {personaName} hasn't filed copy for this season" italic when no cache entry exists. In-progress 2025/26 renders an "In progress" placeholder for the trophy block. **`/insights` archive teaser** swapped from a dead "coming in K1d-β" hint to a real `<a href="/insights/archive">` red-underline link (`data-insights-archive-link`). **32 new tests across 5 new files:** 6 in `leagueHistory.test.ts` (33-season count, range bounds, 2025/26 in-progress flag, every non-in-progress season has champion+runnerUp, unknown-season returns undefined, `seasonStartYear` parsing); 6 in `verdictsStore.test.ts` (null when missing, round-trip body+timestamp, namespaces by both season AND persona, clear targets one entry, corrupt-JSON recovery, schema validation rejects partial entries); 4 in `SeasonRow.test.ts` (markers + points + GD signing, negative-GD signing, in-progress placeholder, active-row `aria-current` + `is-active` class); 7 in `SeasonDetail.test.ts` (header + champion + runner-up, 4 stat tiles, anomaly block when flagged, "No verdict on file" empty-state, verdict body when cached, in-progress fallback hides champion block, no betting copy); 9 in `routes/insights/archive/page.test.ts` (SSR render, both shells mount, `insights` active in KickerShell + `more` in MobileNav, 33×2 season rows, default-active 2025/26 with `aria-current="true"` × 2 shells, detail panel + verdict empty-state × 2 shells, archive footer hint references K1e, no betting copy). **Validated:** `npm run check --prefix frontend` 0/0; `npm run test --prefix frontend -- --run` → 671/671 vitest green (was 639) across 77 files (was 72). **Manual sweep checklist for β:** (1) Click `/insights` archive teaser link → confirm landing on `/insights/archive` with `2025/26` row highlighted (left border in `var(--persona-accent)` + `aria-current="true"`). (2) Click any older season → confirm URL updates to `/insights/archive?season=2003%2F04` (or similar) via replaceState, the rail's active border jumps to the new row, and the detail panel rerenders with that season's champion / runner-up / stats grid. (3) Hard-reload on `/insights/archive?season=2003/04` → confirm the 2003/04 row is the active one on first paint (`onMount` hydration). (4) Hit `/insights/archive?season=garbage` → confirm fallback to `2025/26` (invalid seasons silently ignored). (5) Open devtools → set a cached verdict: `localStorage.setItem('kicker:verdict:2003/04:voice', JSON.stringify({season:'2003/04', personaId:'voice', body:'The Invincibles refused to lose.', generatedAt: new Date().toISOString()}))` → revisit `/insights/archive?season=2003/04` → confirm the verdict block shows the cached body attributed to "Cheers, Geoff" instead of the empty-state hint. (6) Switch persona via Settings → revisit same season → confirm the empty-state hint returns (verdict is keyed per-persona, so a Macca verdict isn't on file). (7) Pick `2018/19` (anomalous per statsPack) → confirm the red-left-bordered OUTLIER SEASON block appears with the reasons list. (8) Pick `2025/26` → confirm the trophy block shows "No champion crowned yet" + "IN PROGRESS" pill. (9) Resize to mobile → confirm rail+detail collapse to a single column under the mobile header, MobileNav highlights `more`. (10) Open devtools network tab on row click → confirm a quiet `competitions/PL/matches?season=YYYY` call goes out (the silent dataService warm-up). (11) No betting copy anywhere. **Discoveries:**
    - **Top-6 final standings + per-club points/GD deliberately not bundled.** Hardcoding 33 seasons × 6 entries × {points, GD, GF, GA} is ~600 fields of historical research; the slice instead bundles only champion + runner-up data (compactly verifiable from public PL records) and uses `statsPack.seasonStats[season]` for rate context. Computing full top-6 from `dataService.getHistoricalMatches(year)` is feasible but expensive (38 weeks × 10 matches per season + match aggregation) and only the most-recent 5 seasons are pre-warmed; older seasons would require fresh API calls per click. Park as a follow-up `K1d-β.1 — Derive full top-6 standings on demand` (~80 LoC: match → standings reducer + UI block + 4 tests) once K1e ships per-season verdicts so the two cache populations can be wired together.
    - **Verdict cache write surface is not yet user-facing.** `writeVerdict` is exported and tested but no UI path calls it — the cache is read-only from the archive's perspective. The natural caller is K1e's extension of `/api/broadsheet` to accept `{kind: 'season-verdict', season, personaId}` and a small "Generate verdict" CTA on `SeasonDetail`. Documented in the slice body's archive footer copy ("live generation lands in K1e").
    - **`mobile-nav.active = 'more'`** mirrors K1d-α's `/insights` choice: `MobileNav`'s 5-tab order is locked at `today/fixtures/predictions/oracle/more`, so any sub-insights route nests under `more` for mobile and surfaces in the desktop sidebar as `insights` (KickerShell's nav includes it). When the `/more` index lands (K1f-adjacent), it should list `/insights`, `/insights/archive`, `/settings`, `/roster`, `/notifications`.
    - **Svelte 5 SSR attribute order is unstable across `class` / `aria-*` / `data-*`** — codifies the K0c "data-attr-prefix collision" rule for `aria-current` specifically. Test pattern: match a row's open-tag substring on the stable `data-*` selector, then assert `aria-current="true"` membership via `.toContain`. Avoid regexes that require both attributes in a specific order.
    - **Cross-season H2H warm-up is targeted, not eager.** A naïve warm of all 33 seasons would burn a free-tier Football-Data API key in a single page mount (33 × 1 call × 10s cooldown per request = quota exhausted in ~6 minutes). The slice instead warms only the *currently-active* season's matches on row click, which still gives K1b-γ's H2H tab cross-season data when the user pivots between same-era fixtures, without exhausting the quota. Codify: any future bulk-historical surface (e.g. K1f search across all seasons) should similarly load lazily, not eagerly.
- [ ] **K1e — Column + Broadsheet** *(Manual-gated, split into α/β/γ)* → `k1.5` — Both consume one-shot Sonnet plumbing (shipped K0f). `/column/[slug]/+page.svelte` builds `ColumnHero` (54–72px serif headline with inline `<em>` highlighted in `var(--persona-accent)`) + `PullQuote` (3px red top rule, oversized red leading quote glyph, italic body — collapses with `HotTake` via `variant?: 'amber-left' | 'red-top'`). `/broadsheet/+page.svelte` triggers `/api/broadsheet`, renders JSON template, caches in localStorage keyed `kicker:broadsheet:gw{N}:{personaId}`. ~12 tests.
  - [x] **K1e-α — Column route + static fixture + primitives** *(Manual-gated, ship pending sweep)* — `frontend/src/routes/column/[slug]/+page.ts` (3-line `load` threading `params.slug`) + `frontend/src/routes/column/[slug]/+page.svelte` mounts KickerShell (desktop, `active="today"` per spec — `/column/*` is a sub-route of Today) + MobileHeader/MobileNav (mobile, `active="today"`), resolves the column via `getColumn(slug)` from a static fixture, renders the full layout: eyebrow → `<ColumnHero>` → drop-cap intro (first char rendered as decorative `aria-hidden` span + sr-only repeat for AT) → `<PullQuote>` → 2-col body grid (`sm:grid-cols-2`) → optional `<CheersGeoffCallout>` foot. 404 block (`data-column-missing`) renders when slug doesn't resolve. **Static fixture** at `lib/fixtures/columns.ts` ships 3 sample columns (`state-of-arsenal` by voice, `mickey-on-united` by volcano, `macca-on-anfield` by scouser) — each with `eyebrow`, `headline: HeadlineSegment[]` (emphasis segments wrap in `<em>` styled via `var(--persona-accent)` — no `{@html}` needed), `byline {personaId, sub, readTimeMinutes, dateline}`, `dropCapIntro`, `pullQuote {body, attribution}`, `body: string[]` paragraphs, optional `cheers` for the Geoff stamp. `lib/components/column/ColumnHero.svelte` ships eyebrow + responsive serif headline (44/54/64px breakpoints) + 3-col byline strip (monogram circle in persona-accent / name + sub / read-time + dateline). `lib/components/column/PullQuote.svelte` ships 3px red top rule + oversized 72px red leading quote glyph (decorative, `aria-hidden`) + 22px italic serif body + uppercase attribution caption — visually distinct from `PunditQuoteBlock` (which uses a left-border + smaller 58px glyph). **23 new tests across 4 files:** 4 in `ColumnHero.test.ts` (markers, emphasis `<em>` wrapping, byline content + monogram first letter, no inline hex), 4 in `PullQuote.test.ts` (markers + body + attribution, italic serif at 22px, decorative U+201C glyph in red at 72px, 3px red CSS-var top rule via source-scan), 6 in `fixtures/columns.test.ts` (count ≥3, slug round-trip, unknown-slug undefined, every personaId is real, every column has headline/intro/body/pullQuote, no betting copy), 9 in `routes/column/[slug]/page.test.ts` (renders for known slug + slug attr, both shells mount, `today` active in KickerShell + MobileNav, 6 layout markers ×2 shells, drop-cap aria-hidden + sr-only repeat, 404 fallback for unknown slug, no betting copy, every fixture slug renders). **Validated:** `npm run check --prefix frontend` 0/0; `npm run test --prefix frontend -- --run` → 694/694 vitest green (was 671) across 81 files (was 77). **Manual sweep checklist for α:** (1) Visit `/column/state-of-arsenal` → confirm desktop nav highlights `Today's Paper` (since `/column/*` nests under Today), the eyebrow reads `STATE OF THE NATION · GUNNERS` in red mono, and the headline reads `Arsenal are finally boring — and that is the point.` with `finally` in the persona accent. (2) Switch persona via Settings → revisit → confirm `finally` recolours to the new accent, the byline monogram circle recolours, and the drop-cap recolours. (3) Confirm byline strip shows `The Voice` + `The Voice of the Terraces` + `4 min read` + `LONDON, FRIDAY`. (4) Confirm the drop-cap intro begins with a large decorative initial letter (T for `There…`), the rest of the paragraph flows around it. (5) Confirm the `<PullQuote>` block has a 3px red top rule, a large red leading quote glyph, italic 22px body, and the attribution caption. (6) Confirm the 3 body paragraphs flow into a 2-col grid on desktop and stack on mobile. (7) Confirm the `<CheersGeoffCallout>` lands at the foot for this column (`0.41` / `XGA PER MATCH` / Invincibles riff). (8) Visit `/column/mickey-on-united` → confirm headline has TWO emphasis segments (`disgrace` and `enjoying it`) both in the volcano accent. (9) Visit `/column/macca-on-anfield` → confirm scouser persona accent + no cheers block at the foot (this column ships without one). (10) Visit `/column/does-not-exist` → confirm `data-column-missing` 404 block lands with the back-link. (11) Resize to mobile → confirm article body compresses cleanly, 2-col grid collapses to 1-col, MobileNav still highlights `today`. (12) No betting copy anywhere on any column page. **Discoveries:**
    - **Static-content fixture is the right boundary for K1e-α.** The spec said `/column/[slug]` consumes `/api/broadsheet` cache; building that endpoint extension + cache + LLM call inside one slice would have doubled the LoC and introduced an LLM-failure surface that's hard to test without mocking the streaming response. Shipping with 3 hand-written sample columns proves the layout/primitives in isolation; β wires the LLM path and the localStorage cache against the same `ColumnRecord` shape (no JSON-template work wasted — the fixture IS the template).
    - **`HeadlineSegment[]` avoids `{@html}`.** The inline `<em>` highlight could have been done with `{@html headlineHtml}` + sanitisation, but the segment-array approach is XSS-safe by construction (Svelte escapes each `{seg.text}`) and trivially testable (the test asserts which segments became `<em>`). Codify for any future LLM-emitted markup that needs targeted highlighting — the LLM emits a `HeadlineSegment[]` JSON, not a `headlineHtml` string.
    - **`{@const}` block-tag-immediate-child rule (Svelte 5).** Tried `{@const intro = dropCap(...)}` inside an `<article>` wrapper — svelte-check rejected it (`const_tag_invalid_placement`). `{@const}` must be the immediate child of `{#if}`, `{#each}`, `{#snippet}`, etc. Fix: hoist into `$derived` at script level. Codify — `{@const}` is more restricted than v4 expects.
    - **Svelte 5 SSR decodes HTML entities.** `&ldquo;` in the source emits `"` (U+201C) in the SSR body; assert on the unicode codepoint, not the entity. Adds to the K0c "data-attr-prefix collision" / "scoped style not in SSR" pattern collection.
    - **Mobile nav uses `today` (not `more`) for `/column/*`.** Sub-routes pass their parent's nav id per `screens-inventory.md`. This differs from `/insights` (which nests under `more`). When the K1f `/more` index ships, it should still NOT list `/column/[slug]` — columns are linked from `/today` directly.
    - **PullQuote vs PunditQuoteBlock — keep both.** They share the "italic blockquote with leading glyph + attribution caption" grammar but diverge sharply on accent: PunditQuoteBlock uses a left-border + 58px glyph (in-flow analysis quote), PullQuote uses a 3px top-rule + 72px glyph (large editorial pull-quote inside long-form copy). The spec recommends a `variant` prop collapse — defer until K1e-β proves the shapes really do unify under broadsheet rendering (Mickey edition uses HotTake/red-left-bar, Gaffer edition uses centred 36px italic, neither is exactly PullQuote — so the collapse may need 3 variants, not 2).
  - [x] **K1e-β — Broadsheet + `/api/broadsheet` cache + JSON rendering** *(Manual-gated, ship pending sweep)* — Shipped: `frontend/src/routes/broadsheet/+page.svelte` mounts both shells (`active="today"` desktop, `active="today"` mobile — broadsheet nests under Today per K1e-α precedent), renders the `BroadsheetJson` shape from `lib/server/broadsheetPrompt.ts` straight: headline (44/54/64px serif), italic standfirst, byline + "generated at" (Europe/London), section blocks (red mono heading + serif body), optional pullQuote (persona-accent top border, 22px italic), optional closingLine, and a Regenerate button when cache is warm. Empty state: per-GW "Generate the GW{N} broadsheet" CTA when no cache exists for the active (gameweek, persona) pair. No-GW fallback when `dataService.getCurrentSeasonMatches()` returns nothing usable (no API key, quota exhausted, off-season). Errors surface inline as `⚠ {code}` (e.g. `rate_limited`, `anthropic_unavailable`, `broadsheet_parse_failed`). Active GW resolved client-side via `findCurrentGameweek(matches)` in `onMount`; SSR honours `data.initialGameweek` when provided. Persona switch mid-page re-reads the cache for the new voice without re-hitting the API. **New cache store** `lib/stores/broadsheetStore.ts` (mirrors `verdictsStore` SSR-safe pattern): key `kicker:broadsheet:gw{N}:{personaId}`, schema-validated read (rejects malformed/incomplete payloads), `readBroadsheet`/`writeBroadsheet`/`clearBroadsheet`. **New request helper** `lib/broadsheet/requestBroadsheet.ts`: POSTs `{personaId, gameweek}` to `/api/broadsheet`, returns tagged `{ok: true, broadsheet, generatedAt}` | `{ok: false, error}` discriminated union — HTTP errors, network rejections, and malformed-success bodies all collapse into the failure variant so the route renders one inline error state. `fetchFn` injectable for unit-testability (mirrors `streamChat`'s pattern). **21 new tests across 3 new files:** 8 in `broadsheetStore.test.ts` (null-on-missing, round-trip, dual-key namespacing, clear-targets-one, corrupt-JSON, missing-fields, malformed-broadsheet-payload, generatedAt override); 6 in `requestBroadsheet.test.ts` (happy-path, POST payload shape, HTTP-error-JSON-code, http_{status}-fallback for non-JSON errors, network rejection → error.message, malformed-success → `malformed_response`); 7 in `routes/broadsheet/page.test.ts` (SSR render, both shells mount, `today` active in both shells with `aria-current="page"`, no-gw empty state by default, GW-known empty state + Generate CTA from `initialGameweek` prop, GAMEWEEK kicker text, no betting copy in any state). **Validated:** `npm run check --prefix frontend` 0/0; `npm run test --prefix frontend -- --run` → 715/715 vitest green (was 694) across 84 files (was 81). **Deferred:** the original β scope included extending `/api/broadsheet` to accept `{kind: 'season-verdict', season, personaId}` so `SeasonDetail` could ship a "Generate verdict" CTA. That's a real K1e-γ slice — endpoint extension + season-context prompt variant + SeasonDetail button + verdict-write wiring + ~6 tests. Documented as `K1e-γ — Season verdict generation` follow-up below. **Manual sweep checklist for β:** (1) Visit `/broadsheet` cold (no cache, no API key in Settings) → confirm the no-gw fallback hint renders inside both shells with a pointer back to Settings. (2) Paste a real Football-Data key in Settings → reload `/broadsheet` → confirm the "No broadsheet on file for Gameweek {N}" empty-state renders with the Generate CTA (where `{N}` matches the current active GW from `/today`'s slate). (3) Click Generate → confirm the loading hint flips on ("filing copy…"), then the full broadsheet renders (headline + standfirst + byline + sections + pullQuote + closingLine) within ~10–30s. (4) Reload the page → confirm the broadsheet renders instantly from cache (no second LLM call — check devtools network for absence of `/api/broadsheet` POST). (5) Switch persona via Settings → revisit `/broadsheet` → confirm the empty-state returns for the new persona (cache is per-voice). Generate again → confirm a different stylistic voice. Switch back to original persona → confirm the original broadsheet re-renders from cache. (6) Click Regenerate on a cached broadsheet → confirm the cache is cleared + a fresh LLM call fires + the page rerenders with the new copy. (7) Force a rate-limit (spam Generate 11 times in <60s) → confirm `⚠ rate_limited` lands inline. (8) Temporarily unset `ANTHROPIC_API_KEY` server-side → confirm `⚠ anthropic_unavailable` lands inline. (9) Confirm the persona-accent top border on the PullQuote block matches the active persona (switch persona on a cached broadsheet → border colour changes). (10) Resize to mobile → confirm the broadsheet stacks cleanly inside the mobile shell, MobileNav highlights `today`. (11) No betting copy anywhere on any state. **Discoveries:**
    - **`$state` initializer can't reference props directly.** Svelte 5 emits `state_referenced_locally` when `let foo = $state(data?.x)` reads a prop in the initializer — the compiler treats it as a one-shot snapshot that won't track prop updates. Fix pattern: declare `let resolved = $state<T | null>(null)` for the mutable client-side override, then expose the unified value via `const final = $derived(resolved ?? data?.x ?? null)`. Codify for any future route that wants to seed reactive state from `+page.ts` data — the indirection through `$derived` is the canonical idiom.
    - **One-shot Sonnet → schema-validated cache.** The cache rejects payloads where `broadsheet.sections` isn't an array, so a partially-corrupted cache entry (mid-K2 schema migration, manual devtools tinkering) won't crash the route — it falls back to the Generate CTA as if the cache were empty. Worth codifying for all future LLM-payload caches: validate the shape, not just `typeof === 'object'`.
    - **Cache is per (GW, persona), not per (date, persona).** A reader who comes back mid-week sees the same broadsheet (correct — broadsheet is a "preview of the gameweek" artefact, not a daily one). When the GW rolls over, the new key naturally misses cache and prompts a fresh generation. No TTL needed.
    - **Generated-at is read from the server payload, not `new Date()`.** `/api/broadsheet` returns `generatedAt` in its JSON; the cache write honours that timestamp instead of using local clock. Means cross-device sync would just work if we ever moved the cache to Supabase (different clients see the same authoritative timestamp).
    - **The Regenerate button is the only client-side way to bust the cache** — there's no "Clear all broadsheet cache" admin surface. If a user reports stale content, the workflow is `localStorage.removeItem('kicker:broadsheet:gw{N}:{personaId}')` in devtools, or just hit Regenerate. K2 admin tooling can add a sweep.
  - [x] **K1e-γ — Season verdict generation** *(Manual-gated, ship pending sweep)* — Shipped: `/api/broadsheet` gains an optional `kind` discriminator (default `'broadsheet'` for backwards compat with K1e-β + the K1d-β cache callers); new `kind: 'season-verdict'` variant accepts `{personaId, season}` with `season` matching `/^\d{4}\/\d{2}$/`, returns `{verdict: string, generatedAt: string}` (plain prose, not JSON — verdicts are a single trimmed paragraph). New prompt builder `buildSeasonVerdictPrompt(persona, season, record?, seasonStats?)` in `lib/server/broadsheetPrompt.ts` composes the persona system prompt with the season's champion / runner-up / aggregate rate-line / outlier flags (all resolved server-side from bundled `leagueHistory` + `statsPack` — never trusted from the client payload). Empty-verdict guard returns `500 empty_verdict` so a blank LLM response surfaces inline rather than silently writing junk to cache. New client helper `lib/broadsheet/requestSeasonVerdict.ts` mirrors `requestBroadsheet`'s discriminated `{ok, verdict|error}` shape, injectable fetch for tests. `SeasonDetail.svelte` gains three optional props: `onGenerate?: () => void` (presence drives the CTA visibility — components without a handler stay read-only, e.g. test fixtures), `generating?: boolean` (disables button + flips label to "Filing copy…"), `generateError?: string | null` (inline `data-verdict-error` red-mono chip). Button copy flips per state — "Generate verdict" when cache cold, "Regenerate" when warm, "Filing copy…" while in-flight. `/insights/archive/+page.svelte` owns the generation state (`generating`, `generateError`, `verdictTick`) and the handler — on success it calls `writeVerdict(season, personaId, body)` and bumps `verdictTick`, which is a reactive dep inside the existing `$derived.by(readVerdict)` so the verdict body re-derives without a route reload. Season-switch clears `generateError` so a failure on one season doesn't carry over. Archive footer copy updated to point at the cache layer (`kicker:verdict:{season}:{personaId}`) — no longer references K1e as a future slice. **15 new tests across 3 new/changed files:** 4 added to `server.test.ts` (season-verdict happy path with trimmed output, malformed season → `invalid_season`, unknown kind → `unknown_kind`, empty verdict → `empty_verdict`); 5 in new `requestSeasonVerdict.test.ts` (success, POST payload shape with `kind: 'season-verdict'`, HTTP-error JSON code surfacing, missing-verdict → `malformed_response`, fetch rejection → `network_error`); 5 added to `SeasonDetail.test.ts` (CTA visible only when `onGenerate` supplied, "Regenerate" label flip when verdict cached, generating state with `disabled` + `data-verdict-generating`, `data-verdict-error` surfaces error, omit-CTA when no handler); 1 added to `routes/insights/archive/page.test.ts` (2× `data-verdict-generate` markers across desktop+mobile shells); 1 repurposed (footer assertion now points at `kicker:verdict:` cache key rather than the K1e milestone string). **Validated:** `npm run check --prefix frontend` 0/0; `npm run test --prefix frontend -- --run` → 730/730 vitest green (was 715) across 85 files (was 84). **Manual sweep checklist for γ:** (1) Visit `/insights/archive` → click `2003/04` → confirm the empty-state copy now reads "tap Generate verdict to commission one" instead of the old "check back once the column ships". (2) Click "Generate verdict" → confirm the button flips to "Filing copy…" (disabled) while the LLM call is in flight. (3) Wait ~5–15s → confirm a single paragraph of persona-voiced prose lands under the "Verdict · {persona name}" header, the button flips to "Regenerate", and `localStorage.getItem('kicker:verdict:2003/04:voice')` now contains the entry. (4) Hard-reload → confirm the verdict survives from cache without re-firing the LLM call (devtools network: no POST to `/api/broadsheet`). (5) Switch persona via Settings → revisit same season → confirm the empty-state returns (cache is per-persona). Click Generate again → confirm a stylistically different paragraph lands; switch back to original persona → original verdict re-appears from cache. (6) Click "Regenerate" on a cached verdict → confirm the LLM call fires again and the body updates in place. (7) Force a rate-limit (spam Generate 11 times in <60s) → confirm `⚠ rate_limited` lands in the inline `data-verdict-error` chip without erasing the cached verdict. (8) Temporarily unset `ANTHROPIC_API_KEY` server-side → confirm `⚠ anthropic_unavailable`. (9) Pick `2025/26` (in-progress) → confirm Generate still works — the prompt builder substitutes "This season is still in progress — no champion yet" for the champion fact line and the LLM should produce a reasonable mid-season verdict. (10) Resize to mobile → confirm the CTA renders correctly inside the mobile shell + the button width doesn't break the verdict block layout. (11) No betting copy anywhere on the archive page in any state. **Discoveries:**
    - **`$derived.by(readVerdict)` needs an explicit reactive dep to re-run after a cache write.** `localStorage` reads aren't reactive, so without a dep the verdict block stays stale until the user navigates away and back. Codified pattern: `let verdictTick = $state(0)` + read `verdictTick` inside the `$derived.by` body + `verdictTick++` after each successful write. Cheaper than mirroring the entire cache shape into `$state` (which would also have to invalidate on persona change). Apply to any future `$derived` that wraps a non-reactive read.
    - **Backwards-compat via optional discriminator.** The K1e-β `requestBroadsheet` callers still POST `{personaId, gameweek}` with no `kind` field; the server now treats `body.kind === undefined` as `'broadsheet'` so those callers are untouched. Tests confirm: every existing K1e-β test in `server.test.ts` and `requestBroadsheet.test.ts` still passes unchanged. Codify the pattern — future kind variants extend this discriminator rather than introducing new endpoints.
    - **Plain prose vs JSON.** Broadsheet returns structured JSON because the template has 6 slots (headline/standfirst/byline/sections/pullQuote/closingLine). Verdict is one paragraph, so JSON wrapping would just add a parse step + failure mode for no benefit. The server returns `{verdict, generatedAt}` and the client renders the verdict verbatim. The `empty_verdict` guard catches the only failure mode (model returns whitespace).
    - **Season facts resolved server-side.** Both `leagueHistory` and `statsPack` are pure data modules with no runtime deps — the server route imports them directly via `$lib/...` and resolves the season facts from its own state rather than trusting the client. Means a tampered client payload can't inject false stats into the prompt. Pattern for any future per-context prompt: pass an identifier from the client (here `season: string`), resolve facts server-side.
    - **CTA presence gated on `onGenerate` prop, not a boolean.** Older versions of `SeasonDetail` (e.g. test fixtures that render the component standalone) render verdict-empty without ever wiring a generate handler. Gating the CTA on `onGenerate ? ... : null` means those callers stay read-only without a flag toggle. Codify for future presentational components — prop-presence is the cleanest "feature on/off" hinge.
    - **Verdict cache write path is no longer dormant.** Pre-γ, `writeVerdict` was exported + tested but never called from any UI. γ closes the loop — `writeVerdict` is now called from the archive route immediately after a successful `requestSeasonVerdict`, keyed by `(season, personaId)` exactly as the K1d-β contract specified.
- [ ] **K1f — Notifications + Search advanced** *(Manual-gated, split into α/β)* → `k1.6`
  - [x] **K1f-α — Notifications inbox + feed store + NotificationItem** *(Manual-gated, ship pending sweep)* — Shipped: `/notifications/+page.svelte` mounts both shells (desktop KickerShell with no `active` since the desktop nav rail doesn't include a `notifications` id — codified per `K1f` plan-body note that `/notifications` belongs to the future `/more` group; mobile `active="more"` mirroring `/insights`). Renders Rule header `THE WIRE · Notifications · {N unread | All caught up}` over an inbox layout: SSR loading hint → empty-state on hydrate-with-no-items → action row (`Mark all read` + `Clear all`) + list when items exist. Falls back to a centred italic empty-state explaining "Match kick-offs, model edges, and broadsheet drops will surface here once the engine starts filing" — honest about the feed being driver-less in α. Foot links to `/settings#notifications` for prefs. **New store** `lib/stores/notificationsFeed.ts` (SSR-safe, schema-validated per-item, `value-bet` type rejected by the validator — codifies the betting-removal lock; cap `MAX_ITEMS = 50`, key `kicker:notificationsFeed`): exports `readNotifications / addNotification / markRead / markAllRead / clearAll`, plus `STORAGE_KEY` + `MAX_ITEMS` constants and `NotificationType` (`match-start | model-edge | broadsheet-ready` — the R0+ delta replacing `value-bet`) + `NotificationItem` types. **New primitive** `lib/components/notifications/NotificationItem.svelte` — 3-col grid (120px type label / title+body / time), persona-accent type label (via `var(--persona-accent, var(--red))` — no inline hex), unread dot (8px circle in persona accent, omitted when `read: true`), serif title + italic body + mono relative time. `data-notification-item` / `-type` / `-read` markers. **New util** `lib/utils/relativeTime.ts` — `formatRelativeTime(input, now?)` returns "just now" / "5m ago" / "3h ago" / "Yesterday" / "3d ago" / `DD MMM` for older; handles invalid input + future timestamps cleanly. **27 new tests across 4 new files:** 6 in `relativeTime.test.ts` (the 5 unit bands + invalid + future clamp), 9 in `notificationsFeed.test.ts` (empty, add-prepends, MAX_ITEMS cap, markRead one-only, markAllRead, clearAll, corrupt-JSON recovery, invalid-type filter — explicitly proves `value-bet` is rejected, partial-entry filter), 5 in `NotificationItem.test.ts` (markers + content, unread-dot gated on read, type-label mapping across all 3 types, CSS-var-not-hex, no betting copy), 7 in `routes/notifications/page.test.ts` (SSR render, both shells, `more` active in MobileNav with `aria-current="page"`, SSR-loading appears 2× then yields no list/empty markers on first paint, settings-link href + marker, Rule kicker + title text, no betting copy). **Validated:** `npm run check --prefix frontend` 0/0; `npm run test --prefix frontend -- --run` → 757/757 vitest green (was 730) across 89 files (was 85). **Manual sweep checklist for α:** (1) Visit `/notifications` → confirm desktop renders inside KickerShell with NO sidebar item highlighted (no `aria-current="page"` on any desktop nav anchor — α intentionally leaves the desktop nav id unset since `notifications` isn't in the locked 6-id list) and Rule reads `THE WIRE · Notifications · All caught up`. (2) Empty-state copy renders explaining the inbox will populate once the engine fires. (3) Open devtools console → run `localStorage.setItem('kicker:notificationsFeed', JSON.stringify([{id:'a',type:'match-start',title:'Arsenal v Liverpool kicks off',body:'Anfield · 16:30',createdAt:new Date(Date.now()-5*60000).toISOString(),read:false},{id:'b',type:'model-edge',title:'Edge on Spurs (H)',body:'ELO and Poisson agree the home side is undervalued',createdAt:new Date(Date.now()-3*3600000).toISOString(),read:false},{id:'c',type:'broadsheet-ready',title:'GW32 broadsheet filed by The Voice',body:'Tap to read',createdAt:new Date(Date.now()-26*3600000).toISOString(),read:true,href:'/broadsheet'}]))` → reload → confirm 3 items render newest-first, unread dot on first two, mono labels `KICK-OFF` / `MODEL EDGE` / `BROADSHEET`, relative times `5m ago` / `3h ago` / `Yesterday`. (4) Click the first item → confirm the unread dot disappears (markRead fired) and the unread counter in the Rule action drops from `2 unread` to `1 unread`. (5) Click `Mark all read` → confirm all dots disappear and Rule action flips to `All caught up`. The button should disable itself (opacity 40%). (6) Click `Clear all` → confirm list empties and empty-state returns. (7) Manually inject a `value-bet` typed item (`localStorage.setItem('kicker:notificationsFeed', JSON.stringify([{id:'x',type:'value-bet',title:'shady',body:'',createdAt:new Date().toISOString(),read:false}]))`) → reload → confirm it does NOT render (the schema validator rejects the type, protecting the betting-removal lock). (8) Set an item with `href: '/broadsheet'` → click it → confirm navigation fires AND markRead happens before the navigation. (9) Click `Manage in Settings →` → confirm landing on `/settings#notifications`. (10) Resize to mobile → confirm `/notifications` collapses to a single column inside the mobile shell, MobileNav highlights `More`, action buttons remain reachable above the bottom-nav. (11) Switch persona via Settings → revisit → confirm the type labels + unread dots recolour to the new persona accent. (12) No betting copy anywhere. **Discoveries:**
    - **`/notifications` has no desktop nav slot.** KickerShell's `NavId` is `'today' | 'fixtures' | 'predictions' | 'oracle' | 'insights' | 'settings'` — adding a `notifications` slot would be a global nav rail change. α deliberately leaves `active` unset (legal because the prop is optional). The honest fix is the same as `/insights` archive vs MobileNav: when `/more` lands (still K1f-adjacent), it lists `/notifications` alongside `/insights`, `/settings`, `/roster`. Codify: any sub-`/more` route uses no desktop active + `active="more"` on mobile, mirroring `/insights`.
    - **The feed has no producer yet.** α ships the inbox + the store + the validator, but nothing in the app currently *writes* notifications. The natural producers are (a) match-start scheduler that polls fixture status changes (K2-ish), (b) prediction engine when a fixture's model-edge crosses a threshold (depends on K1b-γ.1's per-fixture analysis path), and (c) `/api/broadsheet` success → `addNotification({type: 'broadsheet-ready', ...})`. Recommend `K1f-α.1 — Wire broadsheet-ready producer` as the cheapest first producer: ~10 LoC at the success branch of `requestBroadsheet` + 2 tests. Park until α sweep signs off.
    - **`value-bet` type filter doubles as a betting-removal contract test.** The schema validator rejects unknown type strings, but the test explicitly proves the legacy `value-bet` value is filtered out. Codify: any future enum-like cache schema should have an explicit test asserting the deprecated value is rejected, so an accidental re-introduction breaks the build.
    - **Relative time is `Intl`-free for the short bands.** "5m ago" / "3h ago" / "Yesterday" / "3d ago" are pure arithmetic on `Date.getTime()`. Only the > 1-week fallback uses `toLocaleDateString('en-GB', ...)`. Means the bulk of the formatter is timezone-independent and trivially testable with `Date.now()` arithmetic, no `Intl.RelativeTimeFormat` polyfill exposure.
    - **`NotificationItem` is a `<button>`, not an `<a>`.** Because click-handling needs to call `markRead` *before* navigation (and items can be href-less when they're informational only — e.g. "Match kicked off, no link"). Click handler is `onclick` → `markRead` → optional `window.location.assign(href)`. Alternative would be a `<a onclick>` with `event.preventDefault()`, but that's accessibility-fragile for items without targets. The `<button>` form is the correct semantic for "interactive row that may or may not navigate".
  - [x] **K1f-α.1 — Wire broadsheet-ready producer** *(Manual-gated, ship pending sweep)* — Shipped: new `lib/broadsheet/notifyBroadsheetReady.ts` helper exposes `notifyBroadsheetReady({personaId, personaName, gameweek, headline})` which composes the notification shape (`type: 'broadsheet-ready'`, title `GW{N} broadsheet filed by {persona.name}`, body = headline, href `/broadsheet`, id `broadsheet-ready-gw{N}-{personaId}-{ts}`) and writes via the schema-validated `addNotification`. `/broadsheet`'s `generate()` calls it immediately after a successful `writeBroadsheet`, so Regenerate also re-fires (each successful generation gets its own inbox entry — newest-first, capped by the store's MAX_ITEMS=50). **2 new tests** in `notifyBroadsheetReady.test.ts`: (1) full-shape assertion — type/title/body/href/read=false/id-pattern + verifies the entry round-trips through `STORAGE_KEY`, (2) two-call newest-first ordering across different personas. **Validated:** `npm run check --prefix frontend` 0/0; `npm run test --prefix frontend -- --run` → 759/759 vitest green (was 757) across 90 files (was 89). **Manual sweep checklist:** (1) Visit `/broadsheet` with a real Football-Data + Anthropic key, click Generate → wait for the broadsheet to render → navigate to `/notifications` → confirm one new `broadsheet-ready` item lands newest-first with title `GW{N} broadsheet filed by {persona.name}`, body = the headline, and clicking it routes back to `/broadsheet`. (2) Click Regenerate → revisit `/notifications` → confirm a second entry appears (each generation is its own inbox event). (3) Switch persona via Settings → generate again → confirm the new item carries the new persona's name in the title. (4) Confirm the inbox unread counter on `/notifications` reflects the new items. **Discoveries:** (1) Producer is a pure helper, not a wrapper around requestBroadsheet — keeps the inbox write decoupled from the API client so future producers (match-start, model-edge) can sit alongside it without inheriting fetch-error semantics; (2) Each successful generation is its own event by design — dedupe (e.g. "only one broadsheet-ready per GW per persona") would need explicit id-based deletion in `addNotification`, deferred to K1f-β's filtered inbox view if it becomes a real UX problem.
  - [ ] **K1f-β — Search advanced** *(Manual-gated, split into β.1/β.2)* — `/search/+page.svelte` builds `SearchInput` + `SearchResultRow` + `RecentChips`. Client-side full-text index over fixtures / players / seasons / threads. Split because β.1 ships the input shell + recall path (no domain data), β.2 ships the index + result wiring (negotiates async `dataService` calls + sync stores + statsPack).
    - [x] **K1f-β.1 — `/search` route shell + SearchInput + RecentChips + recentSearches store** *(Manual-gated, ship pending sweep)* — Shipped: `/search/+page.svelte` mounts both shells (desktop KickerShell with no `active` since `search` isn't in the locked NavId list — same precedent as `/notifications`; mobile `active="more"`). Rule `SEARCH · The morgue · K1f · β.1` over a `SearchInput` (controlled, `bind:value` via Svelte 5 `$bindable`, submits on Enter or button click, disabled-on-whitespace-only) + `RecentChips` (chips deduped case-insensitively, each chip carries an `×` removal button as a sibling `<button>` to avoid nested-button invalid HTML, plus a `Clear` action for the row). β.1's results stub flips the empty-state to a `“{query}”` confirmation + "full-text index lands in K1f-β.2" hint, but the recall path is end-to-end — submitting a query writes to recent, picking a recent chip re-runs the search, removing a chip drops just that entry. New store `lib/stores/recentSearches.ts` (key `kicker:recentSearches`, MAX_RECENT=8, SSR-safe per the K1f-α `notificationsFeed.ts` pattern) exports `readRecent / addRecent / removeRecent / clearRecent` + constants. Add path trims whitespace, ignores empty queries, and dedupes case-insensitively by moving the entry to the front (so 'arsenal' after 'Arsenal' yields `['arsenal']` not `['arsenal', 'Arsenal']`). Read path tolerates corrupt JSON, non-array payloads, and non-string entries. **29 new tests across 4 new files:** 11 in `recentSearches.test.ts` (empty default, prepend newest-first, dedupe + case-insensitive move-to-front, ignore empty/whitespace, MAX_RECENT cap, removeRecent case-insensitive, clearRecent removes key, corrupt-JSON recovery, non-array payload, non-string entry filter, SSR no-op); 7 in `SearchInput.test.ts` (markers + placeholder, Clear hidden when empty, Clear visible with value, submit disabled on whitespace-only, submit enabled with characters, CSS-var-not-hex for glyph, custom placeholder prop); 4 in `RecentChips.test.ts` (renders nothing when empty, one chip per query with marker attrs, label + Clear button, no betting copy); 7 in `routes/search/page.test.ts` (SSR render, both shells, `more` active in MobileNav, SearchInput rendered 2× (one per shell), SSR loading hint 2× without empty/results/recent markers, kicker + title text, no betting copy). **Validated:** `npm run check --prefix frontend` 0/0; `npm run test --prefix frontend -- --run` → 788/788 vitest green (was 759) across 94 files (was 90). **Manual sweep checklist for β.1:** (1) Visit `/search` → confirm desktop nav has nothing highlighted (intentional — search sits under future `/more`) and the Rule reads `SEARCH · The morgue · K1f · β.1`. (2) Confirm the empty-state copy explains the index ships in β.2 + invites the user to log queries via Enter. (3) Type `arsenal` → confirm the Clear and Search ↵ buttons appear, Search ↵ goes from disabled to enabled at the first non-space character. (4) Press Enter → confirm a `“arsenal”` confirmation block appears and a new chip `arsenal` shows under `Recent searches`. (5) Type `   ` (whitespace) → confirm Search ↵ stays disabled. (6) Type `liverpool` → submit → confirm two chips now (`liverpool` newest-first). (7) Click the `arsenal` chip → confirm the input pre-fills, the chip moves to the front (dedupe), and the result confirmation updates. (8) Type `ARSENAL` → submit → confirm only one `ARSENAL` chip remains at the front (case-insensitive dedupe). (9) Click the `×` on a chip → confirm only that chip drops. (10) Click the row's `Clear` button → confirm all chips clear. (11) Reload → confirm chips persist (localStorage round-trip). (12) Resize to mobile → confirm `/search` collapses cleanly, MobileNav highlights `More`, the SearchInput's Search ↵ button stays reachable above the bottom-nav. (13) Switch persona via Settings → confirm the SearchInput `/` glyph recolours to the new persona accent. (14) No betting copy anywhere. **Discoveries:**
      - **`disabled` substring footgun.** Tailwind's `disabled:opacity-40` utility contains the literal word `disabled`, so a test of `expect(html).toContain('disabled')` to assert the boolean attribute is wrong — it matches the class string too. Fix pattern: regex `/\sdisabled(=""|>|\s)/` matches the actual HTML attribute form (Svelte SSR emits either ` disabled=""` or ` disabled>` depending on adjacent attributes). Codify alongside the K0c data-attr-prefix collision rule.
      - **`$bindable` with `?:`-defaulted Props.** `let { value = $bindable('') }: Props = $props()` works in SSR even though the wrapper isn't actually a controlled input from the test's perspective. Means the test can pass `props: { value: 'arsenal' }` and read it back from the SSR'd HTML without needing a parent bind. Codify for any future input primitive — the bindable default keeps the standalone-render path working.
      - **Search has no desktop nav slot — same `/more` pattern as `/notifications` and `/insights`.** When the K1f-adjacent `/more` index lands, it should list `/search`, `/notifications`, `/insights`, `/settings`, `/roster`. Mobile uses `active="more"`; desktop's `active` prop stays unset (legal because optional).
      - **Chip remove + chip pick are sibling `<button>`s, not a nested button-in-button.** Same pattern as `ThreadListRail`'s row + delete buttons from K1a-β.4. Nested buttons emit invalid HTML and break click semantics. The chip uses a flex-row container with a left border between the two buttons so it still reads as a single visual unit.
      - **β.1 results stub vs the planned `SearchResultRow`.** β.1 deliberately stops short of the result primitive — the stub renders the bare query string back, which is enough to prove the recall path. β.2 introduces `SearchResultRow` (typed across fixture / player / season / thread variants) once the index has shapes to feed it. Means β.2 doesn't need to refactor β.1's UI — only swap the `data-search-results-stub` block for a `data-search-results-list` block.
    - [x] **K1f-β.2 — Full-text index + SearchResultRow** *(Manual-gated, ship pending sweep)* — Shipped: pure index module `lib/search/buildIndex.ts` exposes typed `SearchItem` (`kind: 'fixture' | 'player' | 'season' | 'thread'`) + `buildIndex(snapshot)` + `searchIndex(items, query, limit=25)`. Per the spec: fixtures → `/fixtures/[id]` href + `"Home v Away"` label + `YYYY-MM-DD · GW{n}` sub, scorers → NO href (player profile is K1g) + `"{team} · {goals} goals"` sub, seasons → `/insights/archive?season={encoded}` href, threads → `/oracle?thread={id}` href + first-user-message snippet in tokens. Scoring is exact > label-prefix > token-exact > token-prefix > token-includes, with a hard AND-gate (every query token must hit *something* or the item drops to 0) — multi-token queries like `arsenal liverpool` correctly collapse to the one fixture that satisfies both. Async glue `lib/search/loadSearchSources.ts` wraps `dataService.getCurrentSeasonMatches()` + `dataService.getTopScorers(20)` (each in a `safeArray` so a missing API key / quota exhaustion degrades to `[]` rather than crashing), defaults seasons from `LEAGUE_HISTORY`, defaults threads from `get(threadsStore).threads` — every dep is injectable per the β.1 streamChat pattern. `SearchResultRow.svelte` renders an `<a>` when href is set, a non-interactive `<div>` for player rows; kind label colour driven by `var(--persona-accent, var(--red))`. `/search/+page.svelte` rewired: lazy module-level cache (`cachedIndex` + `pendingBuild` so concurrent submits share one load), `searching` flag flips the results header between "Searching…" and "Results for {q}", empty-state copy points at season-format hints when no matches land. Recall path from β.1 (submit → chip → re-submit) survives unchanged — `handlePick` calls `runSearch` which re-uses the cached index. Kicker bumped to `K1f · β.2`. **16 new tests across 3 files:** 11 in `buildIndex.test.ts` (item-per-source typing, per-kind href/sub/label assertions for all 4 variants, URL-encoded season slash, empty-on-whitespace, cross-kind substring match, exact-season ranks top, no-token-hits returns empty, limit cap, AND-token gate); 5 in `SearchResultRow.test.ts` (`<a>` variant with markers + href, non-interactive variant for player, persona-accent CSS-var/no inline hex, sub omission when undefined, no betting copy); 1 negative-assertion added to `routes/search/page.test.ts` (SSR has no `data-search-results-list` either) + description bumped β.1 → β.2. **Validated:** `npm run check --prefix frontend` 0/0; `npm run test --prefix frontend -- --run` → 804/804 vitest green (was 788) across 96 files (was 94). **Manual sweep checklist for β.2:** (1) Visit `/search` cold → confirm Rule reads `SEARCH · The morgue · K1f · β.2` and the empty-state copy invites typing a query. (2) With a real Football-Data key in Settings, type `arsenal` → Enter → confirm Results for "arsenal" header appears, followed by a list of matching results spanning fixtures (Arsenal v ...), the 2003/04 / 2024/25 archive season cards, threads that mention Arsenal (if any in `kicker:threads`), and any top-scorer who plays for Arsenal. (3) Click a fixture result → confirm navigation to `/fixtures/{id}`. (4) Click a season result → confirm navigation to `/insights/archive?season=2003%2F04` (URL-encoded slash). (5) Click a player result → confirm NOTHING happens (no href, by design — K1g lands player profiles). (6) Click a thread result → confirm navigation to `/oracle?thread={id}` (note: `/oracle` may not yet parse this query param — that's a K1f-β.3 follow-up). (7) Type a multi-word query like `arsenal liverpool` → confirm only fixtures matching BOTH tokens appear (AND-gated). (8) Type a nonsense query like `xyzqqq` → confirm the `No filings on "xyzqqq"` empty-state copy renders. (9) Wipe the Football-Data key in Settings → reload → search `2003/04` → confirm season results still render even with no API key (bundled `LEAGUE_HISTORY` survives the safe-array fallback). (10) Open devtools network on second-and-third searches → confirm `/competitions/PL/matches` is NOT re-fetched (module-level cache holds). (11) Pick a recent-chip → confirm it re-runs without a fresh API call. (12) Resize to mobile → confirm result rows stack cleanly inside the mobile shell and remain tap-friendly above the bottom-nav. (13) No betting copy anywhere. **Discoveries:**
      - **Hard AND-gate on query tokens vs naive bag-of-words.** A naive "any token hit boosts score" scheme would surface noisy partial matches (`arsenal liverpool` returning all Arsenal-flavoured items even though `liverpool` doesn't hit them). The hard gate (`if (!hit) return 0`) lines up with user expectation: every typed word must land somewhere, full stop. Worth codifying for any future K-slice search surfaces (player season-stats, model-edge log search).
      - **`safeArray` over `try/catch` per source.** Both data-service calls throw on no-API-key (per the K1d-α discovery). Wrapping each loader in `safeArray` keeps the index alive on the sources that DO work — a user with no API key still gets seasons + threads. Mirrors the `requestBroadsheet` / `requestSeasonVerdict` discriminated-union pattern in spirit (failure ≠ thrown exception).
      - **Module-level cache vs `$state` cache.** Cache lives at module scope, not component scope, so HMR + persona switches don't invalidate it (the index is identical regardless of persona). Means the user's session shares one index across `/search` mounts — including the case where they leave and return to the route. The trade-off: a hot-reload during dev sometimes serves stale data; a hard reload always rebuilds. Cheap enough that this is the right default.
      - **Player results have no href on purpose.** Per the spec: K1g introduces `/roster/[id]` (or similar). Until then, a player row renders as a non-interactive `<div>` rather than a dead link. The `SearchResultRow.svelte` if/else branch enforces this at the component level — no caller can accidentally pass a player item with an href and bypass the missing-profile state.
      - **`/oracle?thread={id}` doesn't yet hydrate the active thread from the query string.** Clicking a thread result lands the user on `/oracle` but β.3 needs to parse `?thread=…` and call `threadsStore.setActive(id)` on mount. Recommend `K1f-β.3 — Oracle thread deep-link` as a small follow-up (~10 LoC + 2 tests). *(Shipped as β.3 below 2026-05-24.)*
    - [x] **K1f-β.3 — Oracle thread deep-link** *(Manual-gated, ship pending sweep)* — Shipped: new `frontend/src/routes/oracle/+page.ts` reads `?thread={id}` from `url.searchParams` and threads it into the page as `data.thread`. `/oracle/+page.svelte` accepts an optional `data` prop and calls `applyThreadDeepLink(data?.thread)` from `onMount`. New helper `lib/oracle/applyThreadDeepLink.ts` is browser-only and pure — given an id, it reads `threadsStore` state, verifies the thread exists, and calls `threadsStore.setActive(id)`; returns `true` on activation, `false` for nullish / unknown ids (so a stale link from search can't leave the UI pointing at a deleted thread). **3 new tests** in `applyThreadDeepLink.test.ts`: (1) returns false + leaves state unchanged for `null`/`undefined`/`''`, (2) returns false + keeps current active for unknown ids, (3) activates the matching thread when the id exists. `afterEach` calls `threadsStore.clearAll()` to scrub the module-level singleton between cases. The existing `routes/oracle/page.test.ts` keeps passing untouched because `data` is optional and `data?.thread` is null-safe under SSR. **Validated:** `npm run check --prefix frontend` 0/0; `npm run test --prefix frontend -- --run` → 807/807 vitest green (was 804) across 97 files (was 96). **Manual sweep checklist for β.3:** (1) Open `/oracle` → submit one user message in any thread (so the thread persists). Note the thread id from devtools (`localStorage.getItem('kicker:threads')`). (2) Visit `/oracle?thread={that-id}` directly → confirm the matching thread is now the active one (its left-rail row carries the persona-accent border + its messages render in the centre column). (3) Visit `/oracle?thread=does-not-exist` → confirm the previously-active thread (or the empty-state hint, if none) is unaffected. (4) From `/search`, type a query that returns a thread result (e.g. some text from a logged user message), click the thread row → confirm `/oracle?thread={id}` loads with that thread active. (5) Switch persona via Settings → revisit `/oracle?thread={id}` → confirm the thread stays active across persona changes. (6) No betting copy on the route in any state. **Discoveries:**
      - **`+page.ts` over `$app/stores`.** Reads the search param at the route boundary and threads it into a typed `data` prop, mirroring the K1b-α codified pattern (also used by `fixtures/[id]/+page.ts`). Avoids needing to mock `$app/stores` in SSR tests and keeps the route component's `onMount` to a single statement.
      - **Pure-helper boundary for browser-only logic.** `applyThreadDeepLink` is the second time this pattern surfaces (after `notifyBroadsheetReady` in K1f-α.1): extract anything that touches `threadsStore` / `localStorage` into a callable function so vitest can drive it directly without `@testing-library/svelte` or jsdom. Component just calls the helper from `onMount`.
      - **Validate-then-activate.** `threadsStore.setActive` is already a no-op for unknown ids (it has its own existence check) — `applyThreadDeepLink` duplicates that guard so the return value can faithfully report whether activation happened (useful if a future caller wants to fall back to "create empty thread" when the deep-link misses).
- [x] **K1g — Roster + voice range** *(Manual-gated, ship pending sweep)* → `k1.7` — Shipped: `/roster/+page.svelte` mounts both shells (desktop KickerShell with no `active` since `roster` isn't in the locked NavId list — same precedent as `/notifications` and `/search`; mobile `active="more"`), iterates `KICKER_PERSONA_ORDER` over the reused `PunditCard` primitive in a 1/2/5-col responsive grid (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-5`), subscribes to `personaStore` for the live-selected state, and dispatches `personaStore.set(id)` on card click for instant voice switching (mirroring `/settings`'s PunditPickerCard ethos — onclick IS the mutation, no save button). Foot link cross-routes to `/roster/voices`. `/roster/voices/+page.svelte` follows the same shell contract and renders 10× new `VoiceColumn` primitive driven by `KICKER_VOICE_TAKES` (existing fixture from `lib/fixtures/voiceTakes.ts`) over the same matchup (Liverpool v Tottenham at Anfield) so the user can directly compare how each persona files the same fixture — header (monogram + name + id-tag) → 13px serif `open` paragraph → italic `aside` with `border-left` accent → mono footer `Pick · {pick}` / `{conf}%`. Click any column → `personaStore.set(id)` (same ethos as roster). `VoiceColumn` is a `<button>` with `aria-pressed` + `data-voice-*` markers (`-column`, `-id`, `-selected`, `-mono`, `-name`, `-id-tag`, `-open`, `-aside`, `-pick`, `-conf`); selected state recolours border + outline + monogram background via `var(--persona-accent, var(--ink))` — no inline hex. **22 new tests across 3 files:** 5 in `VoiceColumn.test.ts` (full marker + content render, selected=false absence, selected=true accent + aria-pressed, no inline hex when selected, no betting copy); 9 in `routes/roster/page.test.ts` (SSR render, both shells, `more` active in MobileNav with `aria-current="page"`, desktop sidebar leaves no anchor as current, 10×2 PunditCard count via `data-pundit-id="` matches, 2 roster grids, voice-range cross-link href + marker, kicker "THE STAFF" + title, no betting copy); 8 in `routes/roster/voices/page.test.ts` (same shape: SSR, both shells, mobile-more, 10×2 VoiceColumn count, 2 grids, roster cross-link href + marker, kicker "VOICE RANGE", no betting copy). **Validated:** `npm run check --prefix frontend` 0/0; `npm run test --prefix frontend -- --run` → 829/829 vitest green (was 807) across 100 files (was 97). **Manual sweep checklist for K1g:** (1) Visit `/roster` → confirm desktop nav has nothing highlighted (intentional — roster sits under future `/more`) and the Rule reads `THE STAFF · The Roster · Tap a pundit to switch voice`. (2) Confirm the grid renders 10 PunditCards in a 5-col layout on desktop (≥lg) and 2-col on tablet. (3) Confirm the card matching the currently-active persona carries the accent border + SELECTED badge. (4) Click a different card → confirm the persona switches instantly: the SELECTED badge moves, the active border recolours, and the desktop sidebar's "Pundit on Duty" block + accent dot update without a reload. (5) Refresh → confirm the new persona persists (localStorage `kicker:personaId`). (6) Click `Hear all ten on the same fixture →` → confirm navigation to `/roster/voices`. (7) On `/roster/voices`, confirm the Rule reads `VOICE RANGE · Ten voices, one matchup · Liverpool v Tottenham · Anfield`. (8) Confirm 10 VoiceColumn cards render with: monogram + persona name in header; a serif `open` paragraph that reflects each pundit's voice (Macca's "to be fair", Mickey's "DISGRACE", Wanderer's "triffic", etc.); an italic aside under a thin left rule; a mono footer line `Pick · LIV 2-0 / 69%` (or per-persona variant). (9) The currently-active persona's VoiceColumn carries the accent border + monogram inversion. (10) Click a different column → confirm the persona switch fires (column accent moves, sidebar Pundit-on-Duty updates). (11) Click `← Back to the roster` → confirm `/roster` reopens with the same persona active. (12) Resize to mobile → confirm both pages collapse to a single column, MobileHeader shows `The Roster` / `Voice Range`, MobileNav highlights `More`, and tapping any card still switches persona instantly + recolours the bottom-nav accent. (13) Switch persona via Settings → revisit either page → confirm the SELECTED state lands on the right card without click. (14) No betting copy anywhere on either page. **Discoveries:**
  - **`PunditCard` was already built for this slice.** The primitive existed (with `aria-pressed` + accent border + `onclick(id)`) from earlier work but had no consumer outside its own test file — K1g is what activates it. Roster page is ~30 LoC because the heavy lifting was already shipped. Codify: when a primitive lands without a consumer, link it from the *next* unchecked slice's plan body so a future loop iteration doesn't accidentally rebuild it.
  - **`VoiceColumn` mirrors `PunditCard`'s interaction contract on purpose.** Both are `<button>` + `aria-pressed` + `data-{x}-selected` + `onclick(id)`. Means `/roster` and `/roster/voices` dispatch persona selection identically (`personaStore.set(id)`), and a future selector helper (e.g. an A/B persona-picker hook) only has to know one callback shape.
  - **Desktop nav slot remains the right call.** Tempting to add `roster` to `KickerShell.NavId` for clarity, but the locked 6-id sidebar is already at the visual ceiling per the spec — `today/fixtures/predictions/oracle/insights/settings` is the editorial spine. `/roster` joins `/notifications` + `/search` under the future `/more` index page. When `/more` lands, it should list `/roster` + `/roster/voices` (or roster as a single entry pointing at the picker, with voices as a sub-link from that page).
  - **`KICKER_VOICE_TAKES` is single-fixture by design.** All 10 takes refer to the same Liverpool v Tottenham matchup. Direct comparability is the whole point of the `/roster/voices` UX — see how each pundit handles the same evidence. A future K-slice that wanted per-fixture voice samples would need a *function* `(personaId, fixtureId) → VoiceTake` backed by an LLM call (cache pattern same as K1e-γ verdicts). Not in scope for K1g.
  - **Test count is 22 not the planned ~6.** The slice spec called for ~6 tests; I shipped 22 to lock the contract on three new surfaces (primitive + 2 routes). Conservative interpretation of "~" — each surface independently regression-tested. If the loop wants a leaner default for future presentational slices, codify "~6" as "1–2 tests per surface" rather than "6 total".
- [x] **K1h — Landing + pricing tiers** *(Manual-gated, hand-built 2026-05-25, ship pending sweep)* → `k1.8` — `/landing/+page.svelte` (444 lines) hand-authored by user before the overnight loop. Typed `Feature` + `PlanTier` interfaces, three pricing tiers (Touchline £0 / Press Box £4 / Print Run £20), imports from `$lib/personas` + `$lib/fixtures/voiceTakes` per loop conventions. **No auth wiring** (paywall is K2c). Test coverage shipped separately as K1h.1 below.
- [x] **K1h.1 — Landing tests** *(Auto-gated, ship pending sweep — 2026-05-25)* — Co-located `frontend/src/routes/landing/page.test.ts` shipping 7 SSR tests against the standalone landing surface (the page intentionally does NOT mount KickerShell/MobileHeader, so the usual "both shells" assertion is omitted): (1) `data-landing-page` marker present, (2) 6 `data-landing-feature` cards using the `(?![-\w])` lookahead to dodge the K0c data-attr-prefix collision rule, (3) 4 `data-landing-voice` cards with the locked `scouser / volcano / optimist / philosopher` id quartet, (4) 3 `data-landing-plan` cards with `touchline / press-box / print-run` tier markers, (5) Press Box's `MOST POPULAR` + Print Run's `FOR THE COMMITTED` badge copy (2 `data-landing-plan-badge` instances — Touchline is badge-less), (6) every primary CTA (`data-landing-cta-nav`, `data-landing-cta-nav-mobile`, `data-landing-cta-hero`, `data-landing-plan-cta`) points at `/onboarding`, (7) no betting copy. **Validated:** `npm run check --prefix frontend` 0/0; `npm run test --prefix frontend -- --run` → 836/836 vitest green (was 829) across 101 files (was 100). **Discoveries:**
  - **Landing is the first chrome-less route in the K-series.** Every other K-route mounts KickerShell + MobileHeader; landing is a public marketing page so the "both shells" assertion would fail. Codify: when adding tests for any future public/marketing route (eventual `/about`, `/legal`, etc.), drop the shell assertions and test the landing-specific markers directly.
  - **Badge count is 2, not 3.** The Touchline tier intentionally ships without a badge — only Press Box ("MOST POPULAR") and Print Run ("FOR THE COMMITTED") render a `data-landing-plan-badge`. The test asserts `length === 2` to lock that contract; if a future product call adds a "FREE FOREVER" badge to Touchline, the assertion needs a one-line bump.
  - **CTA href contract is uniform — every primary CTA points at `/onboarding`.** No tier-specific routing yet (no `/onboarding?tier=press-box`, etc.). When K2c wires the paywall, the per-plan CTAs will need to carry a tier hint; the test will then need updating to expect e.g. `href="/onboarding?tier=press-box"` for the Press Box CTA. Park as a K2c sub-task.
- [x] **K1i — Rumours teaser** *(Manual-gated, ship pending sweep — 2026-05-25)* → `k1.9` — Shipped: `/rumours/+page.svelte` mounts both shells (desktop KickerShell with no `active` since `rumours` isn't in the locked NavId list — same precedent as `/roster`, `/notifications`, `/search`; mobile `active="more"`). **Desktop layout:** dark ink-on-paper COMING SOON hero (red `COMING SOON · JUNE 2026` chip + 72px serif "Transfer / *Season.*" with amber italic emphasis on the second word + italic standfirst + 3-feature grid in `rgba(255,255,255,0.06)` panels with amber labels + `NOTIFY ME WHEN LIVE →` CTA + "no spam" italic). Below the hero, a 2-line section header `PREVIEW · EARLY RUMOURS / What Macca's already watching` over a 6-row teaser `<ul>` (one `<li>` per `KICKER_RUMOURS` fixture entry) in the locked 5-col grid `[1fr_120px_120px_60px_1fr]` (player+route / fee / heat / locked-prob / Macca quote). Footer Cheers Geoff callout (`£2.1bn` / `SPENT LAST WINDOW` / free-agents riff). **Mobile layout:** per `screens-inventory.md` "desktop-only stub on mobile" — mobile shell renders only the dark hero section (compressed 44px headline) + a friendly "open `/rumours` on desktop to see Macca's early takes" hint + the same `NOTIFY ME WHEN LIVE →` CTA. No teaser table on mobile (the 5-col grid would not compress legibly). **New primitives:** `lib/components/rumours/HeatBar.svelte` (5-cell row, fills cells 1..N with `var(--red)`, empty cells in `var(--paper-inset)` with `var(--rule-strong)` borders; `data-heat-bar` / `data-heat-value` / per-cell `data-heat-cell="filled|empty"` markers) + `lib/components/rumours/LockedColumn.svelte` (centred padlock SVG + uppercase `JUNE 9` unlock label in `text-ink-faint`; `data-locked-column` / `-glyph` / `-unlock-label` markers, configurable `unlockLabel` prop with default). **New fixture:** `lib/fixtures/rumours.ts` exports typed `RumourRecord` + `KICKER_RUMOURS` (6 entries — Wirtz→Liverpool, Osimhen→Chelsea, Rice→Real Madrid, Rashford→Milan, Williams→Barca, Yoro→PSG — copy lifted verbatim from `kicker-rumours.html` Macca takes). No inline hex anywhere (everything via tokens), `NOTIFY ME WHEN LIVE` button is a `type="button"` stub (no waitlist wiring — that's K1h/K2c territory). **8 new tests** (`src/routes/rumours/page.test.ts`): SSR render, both shells mount with markers, `more` active in MobileNav with `aria-current="page"`, no desktop nav anchor active, `COMING SOON · JUNE 2026` appears exactly twice (hero × 1 + mobile stub × 1) + both hero markers, 6 rumour rows × HeatBar × LockedColumn (lookahead-anchored to dodge data-attr-prefix collisions per K0c rule) + `PROBABILITY SCORES UNLOCK JUNE 9` copy, Cheers Geoff footer with `£2.1bn` + `SPENT LAST WINDOW`, no betting copy. **Validated:** `npm run check --prefix frontend` 0/0/0 (574 files); `npm run test --prefix frontend -- --run` → 844/844 vitest green (was 836) across 102 files (was 101). **Manual sweep checklist for K1i:** (1) Visit `/rumours` on desktop → confirm the black hero panel renders with the red `COMING SOON · JUNE 2026` chip + amber `Season.` headline emphasis + the 3-feature panel grid (DEAL PROBABILITY / MACCA'S DESK / IMPACT RATING) + the `NOTIFY ME WHEN LIVE →` button + the italic no-spam line beside it. (2) Confirm the 6 rumour rows render below the hero — each with player + transfer route mono line + REPORTED FEE mono digit + 5-cell HEAT meter (Wirtz=5/5 red, Yoro=1/5 red) + locked PROB padlock with `JUNE 9` caption + a red-left-bordered MACCA SAYS italic quote. (3) Confirm the Cheers Geoff `£2.1bn / SPENT LAST WINDOW` callout lands beneath the list. (4) Confirm `PROBABILITY SCORES UNLOCK JUNE 9` mono line appears in the section header right-aligned. (5) Click `NOTIFY ME WHEN LIVE →` → confirm it does nothing yet (stub — copy says K1i ships static; waitlist is K1h/K2c). (6) Confirm the desktop sidebar has nothing highlighted (rumours has no NavId slot — same as `/roster`/`/notifications`/`/search`). (7) Resize to mobile → confirm the teaser table is GONE and a stub-hero with the compressed 44px Transfer / *Season.* headline + the "open on desktop" hint + the NOTIFY CTA render instead, MobileNav highlights `More`. (8) Switch persona via Settings → revisit → confirm the hero still uses the red/amber editorial palette (rumours is intentionally persona-agnostic — Macca's voice colours the prose, not the chrome). (9) No betting copy anywhere on either layout. **Discoveries:**
  - **Rumours is the second route that diverges between desktop and mobile bodies, not just chrome.** `/landing` is chrome-less entirely; everywhere else, desktop and mobile render the SAME `body` snippet inside different shells. Rumours is the first to ship explicitly different content per shell — the desktop teaser table relies on a 5-col grid that doesn't compress without redesign, so mobile gets a "see you on desktop" stub instead of a bad layout. Codify for any future K-route whose information density is screen-class-dependent: it's legitimate to ship a stub on the smaller layout rather than force a poor compression.
  - **HeatBar + LockedColumn live under `lib/components/rumours/` — a new component folder.** Mirrors the per-feature folder convention (`match/`, `chat/`, `roster/`, `notifications/`, `search/`, `rumours/`). Keeps the primitive co-located with its consumer; both will be reused by the live rumours surface when K1i graduates from teaser to real product (HeatBar is generic-enough to slot into any 0–5 confidence display; LockedColumn could front any paywall-locked column on the future Press Box tier).
  - **`KICKER_RUMOURS` is a static fixture, not a backend feed.** The whole slice is intentionally a teaser — there's no Football-Data endpoint for transfer rumours, no DB, no LLM call. When the June 2026 window opens (the "window-gated" qualifier in the plan), K1i's followup `K1i-β — Live rumour ingestion` would need to either (a) scrape a third-party rumour aggregator (legally fraught), (b) build a manual editorial pipeline (Macca files rumours by hand → admin form → DB), or (c) re-purpose `/api/broadsheet` to generate rumour copy from a persona prompt + a list of player names (the cheapest path). Park as a K2-adjacent decision; for MVP the static teaser is the contract.
  - **No persona-accent on the hero.** Tempted to recolour the amber `Season.` emphasis with `var(--persona-accent)` (it would match the per-persona signature seen on `/column/[slug]`). Held back because Rumours is "Macca's desk" by design (per the kicker `MACCA'S DESK` line) and the editorial red/amber palette frames the announcement as a paper-wide event, not a per-pundit column. If user feedback later asks for the recolour, swap `text-amber` → inline `style="color: var(--persona-accent);"` on the `<em>` and keep everything else.
  - **`<em class="text-amber">` over `not-italic` overrides.** Tried `class="text-amber not-italic" style="font-style: italic;"` initially — Tailwind's `not-italic` cancels the browser default. Cleaner: drop both, let `<em>` carry its native italic, and only colour with `text-amber`. Codifies a small Tailwind gotcha — don't fight the semantic element's default styling.
