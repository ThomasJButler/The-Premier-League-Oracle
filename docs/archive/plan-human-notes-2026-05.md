## Human notes for next iteration

- **2026-05-25 — K1i shipped (Rumours teaser, commit pending sweep).** `/rumours` now resolves: dark COMING SOON hero (red chip + 72px serif "Transfer / *Season.*" with amber italic emphasis + 3-feature panel grid + NOTIFY ME WHEN LIVE → CTA + no-spam line) over a 6-row teaser table (player+route / fee / heat / locked-prob / Macca quote) in the 5-col grid lifted from `kicker-rumours.html`, capped by a Cheers Geoff `£2.1bn / SPENT LAST WINDOW` footer. Mobile renders a stub-hero only (the desktop teaser table relies on a 5-col grid that doesn't compress) with a "see you on desktop" hint per `screens-inventory.md` "desktop-only stub on mobile". New primitives `HeatBar` (5-cell red meter) + `LockedColumn` (padlock + JUNE 9), new fixture `KICKER_RUMOURS` (6 entries, copy verbatim from the JSX prototype). Test count 836 → 844 (+8 in one new file `routes/rumours/page.test.ts`), svelte-check 0/0. **Sweep checklist:** see slice body above (9 items, key ones are #1 — full desktop hero + 3-feature grid renders — #7 — mobile shows stub only, not the teaser table — and #8 — persona switch doesn't recolour the editorial red/amber chrome, which is intentional per "Macca's desk" framing). **Follow-ups surfaced:**
  - **`K1i-β — Live rumour ingestion`** (post-window opening, June 2026+). The teaser is static; live rumours need either (a) third-party aggregator scraping (legally fraught), (b) manual editorial pipeline + admin form, or (c) repurpose `/api/broadsheet` with a `kind: 'rumour-table'` discriminator + persona prompt + player-name list. Park as K2-adjacent.
  - **`NOTIFY ME WHEN LIVE →` is a stub** — wires to nowhere yet. Waitlist plumbing belongs to K1h (Landing's email-capture path) or K2c (auth/paywall). When that lands, swap the `<button type="button">` for the same waitlist target the Landing page uses (likely an `/api/waitlist` POST with `{email, source: 'rumours'}`).
  - **MVP K1 phase is now complete on `Kicker-Development`.** Every K1 slice K1a–K1i is shipped (sweep-pending). The natural next step is the **K1.x tag sweep** (apply tags `k1.1` through `k1.9` at their respective HEADs as sweeps sign off), then a decision on whether to start K2 (mobile responsive pass, audio, paywall) or revisit the smaller parked follow-ups (`K1c.1` live entry point, `K1a-β.3.1` persona token instructions, `K1d-β.1` top-6 standings, `K1b-γ.1` live persona-voiced match analysis, `/more` index page now serving 5 child routes without a home).
  - **`/more` index is now load-bearing** — `/notifications`, `/search`, `/insights`, `/roster`, `/settings`, **`/rumours`** all nest under `more` on mobile. Six routes deep without an index page is a real navigation hole; recommend prioritising a tiny `/more/+page.svelte` slice (~30 LoC + 3 tests) before any K2 work.

- **2026-05-25 — K1h.1 shipped (Landing tests, commit pending sweep).** New `frontend/src/routes/landing/page.test.ts` ships 7 SSR tests against the hand-built `/landing` route: page marker, 6 features, 4 voice cards, 3 pricing tiers (with locked tier ids), 2 plan badges (MOST POPULAR + FOR THE COMMITTED — Touchline is badge-less by design), uniform `/onboarding` CTA href contract across nav + hero + per-plan buttons, no betting copy. Test count 829 → 836 (+7 in one new file), svelte-check 0/0. **Sweep checklist for K1h.1:** purely an automated slice — `npm run test --prefix frontend -- --run` should be green; no manual UI sweep needed beyond confirming the landing page still renders if a sweep is in progress. **Discoveries:** (1) Landing is the first chrome-less K-route — drops the "both shells" assertion that's standard everywhere else; codify for any future public marketing route. (2) Badge count is 2 not 3 — Touchline tier is intentionally badge-less; assertion locked at `length === 2`. (3) CTA href contract is currently uniform (every CTA → `/onboarding`); when K2c wires the paywall the per-plan CTAs will likely carry a tier hint and this test will need updating. **Next loop runs K1i — Rumours teaser** (window-gated, post-K1.0, single-page coming-soon hero + email-capture + locked rumour table with `HeatBar` + `LockedColumn` primitives, ~4 tests) UNLESS the user prefers one of the parked smaller follow-ups: `K1c.1` live entry point, `K1a-β.3.1` persona token instructions, `K1d-β.1` top-6 standings, `K1b-γ.1` live persona-voiced match analysis, or the load-bearing `/more` index page that now has 5 child routes (`/notifications`, `/search`, `/insights`, `/roster`, `/settings`) without a home.

- **2026-05-24 — K1g shipped (Roster + voice range, commit pending sweep).** Two new routes (`/roster` 5-col PunditCard grid + `/roster/voices` 5-col VoiceColumn grid driven by `KICKER_VOICE_TAKES`) plus one new primitive (`lib/components/roster/VoiceColumn.svelte`). Both pages dispatch `personaStore.set(id)` on card click — the persona switch ethos from `/settings` carried over verbatim. Desktop sidebar leaves no nav anchor active (roster has no slot in the locked 6-id `KickerShell.NavId` list — same precedent as `/notifications`/`/search`); mobile uses `active="more"`. Test count 807 → 829 (+22 across 3 new files); svelte-check 0/0. **Sweep checklist:** see slice body above (14 items, key ones are #4 — click card → instant persona switch updates sidebar Pundit-on-Duty — and #10 — same on the voice-range grid — and #11 — back-link round-trip preserves persona). **Follow-ups now next:**
  - **K1h — Landing + pricing tiers** is the natural next slice. `/landing/+page.svelte` per `kicker-landing.jsx`. Pricing tiers (Touchline £0 / Press Box £4 / Print Run £20). CTAs wire to `/onboarding` + a pre-launch waitlist. NO auth wiring (paywall is K2c). ~5 tests.
  - **K1i — Rumours teaser** sits after K1h (window-gated, post-K1.0).
  - Smaller follow-ups still parked: `K1c.1` live entry point, `K1a-β.3.1` persona token instructions, `K1d-β.1` top-6 standings, `K1b-γ.1` live persona-voiced match analysis.
  - **`/more` index** still ungrouped — when it lands (K1f-adjacent), list `/notifications`, `/search`, `/insights`, `/roster` (+ optionally a deep-link to `/roster/voices`), `/settings`. Now five routes deep without a home, so this is becoming load-bearing.

- **2026-05-24 — K1f-β.2 shipped (full-text index + SearchResultRow, commit pending sweep).** `/search` now returns real ranked results across fixtures (Arsenal v Liverpool → `/fixtures/{id}`), top scorers (no href until K1g), the 33-season archive (`/insights/archive?season=…`), and Oracle threads (`/oracle?thread={id}`). New modules: `lib/search/buildIndex.ts` (pure typed index + scoring), `lib/search/loadSearchSources.ts` (async glue with injectable deps), `lib/components/search/SearchResultRow.svelte` (typed variants — `<a>` when href set, `<div>` for player rows). Scoring is exact > label-prefix > token-exact > token-prefix > token-includes with a hard AND-gate so `arsenal liverpool` correctly collapses to one fixture. Index caches at module scope so chip picks + persona switches re-use the same data, and a missing API key degrades gracefully (seasons + threads still surface). Test count 788 → 804 (+16 across 3 files: `buildIndex.test.ts`, `SearchResultRow.test.ts`, `routes/search/page.test.ts` description bump + 1 negative assertion). svelte-check 0/0. **Sweep checklist:** see slice body above (13 items, key ones are #2 — fixture/season/thread/player kinds all surface for `arsenal` — and #7 — multi-token AND-gate must hide single-token hits — and #10 — module cache must avoid re-fetching). **Follow-ups surfaced:**
  - `K1f-β.3 — Oracle thread deep-link` (~10 LoC + 2 tests). `/oracle?thread={id}` from search results needs `setActive` wiring on mount.
  - K1g — Roster + voice range remains the natural next big slice. ~6 tests.
  - Smaller follow-ups still parked: `K1c.1` live entry point, `K1a-β.3.1` persona token instructions, `K1d-β.1` top-6 standings, `K1b-γ.1` live persona-voiced match analysis.

- **2026-05-24 — K1f-β.1 shipped (`/search` route shell + SearchInput + RecentChips + recentSearches store, commit pending sweep).** `/search` resolves: a SearchInput pinned under the Rule, an empty-state explaining the full-text index lands in β.2, and a recent-chips row that hydrates from `kicker:recentSearches`. Submitting a query writes to recent (case-insensitive dedupe + move-to-front) and flips the empty-state to a `“{query}”` confirmation — the recall path (submit → chip → re-submit) works end-to-end. New store `lib/stores/recentSearches.ts` mirrors `notificationsFeed.ts`'s SSR-safe + schema-tolerant pattern. New primitives `lib/components/search/{SearchInput,RecentChips}.svelte` — SearchInput uses Svelte 5 `$bindable` for the input value, RecentChips uses sibling `<button>`s for pick + remove (not nested, mirroring `ThreadListRail`). Test count 759 → 788 (+29 across 4 new files: `recentSearches.test.ts`, `SearchInput.test.ts`, `RecentChips.test.ts`, `routes/search/page.test.ts`). svelte-check 0/0. **Sweep checklist:** see slice body above (14 items, key ones are #7/#8 — chip pick + ALL-CAPS resubmit must dedupe case-insensitively — and #11 — recent chips must persist across reload). **Discoveries:**
  1. **`disabled` substring footgun.** Tailwind's `disabled:opacity-40` class contains the literal word `disabled` — `toContain('disabled')` matches the class, not the boolean attribute. Fix: regex `/\sdisabled(=""|>|\s)/` against the actual HTML attribute form.
  2. **`$bindable` defaults keep standalone SSR rendering testable** — `let { value = $bindable('') }` lets tests pass `value: 'arsenal'` directly without a parent bind.
  3. **`/search` joins `/notifications` + `/insights` as routes with NO desktop nav slot** — when the `/more` index lands (K1f-adjacent), it lists `/search`, `/notifications`, `/insights`, `/settings`, `/roster`.
  4. **β.1 stops short of `SearchResultRow` by design.** The bare query-echo stub proves the recall path; β.2 introduces the typed row variant only when the index has shapes to feed it. **Next loop runs K1f-β.2 — Full-text index + SearchResultRow** (covers fixtures via `dataService.getCurrentSeasonMatches`, seasons from `leagueHistory`, threads from `threadsStore`, optionally players via `dataService.getTopScorers`; ~12 tests) UNLESS the user prefers one of the smaller follow-ups (`K1c.1` live entry point, `K1a-β.3.1` persona token instructions, `K1d-β.1` top-6 standings, `K1b-γ.1` live persona-voiced match analysis).

- **2026-05-24 — K1f-α.1 shipped (broadsheet-ready notification producer wired, commit pending sweep).** New `lib/broadsheet/notifyBroadsheetReady.ts` helper (~12 LoC) composes the inbox shape (`type: 'broadsheet-ready'`, title `GW{N} broadsheet filed by {persona.name}`, body = headline, href `/broadsheet`); `/broadsheet`'s `generate()` calls it immediately after a successful `writeBroadsheet`. Each successful generation (initial + each Regenerate) gets its own inbox entry, newest-first, capped at MAX_ITEMS=50. Test count 757 → 759 (+2 across new `notifyBroadsheetReady.test.ts`), svelte-check 0/0. **Sweep checklist:** see slice body above (4 items, key one is #1 — generate a real broadsheet end-to-end and confirm the entry lands in `/notifications` with persona-voiced title + headline body). **Discoveries:** (1) Pure helper over a fetch-wrapper — keeps inbox-write decoupled from API-client error semantics, so future producers (match-start, model-edge) compose alongside without inheriting failure shapes; (2) No dedupe by design — each generation is its own event; if that proves noisy, K1f-β filtered inbox views can collapse them per `(gw, persona)`. **Next loop runs K1f-β — Search advanced** (the natural next big slice — `/search/+page.svelte` + client-side full-text index over fixtures/players/seasons/threads, ~10 tests) UNLESS the user prefers one of the smaller follow-ups (`K1c.1` live entry point, `K1a-β.3.1` persona token instructions, `K1d-β.1` top-6 standings, `K1b-γ.1` live persona-voiced match analysis).

- **2026-05-24 — K1f-α shipped (`/notifications` inbox + feed store + NotificationItem, commit pending sweep).** `/notifications` resolves: an empty-state inbox by default (the feed has no engine producer yet — honest about that in the copy), with a `THE WIRE · Notifications` Rule header, `Mark all read` / `Clear all` actions when items exist, and a `Manage in Settings →` footer link to `/settings#notifications`. New store `lib/stores/notificationsFeed.ts` is SSR-safe + schema-validated; the validator explicitly rejects the legacy `value-bet` type so the betting-removal lock is enforced at the data layer. New primitive `lib/components/notifications/NotificationItem.svelte` is a `<button>` (not `<a>`) so `markRead` fires before optional navigation. New util `lib/utils/relativeTime.ts` formats "just now / Nm ago / Nh ago / Yesterday / Nd ago / DD MMM" — `Intl`-free for the short bands. Test count 730 → 757 (+27 across 4 new files), svelte-check 0/0. **Sweep checklist:** see slice body above (12 items, key ones are #3 — manually inject 3 items via `localStorage.setItem('kicker:notificationsFeed', ...)` and confirm they render with correct labels + relative times + unread dots — and #7 — confirm the validator filters out `value-bet`-typed items, proving the betting-removal contract). **Follow-ups surfaced:**
  - `K1f-α.1 — Wire broadsheet-ready producer` (~10 LoC + 2 tests). The cheapest first producer: on `requestBroadsheet` success in `/broadsheet`, call `addNotification({type: 'broadsheet-ready', href: '/broadsheet', ...})` so the inbox populates from an existing surface.
  - `K1f-β — Search advanced` is the natural next slice — `/search/+page.svelte` + client-side full-text index over fixtures/players/seasons/threads, ~10 tests.
- **Discovery — `/notifications` has no desktop nav slot.** KickerShell's `NavId` doesn't include `notifications`. α leaves desktop `active` unset (optional prop) and uses mobile `active="more"`. Codify for all future sub-`/more` routes (`/roster`, the eventual `/more` index). Adding a slot to KickerShell's nav would be a global change and isn't necessary for MVP.
- **Discovery — `<button>` over `<a>` for click-then-navigate semantics.** When a row needs to do something (markRead) before navigating, and may be href-less, `<button>` + optional `window.location.assign` is cleaner than `<a onclick="preventDefault(); …">`. Codify for any future inbox / activity-feed primitive.
- **Discovery — `value-bet` type filter as a contract test.** The store's schema validator rejects `value-bet`, and the test asserts that explicitly. Apply this pattern to any future enum-typed cache schema where a deprecated value would be unsafe to silently re-introduce.
- **Next loop runs K1f-α.1 (broadsheet-ready producer wire — cheapest path to a non-empty inbox)** UNLESS the user prefers to jump to K1f-β (Search advanced — the full-text index slice) or one of the smaller follow-ups (`K1c.1` live entry point, `K1a-β.3.1` persona token instructions, `K1d-β.1` top-6 standings, `K1b-γ.1` live persona-voiced match analysis).

- **2026-05-24 — K1e-γ shipped (season verdict generation, commit pending sweep).** `/api/broadsheet` now accepts an optional `kind` discriminator (default `'broadsheet'` so K1e-β callers stay untouched); the new `kind: 'season-verdict'` variant takes `{personaId, season}` and returns a one-paragraph persona-voiced verdict. `SeasonDetail.svelte` gains an optional `onGenerate`/`generating`/`generateError` prop trio that drives a "Generate verdict" / "Regenerate" / "Filing copy…" CTA — `/insights/archive` wires the handler, calls `writeVerdict()` on success, and bumps a `verdictTick = $state(0)` so the existing `$derived.by(readVerdict)` re-runs. New client helper `lib/broadsheet/requestSeasonVerdict.ts` mirrors `requestBroadsheet`'s discriminated `{ok, verdict|error}` shape. Test count 715 → 730 (+15 across server.test.ts, new requestSeasonVerdict.test.ts, SeasonDetail.test.ts, and the archive page test). svelte-check 0/0. **Sweep checklist:** see slice body above (11 items, key ones are #3 — first Generate should produce a coherent persona-voiced paragraph within ~10s and persist to `localStorage.kicker:verdict:{season}:{personaId}` — and #5 — persona switch must re-read the per-persona cache, not re-fire the LLM). **Discoveries:** (1) `$derived.by` over a non-reactive source like `localStorage` needs an explicit reactive dep — the tick pattern (`$state(0)` + `tick++` after writes) is the canonical fix; (2) Backwards-compat via *optional* discriminator — `kind?: string` defaulting to `'broadsheet'` server-side keeps every K1e-β caller working without a payload migration; (3) Plain prose vs JSON — verdicts are one paragraph so the server returns `{verdict, generatedAt}` directly with an `empty_verdict` guard, no parse step; (4) Season facts resolved server-side from bundled `leagueHistory` + `statsPack` rather than trusting the client payload; (5) `writeVerdict` is no longer dormant — γ closes the K1d-β-to-K1e loop. **Next loop runs K1f — Notifications + Search advanced** (or one of the smaller follow-ups: `K1c.1` live entry point, `K1a-β.3.1` persona token instructions, `K1d-β.1` top-6 standings, `K1b-γ.1` live persona-voiced match analysis). K1f is the natural next big slice — `/notifications` builds NotificationItem + reuses PrefToggle from K0l-β, `/search` builds the client-side full-text index over fixtures/players/seasons/threads, ~14 tests total.

- **2026-05-24 — K1e-β shipped (`/broadsheet` route + cache + JSON rendering, commit pending sweep).** `/broadsheet` now resolves: when there's no cache for the active (GW, persona) pair, you see "No broadsheet on file for Gameweek {N} from {persona.name}" + a Generate CTA; click Generate → one-shot Sonnet 4.5 call → full broadsheet renders (headline / standfirst / byline+generated-at / section blocks / pullQuote / closingLine) → cached at `kicker:broadsheet:gw{N}:{personaId}` for instant re-render on subsequent visits. A Regenerate button on the cached state busts the entry + re-fires. Persona switch re-reads the cache for the new voice (no LLM call). Errors land inline as `⚠ {code}` (rate_limited, anthropic_unavailable, malformed_response, broadsheet_parse_failed). Test count 694 → 715 (+21 across 3 new files: `broadsheetStore.test.ts`, `requestBroadsheet.test.ts`, `routes/broadsheet/page.test.ts`), svelte-check 0/0. **Sweep checklist:** see slice body above (11 items, key ones are #3 — first Generate should produce a coherent persona-voiced broadsheet within ~30s — and #5 — persona switch must re-read cache, NOT re-hit the LLM). **Follow-up surfaced:** the original β scope also said "wire the K1d-β follow-up: extend `/api/broadsheet` to accept `{kind: 'season-verdict', season, personaId}` so `SeasonDetail` can ship a 'Generate verdict' CTA". That's its own slice — endpoint shape change + new prompt variant in `broadsheetPrompt.ts` + SeasonDetail UI change + verdict-write wiring + tests. Tracked as `K1e-γ — Season verdict generation` below. **Discovery — `$state` can't reference props in its initializer (Svelte 5).** The compiler emits `state_referenced_locally` because props are reactive but `$state` snapshots its init value. Canonical fix: split into `let resolved = $state(null)` for the client-side override + `const final = $derived(resolved ?? data?.x ?? null)` to unify. Codify for future routes that want to seed state from `+page.ts` data — used here for `gameweek` and applicable to any K1f search-results route etc. **Discovery — schema-validate the cache payload, not just the JSON parse.** The broadsheet cache rejects entries where `broadsheet.sections` isn't an array. Means a partial-migration or hand-edited cache entry won't crash the route — it falls back to the Generate CTA. Apply to every future LLM-payload cache. **Discovery — generated-at lives in the server payload.** `/api/broadsheet` returns its own `generatedAt`; the cache honours that timestamp rather than `new Date()`. Makes cross-device sync trivial if the cache ever moves off localStorage. **Next loop runs K1e-γ — Season verdict generation** (extend `/api/broadsheet` + SeasonDetail CTA + verdict-write path — depends on K1e-β shape that just shipped) UNLESS the user prefers to skip ahead to K1f (Notifications + Search) or one of the smaller follow-ups (K1c.1 live entry point, K1a-β.3.1 persona token instructions, K1d-β.1 top-6 standings).

- **2026-05-24 — K1e-α shipped (`/column/[slug]` static-content route + ColumnHero + PullQuote, commit pending sweep).** `/column/state-of-arsenal`, `/column/mickey-on-united`, `/column/macca-on-anfield` now resolve to a full editorial column: eyebrow → 54-64px serif headline with inline persona-accent `<em>` → byline strip (monogram + name + sub + read-time + dateline) → drop-cap intro → red-top-rule pull quote → 2-col body grid → optional Cheers Geoff foot. 404 fallback for unknown slugs. Test count 671 → 694 (+23 across 4 new files: `ColumnHero.test.ts`, `PullQuote.test.ts`, `fixtures/columns.test.ts`, `routes/column/[slug]/page.test.ts`), svelte-check 0/0. **Sweep checklist:** see slice body above (12 items, key ones are #2 — persona switch should recolour `<em>` + monogram + drop-cap — and #8 — multi-emphasis headlines render correctly). **K1e split rationale:** the original K1e bundled column + broadsheet + `/api/broadsheet` cache + JSON template + ~12 tests — too big for one loop. α ships the static-content route + primitives (no LLM dependency); β builds the broadsheet route + LLM wiring + cache against the same `ColumnRecord` shape. **Follow-up surfaced:** the spec recommends collapsing `PullQuote` + `HotTake` + `PunditQuoteBlock` into a single component with a `variant?: 'left-bar' | 'red-top' | 'centred'` prop. β is the natural slice to evaluate — Mickey edition uses HotTake (amber-left), Gaffer uses centred italic (no border at all), PullQuote uses red-top. May need 3 variants, not 2. **Discovery — `{@const}` block-tag-immediate-child rule:** Svelte 5 rejects `{@const}` inside arbitrary elements like `<article>`. Must be immediate child of `{#if}`/`{#each}`/`{#snippet}` etc. Hoist into `$derived` at script level when blocked. **Discovery — Svelte 5 SSR decodes HTML entities:** `&ldquo;` in source emits `"` (U+201C) in SSR body. Assert on the unicode codepoint. **Discovery — `HeadlineSegment[]` over `{@html}`:** segment-array approach is XSS-safe by construction and trivially testable. Codify for any LLM-emitted markup that needs targeted highlighting — emit a JSON segment array, not an HTML string. **Next loop runs K1e-β** — `/broadsheet/+page.svelte` triggers `/api/broadsheet`, renders JSON template per persona-edition, caches in localStorage at `kicker:broadsheet:gw{N}:{personaId}`. β should also wire the K1d-β follow-up (extend `/api/broadsheet` for season-verdict + "Generate verdict" CTA on `SeasonDetail`).

- **2026-05-24 — K1d-β shipped (`/insights/archive` + verdict cache, commit pending sweep).** Archive route lives at `/insights/archive`: a 33-row season rail (newest-first, starts on `2025/26`) over a 2-col layout with `<SeasonDetail>` showing champion + runner-up + a 4-tile rate grid (matches / goals-per-match / home win rate / BTTS) + inline anomaly block for outlier seasons + a verdict block fronted by the active persona's name. Selecting a row writes `?season=YYYY-YY` to the URL via `history.replaceState` and best-effort warms `dataService.getHistoricalMatches(seasonStartYear)` so K1b-γ's cross-season H2H tab gets the data cached. Verdict store at `lib/stores/verdictsStore.ts` (key `kicker:verdict:{season}:{personaId}`, SSR-safe, schema-validated on read) — `writeVerdict` is exported + tested but not yet user-callable; K1e wires it. Verdict block falls back to "No verdict on file yet. {personaName} hasn't filed copy for this season" italic when cache cold. `/insights` teaser footer now links to `/insights/archive` (no longer a dead "coming in K1d-β" hint). Test count 639 → 671 (+32 across 5 new files: `leagueHistory.test.ts`, `verdictsStore.test.ts`, `SeasonRow.test.ts`, `SeasonDetail.test.ts`, `routes/insights/archive/page.test.ts`), svelte-check 0/0. **Sweep checklist:** see slice body above (11 items, key ones are #5 — manually inject a cached verdict and confirm it surfaces — and #10 — confirm the silent dataService warm-up fires on row click). **Follow-ups surfaced:**
  - `K1d-β.1 — Derive full top-6 standings on demand` (~80 LoC + 4 tests). The static fixture only bundles champion + runner-up; full top-6 needs a match→standings reducer over `dataService.getHistoricalMatches()`. Cheap to wire alongside K1e once verdict generation populates cache, since both depend on the same per-season warm.
  - `K1d-β.2 — Verdict write path (CTA + /api/broadsheet extension)` is K1e proper — `/api/broadsheet` needs to accept `{kind: 'season-verdict', season, personaId}` and `SeasonDetail` needs a "Generate verdict" button when cache is cold. Park until K1e starts.
  - **Decision deferred:** the warm-up currently fires for `seasonStartYear(activeSeason)` only — not for all 33 seasons. If user feedback flags that cross-season H2H still feels sparse on `/fixtures/{id}#h2h`, the next compromise is to opportunistically warm the *neighbouring* 2–3 seasons (still bounded, still cheap on quota). Park until K1b-γ sweep flags it.
  - **Discovery — `statsPack` covers `seasonStats[season]` keyed by string** (e.g. `"2003/04"`); the rate grid in `SeasonDetail` reads directly without aggregation. Codified: prefer bundled `statsPack` over live API for any per-season aggregate, mirroring K1d-α's same call. **Next loop runs K1e — Column + Broadsheet** unless the user opts for the `K1d-β.1` top-6 derivation or the `K1c.1` live-entry-point follow-up first.

- **2026-05-24 — K1d-α shipped (insights main route, commit pending sweep).** `/insights` now resolves: top-scorers live list (loading → populated or empty-state fallback), 6-tile season-stats grid driven by bundled `statsPack.leagueEra`, season-anomalies timeline from `statsPack.anomalies[]`. Test count 624 → 639 (+15 across 4 new files: `TopScorerRow.test.ts`, `StatTile.test.ts`, `SeasonAnomalyRow.test.ts`, `routes/insights/page.test.ts`), svelte-check 0/0. **Sweep checklist:** see slice body above (7 items, key one is verifying the empty-state appears when the Football-Data key is wiped). **K1d split rationale:** the original K1d slice covered both `/insights` AND `/insights/archive` + persona-voiced verdict — too big for one loop. α ships the read-only main screen (no LLM dependency); β builds the 33-season archive on top, which is the natural slice to also wire `dataService.getHistoricalMatches()` warm-up for K1b-γ's cross-season H2H. **Follow-up surfaced:** `/insights` is reachable from the desktop nav (`insights` was already in `KickerShell`'s `NAV`), but on mobile users land on `/insights` via the `More` tab — which doesn't exist as a route yet. When the `/more` route ships (K1f-adjacent), expose `/insights` + `/settings` + `/roster` + `/notifications` as a list. **Discovery — `statsPack` is the right surface for historical aggregates.** Zero-runtime, immune to API outages, ships with the bundle. Codify: prefer `statsPack` over live API for any season-baseline numbers in future K-slices. **Next loop runs K1d-β** — `/insights/archive` (33-season list rail + `SeasonRow`/`SeasonDetail` + persona-voiced verdict from `/api/broadsheet` cache). β should also warm `dataService.getHistoricalMatches()` so K1b-γ's H2H tab gets cross-season meetings for free.

- **2026-05-24 — K1c shipped (live route + LiveScoreboard + MatchEventRow + LiveCommentaryItem + mocked liveFeed, commit pending sweep).** `/fixtures/{id}/live` now resolves: a pulsing red LIVE scoreboard over a 2-col layout of match events + commentary. Test count 608 → 624 (+16 across 5 new files: `liveFeed.test.ts`, `LiveScoreboard.test.ts`, `MatchEventRow.test.ts`, `LiveCommentaryItem.test.ts`, `routes/fixtures/[id]/live/page.test.ts`), svelte-check 0/0. **Sweep checklist:** see slice body above (8 items, key one is the OS-level reduced-motion verification of both pulse + commentary fade-in). **Follow-up surfaced:** `K1c.1 — Live entry point` (~10 LoC + 2 tests) — the live route is currently URL-only; a `[LIVE →]` chip on `MatchHero` (when status === 'LIVE') + a "View live" link on `/fixtures` rows would make it discoverable. Park until sweep confirms the scoreboard reads correctly. **Discovery — feed is mocked + status is forced:** `getLiveFeed` is deterministic-but-fake; the route synthesises `{status: 'LIVE', minute: 67, score: {2,1}}` so the scoreboard always lights up regardless of the underlying fixture's real status. When a real feed lands, drop the `asLiveFixture()` synth + pull minute/score from the feed. **Discovery — `satisfies LiveEvent[]` over `: LiveEvent[]`** — inline literal arrays with `side: 'home'` / `type: 'GOAL'` positions get widened to `string` by a bare annotation; `satisfies` validates without widening. Codify for any future K-slice that builds fixture/event/seed data arrays. **Next loop runs K1d** — Insights + Archive read-only (top-scorers + season-stats grid + season-story timeline + 33-season archive with persona-voiced verdict from `/api/broadsheet` cache). K1d also unblocks the deferred cross-season H2H from K1b-γ (`dataService.getHistoricalMatches()` warm-up).

- **2026-05-24 — K1b-γ shipped (section tabs + form + h2h + analysis, commit pending sweep).** `/fixtures/{id}` now ships the full match-detail experience: hero + 5-tab section bar (Analysis · Probabilities · Form · H2H · Venue) with hash-routing + persona-attributed analysis quote. Test count 600 → 608 (+8 across 3 new files: `SectionTabs.test.ts`, `FormChips.test.ts`, `h2h.test.ts`), svelte-check 0/0. **Sweep checklist:** see slice body above (11 items). **Follow-up surfaced:** `K1b-γ.1 — Live persona-voiced match analysis` — the analysis tab currently uses the engine's `keyFactors` as the body, attributed to the active persona. Wiring real per-fixture LLM analysis needs either extending `/api/broadsheet` to accept a fixture context or introducing `/api/analysis`, plus a `kicker:analysis:{fixtureId}:{personaId}` cache. Cheap (~50 LoC + 4 tests) once K1e has set the broadsheet endpoint shape. **Decision deferred:** historical (cross-season) H2H — for γ the H2H tab only reads the current-season slate, so most fixtures show 0–1 prior meetings. Folding in `dataService.getHistoricalMatches()` is K1d (Archive) territory since the same warm-up cost benefits both screens. **Discovery — `SectionTabs` is now a reusable primitive.** Generic over `Id extends string` — `/settings` could replace its bespoke `kicker-subnav__row` rendering with `SectionTabs` for free, dropping ~50 lines of one-off CSS. Worth a tidy slice once γ is signed off. **Next loop runs K1c — Live + scoreboard** unless the user opts to migrate `/settings` onto `SectionTabs` first.

- **2026-05-24 — K1b-β shipped (form arrays + EnsembleBars + ScorelineBars, commit pending sweep).** `/fixtures/{id}` now renders the predictions moat: below the hero, a two-column grid (`lg:grid-cols-2`) of `<EnsembleBars>` (4–5 model rows with lean + confidence) and `<ScorelineBars>` (top-7 Poisson grid cells with relative-width bars + absolute pcts). `matchToFixture(found, matches)` now populates `home.formLast5` + `away.formLast5` (newest-first W/D/L from completed prior matches), unlocking γ's form chips. Test count 573 → 600 (+27), svelte-check 0/0. **Sweep checklist for β:** (1) Hit any `/fixtures/{id}` → confirm the two new bar panels render below the hero. EnsembleBars should show 4 rows (ELO/POISSON/FORM/H2H) when the ML backend is OFF (default for fresh users without a backend running). If the ML backend IS reachable, a 5th XGBOOST row should appear tagged with the ensemble pick. (2) Confirm each model row reads `MODEL · lean (HOME-ABBR / AWAY-ABBR / DRAW) · bar · pct%` and the bar fill colour comes from `var(--persona-accent)` — switch persona via Settings, revisit `/fixtures/{id}`, confirm bar colour changes. (3) Confirm ScorelineBars top row's bar fills the full track (it's the modal scoreline, so relative-width = 100%) and subsequent rows shrink proportionally — pct labels stay absolute (e.g. 12% / 11% / 9% / …). (4) Pick a fixture for two known-mismatch teams (Arsenal vs a bottom-6 side) and confirm a 3-x scoreline shows up in the grid (validates the opposition-tier blend is firing through to the grid). (5) Open devtools network → confirm `/fixtures/{id}` does NOT trigger more API calls than `/fixtures` itself did (matches come from the cache populated by `getCurrentSeasonMatches`, no per-fixture refetch). (6) For a fixture with very thin history (early in season or relegated/promoted club), confirm the "Model warming up" fallback rule appears instead of empty bars — this is the documented degradation path. **Discovery — `predictMatch` is async + does Promise.all + can hit the ML backend.** On the match-detail route the prediction is awaited *after* the matches resolve, so first paint shows the hero immediately then the bars pop in ~100ms later (or seconds, if the ML backend is reachable and slow). If perceived latency becomes an issue, the route could render the bars in a Suspense-style stub block with `data-match-detail-prediction-loading` and swap on resolve. Not done in β to keep the slice tight — revisit in γ alongside the section tabs. **Discovery — XGBOOST row uses the ensemble's final pick + confidence, not the ML triple.** `EnhancedPredictionModel.modelOutputs` carries triples for elo/poisson/form/h2h/standings but NOT for ML — the ML prediction is consumed inside `combineModels` and only its top-level `prediction` (`{home, draw, away}`) + `confidence` are kept on the `MLPrediction` object, which isn't round-tripped onto the returned `EnhancedPredictionModel`. To render an honest XGBOOST row driven by its own triple, `OptimizedPredictor.predictMatch` would need to surface `mlPrediction` alongside `modelOutputs`. Cheap to add (~3 LoC + plumbing) but it's an engine signature change — park as a γ-or-later follow-up. **Discovery — `modelOutputs.standings` row is dropped.** v3 redesign's `ModelBreakdown.name` is restricted to `'ELO' | 'POISSON' | 'FORM' | 'H2H' | 'XGBOOST'` — no STANDINGS lane. The standings contribution still flows into the reconstructed ensemble triple, but the user only sees 4 rows by default. If the user wants STANDINGS surfaced, add it to the union in `types/redesign.ts` and the `LEAN_NAMES` array in `v3.ts`. **Decision deferred to γ:** the bars currently render in a `lg:grid-cols-2` block immediately under the hero. γ's section tabs (analysis / probabilities / form / h2h / venue) will reparent ScorelineBars and EnsembleBars into the "probabilities" tab. Recommend keeping the bar primitives as-is and only swapping their parent container in γ — no internal changes needed.

- **2026-05-24 — K1b-α shipped (match-detail route + MatchHero, commit pending sweep).** `/fixtures/{id}` is no longer a 404 — every `MatchRow` click on `/fixtures` now lands on a real hero page. Test count 558 → 573, svelte-check 0/0. K1b too large for one slice (route + hero + adapter patch + EnsembleBars + ScorelineBars + section tabs + PunditQuoteBlock + ~14 tests), so split into α/β/γ — α ships the route + hero; β does the adapter+bars; γ does the tabs. **Sweep checklist for K1b-α:** see the slice body above (6 items). **Next loop runs K1b-β**: patch `matchToFixture` to populate `formLast5` arrays (needs a `deriveFormLast5(matches, teamName, beforeDate)` helper) + build `EnsembleBars` (5-row grid driven by `MatchPrediction.models[]`) + build `ScorelineBars` (top-N from `prediction.topScorelines[]`) + thread `optimizedPredictions.predictMatch()` into the route so MatchHero's pick/conf footer + bars get real data. **Discovery — adapter signature change is mildly breaking.** Existing `matchToFixture(m)` callers (Today, Fixtures, Oracle right-rail) won't pass history; the patch must default the second arg to `undefined` so `formLast5` is simply omitted in those callsites. **Decision deferred to γ:** whether section tabs go via URL hash (`#analysis` etc., mirroring `/settings`) or local component state. URL hash is friendlier for sharing a fixture's H2H tab, but introduces SSR-vs-client-hydration tab flicker that hash-routed Settings dodges only because the default panel is always rendered first. Recommend hash + SSR-default `analysis` panel — same pattern as `/settings`.

- **2026-05-23 — K1a-β.4 shipped (left + right rails, commit pending sweep).** `/oracle` desktop layout is now 3 columns: thread list + watchlist on the left, chat in the middle, today's fixtures on the right. Mobile is untouched — still a single column with header + chat body + bottom nav. **Sweep checklist for β.4:** (1) Hit `/oracle` cold (no threads yet) → confirm the left rail shows the empty hint and the right rail shows "Loading the slate…" briefly before hydrating to fixture cards (or "No upcoming fixtures…" if your Football-Data key is missing/quota'd). (2) Send a message via the composer → confirm the new thread appears in the left rail with the user's first message as the title and the assistant's reply (still streaming) as the preview line. (3) Hit `+ New` → confirm a fresh "New thread" row appears and becomes active (left border lights up in `var(--persona-accent)`). (4) Click a non-active thread row → confirm the centre column rewinds to that thread's history. (5) Click the `×` delete affordance on a non-active row → confirm it disappears without switching the active thread; delete the active row → confirm the most-recently-updated remaining thread becomes active. (6) Switch persona via Settings → confirm the active-thread left border colour changes. (7) Resize to mobile (`<lg`) → confirm rails disappear entirely and only the mono-column chat is visible (data-oracle-layout should not appear on mobile). (8) Right-rail fixture cards: confirm each card shows kickoff in `Europe/London`, abbr matches first 3 chars uppercase of team name, and the win-probability bar reads home/draw/away — same MatchSheetCard contract as `/today` (placeholder probabilities 40/30/30 for now; real per-fixture probs land at K1b alongside `EnsembleBars`). **Discovery — `TodayFixtureRail` uses placeholder probabilities (40/30/30):** the rail leans on `MatchSheetCard`'s existing required `probH/probD/probA` props but doesn't run any per-fixture prediction yet. K1b is the natural slice to wire per-fixture `optimizedPredictions` into the rail (or extract a shared `matchToCardProps` helper from `/today` and `/oracle` rail). For MVP the cards are visually correct but the prob bar doesn't carry information — acceptable since the rail is "what's coming up" not "what the model thinks". **Discovery — `MatchSheetCard` is wide:** at 320px the rail's MatchSheetCard renders fine but the `VERSUS` block compresses noticeably. If user sweep flags it as too cramped, options are (a) drop rail width to 280px and tighten card padding, or (b) introduce a `variant?: 'full' | 'rail'` prop on MatchSheetCard. Park until sweep feedback lands. **Discovery — `data-thread-rail` collides with `data-thread-rail-new` under `\b`:** the page test uses the lookahead pattern `/data-thread-rail(?![-\w])/` to count only the parent marker, codifying the K0c "data-attr-prefix collision" rule again. **Decision deferred:** the left rail's watchlist is a placeholder shape with no interactivity — when K1f search ships, `WatchlistRail` will need a real data backing (probably a new `lib/stores/watchlist.ts` mirroring `threadsStore`'s localStorage idiom). The marker `data-watchlist-rail` is intentionally stable so K1f can replace the body without renaming the rail.

- **2026-05-23 — K1a-β.3 shipped (inline-token parsing, commit pending sweep).** Assistant messages on `/oracle` now understand two inline tokens. **Sweep checklist for β.3:** (1) Manually inject a test thread into devtools — `JSON.parse(localStorage.getItem('kicker:threads'))` — and edit one assistant message's `content` to include `[[CHEERS:42%|XG OVERPERFORMANCE|Saturday's miracle compresses to one daft number.]]` then reload; confirm the CHEERS callout renders inline in the thread with the amber-dashed border + stat/label/text exactly. (2) Same flow with `[[FIXTURE:ARS-LIV]]` (or any pair matching a real upcoming Football-Data fixture — `ARS-CHE`, `MCI-LIV`, etc., depending on the actual gameweek); confirm the chip shows `ARS v LIV` immediately on SSR and decorates with the kickoff time after hydration. (3) Use an obviously-nonexistent pair like `[[FIXTURE:XYZ-ZZZ]]` and confirm the chip falls back to `· no upcoming match` after the lookup resolves (not before — the SSR render shows just the pair). (4) Send a real chat through the composer that happens to make Claude emit a token (the system prompt doesn't yet teach Claude these tokens — that's a follow-up; for now manual injection is the only path). (5) Test malformed cases: `[[FIXTURE:ARS]]` (no dash) and `[[CHEERS:only|two]]` should both render as verbatim source text in the message, not as broken UI. **Discovery — system prompt doesn't teach Claude these tokens yet:** the parser is wired, but `buildSystemPrompt` doesn't yet instruct any persona to emit `[[CHEERS:…]]` or `[[FIXTURE:…]]`, so without a content update on the server side, Claude will rarely (if ever) produce them organically. Recommend a tiny follow-up slice — call it **K1a-β.3.1** — that adds a "When you cite a statistic that's gloriously useless, wrap it as `[[CHEERS:stat|label|text]]`. When you reference a specific upcoming fixture, wrap it as `[[FIXTURE:HOMETLA-AWAYTLA]]`." block to `src/lib/server/buildSystemPrompt.ts` and a couple of integration tests asserting the instructions land in the persona prompt. Cheap (≤10 LoC + 2 tests) and unlocks the feature for real users. **Discovery — `FixtureChip` cache is module-level + process-wide:** great for dedup within a session, but it's never invalidated when `dataService` refreshes underlying matches. If the user keeps `/oracle` open for hours, stale fixture data could surface. Acceptable for MVP (kickoff times don't move that often) but worth a TTL pass alongside K2 polish. **Decision deferred:** the FixtureChip currently renders inline (`<span>`) for visual coherence inside a sentence — if β.4's right-rail surfaces the same pair as a `MatchSheetCard`, we may want a richer block-level variant of `FixtureChip` for messages that are explicitly "let's look at this match", with a `variant?: 'inline' | 'card'` prop. Park until β.4 lands.

- **2026-05-23 — K1a-β.2 shipped (composer + SSE, commit pending sweep).** `/oracle` is now live. The static SEED is gone; on first SSR/empty state you'll see a single italic hint per shell ("Right then — ask {persona.name} anything…"). Type into the composer + hit Send (or click any of the 4 prompt chips → that pre-fills the input, you still hit Send) → user message renders right-aligned, assistant placeholder bubble appears immediately, then text streams in token-by-token. On `[DONE]` the `streaming` flag is cleared. **Manual sweep checklist:** (1) Send first message → confirm a new thread is created (`localStorage.getItem('kicker:threads')` shows one thread with 2 messages + title derived from your text); (2) Send second message in same session → confirm `messages.length` grows to 4 (no new thread); (3) Hard-reload → confirm thread + messages survive and the route renders them on hydration; (4) Pull the network plug mid-stream (or just turn off wifi before sending) → confirm an inline `⚠ network_error` / `⚠ Failed to fetch` appears in the assistant bubble instead of a silent failure or toast; (5) Burn through the rate limit (11 sends in <60s) → confirm an inline `⚠ rate_limited` chip lands in the placeholder; (6) Switch persona mid-thread via Settings → next reply should come back stylistically different (system prompt rebuilds per request). **Discovery — SEED removal cascade:** dropping the SEED removed the `data-geoff-message` / `data-user-message` SSR assertions; the page now SSRs as an empty thread + composer. If a future slice (β.4 left-rail) re-introduces seeded state, those assertions need to come back. **Discovery — vi.fn typing for `fetch`-shape mocks:** `vi.fn(async () => …)` infers `[]` arg tuple under strict TS, breaking `fakeFetch.mock.calls[0]` destructuring. Explicit `vi.fn<(input: string, init?: RequestInit) => Promise<Response>>(…)` fixes it — codify for future SSE/HTTP-shaped test slices. **Decision deferred to β.3:** the empty-state hint mentions "pick a prompt" but `GeoffComposer` chips currently only pre-fill (no auto-submit). Acceptable for MVP — β.3 (token parsing) is the right slice to revisit chip UX if the user wants tap-to-send instead of tap-to-prefill.

- **2026-05-23 — K1a-β.1 shipped (threads store, commit pending sweep).** K1a-β was too large for one iteration (5 deliverables: store + SSE wiring + token parsing + 2 rails + ~12 tests), so split into K1a-β.1–β.4 per the ordered checklist above. β.1 (threads store) is the foundation the other sub-slices depend on. Tests 518 → 533 (+15 in `src/lib/stores/threadsStore.test.ts`), svelte-check 0/0. **Manual sweep for β.1 is trivial — no UI change ships in this slice**, the route still renders the static SEED conversation. To smoke-test the store in isolation: open devtools on `/oracle`, run `localStorage.getItem('kicker:threads')` → expect `null`; the next sub-slice (β.2) makes the store user-visible. **Discovery — `OracleMessage` is intentionally richer than the wire `ChatMessage`:** the store carries `timestamp` + optional `streaming: true` flag for UI use, and `toApiMessages()` strips both for `/api/chat`. β.2 must call `toApiMessages(thread.messages)` rather than passing raw messages — passing raw will 400 (`invalid_messages`) because the server's `isChatMessage` guard only accepts `{role, content}`. **Discovery — `finalizeStreaming()` destructures `streaming` out** instead of setting it to `false`; this means `{#if msg.streaming}` is the correct UI conditional (no falsy-vs-undefined fork). **Decision deferred for β.2:** error handling UX when SSE returns 429 / 503 / mid-stream `{"error":…}` — recommend appending a tagged `OracleMessage` of `role: 'assistant'` with `content: '⚠ …'` so it shows inline in the thread (rather than a toast) and gets persisted with the conversation history; revisit at β.2 commit time.

- **2026-05-12 — K1a-α shipped (commit pending sweep).** Oracle route lives at `/oracle`, mounts both desktop + mobile shells, marks itself active in both navs. Three chat primitives shipped: `GeoffMessage` (3-col avatar / body / marginalia grid, persona-accent driven), `UserMessage` (right-aligned mono pill), `GeoffComposer` (prompt chips + persona pill + input + send button, all wired into local draft state; `onsubmit`/`onchip` callbacks exposed but not consumed yet). Test count 508 → 518 (10 new tests in `src/routes/oracle/page.test.ts`). svelte-check 0/0. **Sweep checklist for K1a-α:** visit `/oracle` → confirm Rule header shows the active persona's name; switch persona via Settings → revisit Oracle → confirm header + GeoffMessage prefix + composer pill all updated; resize to mobile → confirm 5-tab bottom-nav highlights `oracle`, mobile header shows persona short; tab through the composer → input focus visible, Send button disabled while empty, enabled after typing; click each of 4 prompt chips → confirm draft pre-fills; **no betting copy anywhere** on the route. Once that passes, K1a-β runs next: `/api/chat` streaming + threads store + FIXTURE/CHEERS token parsing + right-rail fixtures. **Discovery — `data-composer` boundary trap:** Svelte 5 SSR emits attributes adjacent in the same tag, and `\b` matches between alnum and `-`, so `/data-composer\b/` over-matches `data-composer-input`. Use `/data-composer(?![-\w])/` for parent markers when descendants share a prefix. Codifies the existing K0c "data-attr-prefix collision" note for the lookahead variant.

- **2026-05-12 — K0-cp auto-half shipped.** Playwright spec `frontend/e2e/checkpoint-k1-0.spec.ts` (5 sub-tests × `desktop-chrome` + `mobile-chrome` = 10 cases) — all green locally (11.9s). `frontend/playwright.config.ts` extended with both projects + `baseURL` + `reuseExistingServer`. Sweep doc `docs/the-kicker-spec/sweep-k1-0.md` covers ~70 manual items + the rebase→ff→push→deploy→tag cutover playbook + a rollback recipe. **One environmental nit:** Playwright browsers had to be installed (`npx playwright install chromium` — the cached headless shell was v1208 but `@playwright/test` 1.59.1 wants v1217). Dev machines + CI need the same install before the spec runs. **Discovery — `:visible` pseudo is mandatory:** both desktop and mobile shells co-exist in the SSR DOM with Tailwind `lg:hidden` / `hidden lg:block` toggling visibility, so `.first()` resolves to the desktop shell on mobile viewports and Playwright reports `hidden`. The spec uses a `visible(selector)` helper to scope locators to the active shell. Future Playwright specs hitting any route that mounts both shells must follow the same pattern. **Next action is human-only:** run sweep doc → tick boxes → execute cutover playbook in this chat → tag `k1.0` at the deploy commit.

- **2026-05-12 — K0l-β shipped (commit landed, sweep pending).** Settings page now has functional API-key inputs and a 3-toggle Notifications panel. `PrefToggle` primitive in `lib/components/settings/PrefToggle.svelte`. Test count 502 → 508. svelte-check 0/0. **Manual sweep:** paste a real Football-Data key into `/settings` → API tab → reload, confirm it survives; toggle all 3 notifications, reload, confirm state restored. Once that passes, apply `k0.12` and flip K0l-α + K0l-β both to `[x]`. Then K0-cp is the only thing between us and `k1.0`. **Discovery — BYOK forwarding gap:** `/api/chat` (`frontend/src/routes/api/chat/+server.ts`) calls `getAnthropic()` which reads `process.env.ANTHROPIC_API_KEY` only — there's no `x-anthropic-api-key` header path. The locally-saved key is currently dormant. Wiring this is a separate slice (proposed K0l-γ or fold into K1d/K2c). Not blocking K1.0 since the server key works.

- **2026-05-12 — K0l-α shipped (commit pending sweep).** Settings route lives at `/settings`, sub-tab routing via URL hash, all 6 panels render, pundit picker is live-wired (clicking a `PunditPickerCard` calls `personaStore.set` directly — no save button, matches the K0d "switch is immediate" UX). Test count 492 → 502 (10 new tests in `src/routes/settings/page.test.ts`). svelte-check 0/0. **Tag deliberately not applied** — `k0.12` holds until K0l-β lands so the user gets one cutover sweep covering the full settings surface. **Next loop runs K0l-β** (API-key persistence + `PrefToggle`) per the slice description above. **Sweep doc:** `docs/the-kicker-spec/sweep-k0l.md` already gates α (manual sweep can be folded into the K0l-β sweep at user discretion).

- **2026-05-03 — K0k swept and signed off → `k0.11` tagged.** Predictions screen ships the moat surface: 4 KPI tiles (MODEL ACCURACY / BRIER / CALIBRATION / MODEL EDGE-red), per-row marketImplied ghost-bar overlay + valueEdge chip on the THIS WEEK'S PICKS grid, SETTLED RESULTS log with hit/exact ticks. K0j (Fixtures) also sweep-confirmed → `k0.10` tag applied. **Plan streamlined this iteration:** completed slices moved to `COMPLETED_ITEMS.md`; addressed Notes/discoveries also relocated. **Project-root `CLAUDE.md` added** (different concern from `ClaudeRalph/CLAUDE.md`): root explains "what this repo is" for fresh Claude sessions, ClaudeRalph explains "how to run the loop".

- **2026-05-12 — K0-cp deliverables landed + swept clean.** Playwright checkpoint `frontend/e2e/checkpoint-k1-0.spec.ts` ships 5 sub-tests × 2 projects (desktop-chrome + mobile-chrome) = 10 total, all green (13.3s incl. build). Manual sweep doc `docs/the-kicker-spec/sweep-k1-0.md` (54 items) walked top-to-bottom by user, sign-off received. One discovery surfaced during sweep, logged as `K1j — Predict GW button` follow-up below. **Ready for the production cutover** — `kicker-mvp` → `main` rebase + push + Vercel auto-deploy + `k1.0` tag. Awaiting explicit user "go" on the push.

- **2026-05-12 — K0l fully signed off → `k0.12` tagged.** K0l-α + K0l-β both `[x]`. Settings route ships full functionality: sub-tab routing via URL hash, live pundit picker, Save & reload + Remove buttons on both API keys, 3 PrefToggles on the Notifications panel. 508 tests, svelte-check 0/0.

- **2026-05-12 — K0-cp terminator condition for the next loop:**
  - **Two distinct deliverables, ship in order:**
    1. **Auto-half (Playwright spec):** Write `frontend/e2e/checkpoint-k1-0.spec.ts` with 5 sub-tests covering the MVP routes — `/today` renders KickerShell + 4 KPI tiles + hero MatchSheetCard; `/fixtures` renders the date-grouped list with filter chips; `/predictions` renders the KPI strip + THIS WEEK'S PICKS grid + SETTLED RESULTS log; `/settings` renders the 6 sub-nav rows + the pundit grid + the API field inputs; `/onboarding` renders the 3-screen flow. Each spec runs on `desktop-chrome` and `mobile-chrome` projects. Mobile sub-test additionally verifies the 5-tab bottom-nav order (`today/fixtures/predictions/oracle/more`) with NO `betting` label. Each route asserts no `/value bets|bankroll|kelly/i` copy on the rendered page. Spec uses `mockFootballApi` helper if needed for deterministic state. Ship + commit. Slice tag NOT applied yet — the manual half follows.
    2. **Manual-half (sweep doc + cutover):** Write `docs/the-kicker-spec/sweep-k1-0.md` with the ~25-item checklist (persona switching mid-session, Today KPI tiles match v3 backtest within 0.01, Predictions value-edge chip signs, mobile bottom-nav has Oracle stub pointing somewhere placeholder-safe, axe-core a11y scan on all 5 routes, theme parity no dark-mode escape, hard-reload preserves persona + API keys, paste a real Football-Data key and confirm /today populates, click Remove and confirm fallback to empty state). **Loop stops after committing the sweep doc.** User runs sweep, ticks the checklist, then triggers the cutover playbook manually (described in the K0-cp slice body) — rebase `kicker-mvp` onto `main`, push, Vercel auto-deploys, smoke-test prod URL, apply `k1.0` tag at the deployment commit.
  - **Auto-validate (Playwright half):** `npx playwright test --prefix frontend e2e/checkpoint-k1-0.spec.ts` all-green on both projects. `npm run check --prefix frontend` 0/0. `npm run test --prefix frontend -- --run` all-green (Vitest unchanged at 508; Playwright is separate).
  - **Commit:** `feat(K0-cp): K1.0 cutover Playwright checkpoint + manual sweep doc`. Manual-gated; do NOT auto-apply `k1.0`, do NOT auto-flip `[x]`, do NOT push to `main` or trigger any deploy. The cutover is a human action.
  - **Critical sequencing:** the slice ships Playwright spec + sweep doc together, but the **manual sweep + cutover happens in this chat with me, not in the loop**. The loop's job ends at the commit; the user's job (with me) is the actual flip from v3 → Kicker on production.
  - **Reuse contracts:** Playwright spec pattern follows `e2e/` examples (if any from K-slices) or v3's archived `checkpoint-p0.spec.ts` / `checkpoint-p2.spec.ts` style on the `archive/v3-frontend` branch.
  - **What NOT to do:** do NOT run any cutover commands (no `git push origin main`, no `vercel deploy`, no DNS edits). Do NOT delete the v3 archive branch — it remains the recovery seat-belt. Do NOT modify any source code outside the Playwright spec + sweep doc.

